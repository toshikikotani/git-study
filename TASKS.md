# タスクボード

このファイルが開発ループの単一の入口。**作業を始めるときは必ずここを最初に読み、終わったら必ずここを更新する。**

- 詳細な定義は `docs/mvp-plan.md`。ここは状態を持つだけの場所
- 1タスク = 1セッション = 1コミット
- 実装中に見つかった作業は、その場で Backlog へ追記する。「あとで思い出す」は必ず失敗する

## 状態の定義

| 状態 | 意味 |
|---|---|
| **Now** | いま着手しているもの。**常に1件だけ** |
| **Next** | 依存が解けていて、すぐ着手できるもの |
| **Backlog** | やると決まっているが、依存が未解決か優先度が低いもの |
| **Blocked** | 本人の入力・外部要因を待っているもの。待っている対象を必ず書く |
| **Done** | 完了。共通の完了条件(`docs/mvp-plan.md`)を満たしたもの |

## ループの規約

1. **Now が空なら Next の先頭を Now に移す。** 選ぶ基準は `docs/mvp-plan.md` の「優先順位」
2. **Now は1件だけ。** 並行して手を出さない
3. タスクを終えたら Done へ移し、依存が解けた Backlog を Next へ繰り上げる
4. 実装中に見つけた作業は Backlog へ追記する。**タスクを増やすことを躊躇しない**
5. 設計判断が必要になったら `docs/decisions.md` に ADR を追記してから実装する
6. スキーマを変えたら `docs/schema.sql` と `supabase/migrations/` の両方を更新し、`./scripts/verify-schema.sh` を通す
7. Blocked に入れるときは「何を待っているか」を必ず書く。待ち対象が本人の入力なら、**それを聞く1行を残す**
8. 30分考えても進まないものは Blocked にして次へ行く。止まったまま粘らない

---

## Now

なし

## Next

なし。**`docs/mvp-plan.md` の M0〜M7(全28タスク)がすべて完了した。**
残るのは Blocked の B-1〜B-4(本人の入力・作業待ち)と、下記 Backlog の技術的負債・フェーズ2以降のみ。
`docs/mvp-plan.md`「全体の完了条件」8項目のうち、Discord への実送信(FR-21/24)と
朝配信(FR-30)の2項目は **コード・検証は完了しているが B-3(Webhook 未発行)の間は
実際には届かない**(検知・生成は毎時/毎朝走り、alerts/daily_briefs には積み上がる。
B-3 解消後の次回実行から自動的に届き始める)。

## Backlog

**2026-09-12 時点で、下記フェーズ2/技術的負債のうち着手可能なものはすべて完了した(下記 Done)。**
残っているのは、本人の入力・外部認証情報が無いと着手できないものだけ。

### フェーズ2以降(MVP 対象外)
| ID | タスク | 対応 |
|---|---|---|
| P2-2 | 朝配信の本実装(市場・キャンペーン、FR-31 フィルタ拡充) | FR-30, FR-31 |
| P4-2 | 口座連携 API の再検討 | 9.5 |

P2-1(Gmail 連携)は M2-7 系列(2026-09-08)で完了済みだったため削除。P3-1(副業トラッカー)・
P3-2(転職準備チェックリスト)・ストリーク(FR-62、旧 P2-3)は本人の希望で前倒し実装済み(下記 Done)。
P2-2 は市場・案件動向の実データが要る(現状 Web 検索・AI 生成の仕組みを持たない、ADR-020)ため、
着手すると「調べずに書いた市場情報」を配信することになりかねず保留。P4-2 は具体的な連携先
(Plaid・MoneyForward 等)の選定と契約が本人の判断事項のため保留。

**2026-09-13 に調査**:個人開発のシングルユーザーアプリでは、現時点でどの連携先も現実的に使えない。

- **Moneytree LINK**:公式サイトに「API接続につきましては、セキュリティの観点から法人のお客さまに
  限定させていただいております」と明記。個人は契約自体ができない。より軽量な「LINK API Private」
  も対象は中小企業の自社利用向けで、法人であることが前提(反社チェックを含む審査・NDA締結が必要)
- **Plaid**:日本の金融機関に未対応(2026年時点)。日本展開は早くても2027年以降の見込み
- 銀行の直APIを自分で叩く:電子決済等代行業者としてFSAへの登録が必要で、個人には非現実的

つまり選定・契約の問題ではなく、**個人が使える連携先が現状存在しない**。状況が変わる可能性がある
のは、Plaidの日本対応が進むか、Moneytree等が個人向けプランを新設した場合。それまではCSV・Gmail・
レシート撮影の3経路が実質的な唯一の手段(要求仕様書9.5節の「完済後に再検討」とも整合する)。

### S8 自動化強化(本人発案、2026-09-12)
「開く手間を減らす」方向。設計原則(記録の手間を最小化)と相性が良いため優先度は高め。
P5-2・P5-3 は本セッションで完了(下記 Done)。

| ID | タスク | サイズ | 対応 |
|---|---|---|---|
| P5-1 | 給料日チェックリストを Discord から1タップで完結できるようにする | M | FR-15 |

- **P5-1**:今は `/payday` を開いて手動でチェックする(M4-4)。Discord の Webhook(送信専用)だけでは
  実現できず、Discord Bot + Interactions API(スラッシュコマンドまたはメッセージのボタン)が要る。
  現状の通知基盤(`lib/discord.ts`)は送信専用の設計のため、双方向にするなら別のクライアント
  (discord.js 等)と、Interaction エンドポイントを受ける新しい Route Handler が必要になる。
  B-3(Webhook 発行)より一段大きい設定(Bot 作成・権限・署名検証)が要るため、着手前に本人と
  「Bot を作る前提でよいか」を確認した方がよい(本セッションでは未着手のまま見送った)。

### S9 分析・可視化(本人発案、2026-09-12)
P6-1・P6-2・P6-3 は本セッションで完了(下記 Done)。ホーム画面は「3つの数字だけ」
(FR-61、設計原則)を維持したまま、可視化は `/reports`(新設)に閉じ込めた。

### S10 通知チャネル拡張(本人発案、2026-09-13)
P7-2 は本セッションで完了(下記 Done)。LINE のチャネルアクセストークン・userId が
未発行のため、実際に届くのは B-6(Blocked)解消後。

### 改善・技術的負債
ここには実装中に気づいたことを積む。空でよい。

| ID | タスク | 理由 |
|---|---|---|
| T-2 | CSV アダプタの fixture を実ファイル(匿名化)で用意する | 実フォーマットが判明してから |
| T-3 | ESLint 10 へ上げる | 2026-09-12 に再確認:`eslint-plugin-react`(最新 7.37.5)の peerDependencies が依然 `eslint: ^3〜^9.7` までで `^10` を含まない。`eslint-config-next` 自体は `eslint>=9.0.0` で上限が無く上げられるが、`eslint-plugin-react` 側が対応するまで見送り。上流の対応待ち |
| T-13 | `src/features/import/adapters.ts` の `guessMapping` / `mapRow` を分割する | 列推測とパースが1関数に同居している。T-2(実ファイル fixture)と合わせてやると安全 |
| T-16 | Supabase の Auth 設定(Site URL / Redirect URLs)がリポジトリに残っていない | ダッシュボード側の設定のみで管理している。プロジェクトを作り直す場合に再設定が必要。`supabase/config.toml` の `[auth]` セクションで宣言的に管理する方法もあるが、現状は未導入。本番 Auth 設定を誤って書き換えるリスクがあるため、正確な現在値(本番 URL・許可リダイレクト先)を本人と確認してから着手する |
| T-25 | `rescued_emails` の2マイグレーションを本番 Supabase プロジェクトへ適用する | T-11 で追加した `20260908001200_rescued_emails.sql` / `20260908001300_rescued_emails_rls.sql` は、このセッションに Supabase Management API の資格情報が無く本番へ適用できなかった(B-3 の Discord Webhook と同種のサンドボックス制約)。ローカル PostgreSQL では `verify-schema.sh`/`verify-migrations.sh` 双方で検証済み(911項目一致)。未適用の間は `recordRescuedEmailAsAdmin()` 呼び出しが静かに失敗するだけで(catch 済み)、他の機能には影響しない。適用後は自動的に記録が始まる |
| T-26 | `net_worth_snapshots` の2マイグレーションを本番 Supabase プロジェクトへ適用する | P6-3 で追加した `20260912000100_net_worth_snapshots.sql` / `20260912000200_net_worth_snapshots_rls.sql` は、T-25 と同種のサンドボックス制約(Supabase Management API の資格情報が無い)で本番へ適用できなかった。ローカル PostgreSQL では `verify-schema.sh`/`verify-migrations.sh` 双方で検証済み(926項目一致)。未適用の間は `recordNetWorthSnapshotAsAdmin()` 呼び出しが静かに失敗するだけで(`detect-alerts` cron 内で catch 済み)、`/reports` の資産推移グラフも空状態を出すだけで画面は落ちない(`loadNetWorthTrend().catch(() => [])`)。実際に本番 Supabase(テーブル未作成の状態)に対して両方の失敗経路を検証済み。適用後、次の月末の cron 実行から自動的に記録が始まる |

T-6・T-9・T-11・T-12・T-22・T-23・T-24 は本セッションで完了(下記 Done)。全体的な「5行ルール」適用は
際限がないので、次に触る画面・関数から都度直す方針は継続(一括では手を出さない)。

## Blocked

| ID | タスク | 待っているもの |
|---|---|---|
| B-1 | 負債の正確な内訳を `debts` に入力し `is_estimated` を落とす | **本人の棚卸し**(全借入先の残高・金利・件数・返済日)。仮値のままでも開発は進むが、完済予定日は確定表示できない |
| B-2 | 実際の金融機関 CSV でアダプタを検証する | **本人が利用中の銀行・カードの明細ファイル**(1ヶ月分) |
| B-3 | Discord Webhook URL を GitHub Secrets(`CRON_SECRET` と同じ経路)/ Vercel に `DISCORD_WEBHOOK_URL` として設定する | **本人による Webhook 発行**。M3-1/M3-3/M5-2 は実装・検証済み(ローカル HTTP スタブで送信経路を確認)で、未設定の間は検知・生成だけ行い実送信をスキップする設計のため着手はブロックしていない。設定されれば次回の cron 実行(毎時 / 毎朝07:00 JST)から自動的に届き始める |
| B-6 | LINE Messaging API のチャネルアクセストークン・userId を発行し、`LINE_CHANNEL_ACCESS_TOKEN`/`LINE_USER_ID` として GitHub Secrets / Vercel に設定する | **本人による発行**(`.env.example` に手順を記載)。P7-2 は実装・検証済み(fetch を差し替えたローカルスタブで送信経路・失敗時の扱いを実際の Supabase に対して確認)で、未設定の間は Discord 側だけで動く(B-3 と同じ「あれば使う」設計、着手はブロックしていない)。設定されれば次回の cron 実行から LINE にも届き始める |
| B-8 | Vercel 本番環境変数の `CRON_SECRET` を、GitHub Secrets の `CRON_SECRET` と**完全に一致する値**に直す | **本人による設定**。**2026-09-15 に実際のジョブログ・本番 Supabase への直接照会で再調査**:以前の症状(`APP_BASE_URL`/`CRON_SECRET` が GitHub Secrets に未設定、`curl: (3) URL rejected`)はすでに解消しており、GitHub Secrets 側は両方とも設定済みと確認できた(実際に本番URLへ到達している)。しかし直近の実行(2026-09-15 23:22 時点でも失敗)は一貫して `{"error":"認証情報が正しくありません"}` / ステータス 401 を返している——これは `isAuthorized()` が `getCronSecret()` で例外を投げていない(＝ Vercel 側にも `CRON_SECRET` は設定されている)のに比較が一致しない状態で、**GitHub Secrets と Vercel の `CRON_SECRET` の値が食い違っている**ことを意味する。`job_runs` テーブルを直接照会し、`trigger_source='github_actions'` の成功記録が1件も無いことも確認済み(pg_cron 経由の keepalive 8件のみ成功、これは無関係な別経路)。両方に**同じ文字列**を入れ直せば直る |
| B-9 | Google Cloud Console でOAuthクライアントを作成し、`GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` を発行、`/settings/google` の「Google と連携する」で同意して `GOOGLE_REFRESH_TOKEN` を取得・環境変数へ設定する | **本人による作業**(手順は `.env.example` に記載。ADR-022)。Google Cloud Console でのプロジェクト作成・API有効化・OAuth同意画面の設定・ブラウザでの認可はいずれも本人のGoogleアカウントでの操作が要るため、このセッションからは行えない(B-3/B-6 と同種のサンドボックス制約)。未設定の間は `/api/cron/sync-google` が両方(カレンダー同期・スプレッドシートバックアップ)を静かにスキップするだけでジョブ自体は失敗しない。設定後、次回の cron 実行(毎日07:30 JST)から自動的に動き始める |
| B-11 | LINE Developers コンソールでチャネルシークレットを控え、`LINE_CHANNEL_SECRET`・`LINE_RECEIPT_ACCOUNT_ID`(取り込み先口座のUUID)を GitHub Secrets / Vercel に設定し、Messaging API設定で Webhook URL(`${APP_BASE_URL}/api/webhooks/line`)を有効化する | **本人による設定**(手順は `.env.example` に記載、ADR-025)。B-6(`LINE_CHANNEL_ACCESS_TOKEN`/`LINE_USER_ID`)と`ANTHROPIC_API_KEY`も揃って初めて動く。P10-5 は実装・検証済み(型チェック・lint・本番ビルド・ユニットテスト全通過、`/api/webhooks/line`のルート生成も確認)だが、このセッションにはLINEの資格情報が無く、実際のWebhook配信・署名検証・画像取得は未確認。設定されれば、LINE公式アカウントへレシート写真を送るだけで自動取り込みが始まる |
| B-17 | `receipt_items.category_id` を本番へ適用する — `supabase/apply-pending.sql` を Supabase ダッシュボードの SQL Editor に貼って Run する(1件の ALTER TABLE のみ。何度実行しても壊れず、最後に出る表で ok を確認できる) | **本人による適用**(B-16 と同じ制約:このサンドボックスに Management API 資格情報が無い)。適用されるまで品目の分類結果(P10-34、ADR-035)は保存を試みても列が無くエラーになる設計ではなく、`replaceReceiptItems()` が `category_id` を渡そうとしても列が無ければ Supabase 側がエラーを返すため、レシート取り込み時の警告として表示される(取り込み自体は失敗しない)。適用後は自動的に使えるようになる |

## Done

| ID | タスク | 完了日 |
|---|---|---|
| D-1 | 要求仕様書 v0.1 を `docs/requirements.md` として取り込み | 2026-09-08 |
| D-2 | `docs/decisions.md` — 14章の未決事項に初期値を選定(ADR-001〜015) | 2026-09-08 |
| D-3 | `docs/schema.sql` — 7章のデータモデルを PostgreSQL スキーマ化(26テーブル / 制約 / 索引 / RLS / シミュレーション関数) | 2026-09-08 |
| D-4 | `docs/architecture.md` — 8章に基づく構成・ディレクトリ・責務分担 | 2026-09-08 |
| D-5 | `docs/mvp-plan.md` — 11章の MVP を1セッション粒度の28タスクへ分割 | 2026-09-08 |
| D-6 | `scripts/verify-schema.sh` — スキーマをローカル PostgreSQL で検証(制約 / 関数 / RLS) | 2026-09-08 |
| D-7 | `TASKS.md` — 本ボードの作成 | 2026-09-08 |
| M0-1 | Next.js 16 / TS strict / Tailwind 4 / ESLint / Prettier / Vitest の初期化 | 2026-09-08 |
| M0-4 | `src/lib/env.ts`(zod)、`.env.example`、CI(lint / format / typecheck / test / build / スキーマ検証) | 2026-09-08 |
| M0-5 | ルートレイアウト、JST ヘルパ(`src/lib/date.ts`)、`formatYen()`、ホームの器 | 2026-09-08 |
| M1-1 | `src/domain/payoff.ts` と SQL 関数の一致検証(golden fixture 方式、CI で乖離を検出) | 2026-09-08 |
| M1-5 | ホームの完済カウントダウンと進捗ゲージ(数値は仮置き。M4-2 で実データへ) | 2026-09-08 |
| M2-1 | CSV パーサとアダプタ(Shift_JIS 自動判定 / RFC4180 / 和暦 / 出金入金2列 / 支払区分) | 2026-09-08 |
| M2-2 | 明細タブ(一覧・CSV取り込み・列の自動推測・重複排除・リボ検知の表示) | 2026-09-08 |
| M2-3 | ルールベース分類エンジン(5種のマッチ / 優先度 / FR-21 検知 / 修正からの学習) | 2026-09-08 |
| M4-1 | `domain/budget.ts` 残額計算(振替・対象外の除外 / 繰越 / FR-20 の閾値判定) | 2026-09-08 |
| T-1 | `docs/schema.sql` を `supabase/migrations/` へ分割し、乖離検出を CI に追加(890項目を比較) | 2026-09-08 |
| ADR-016 | カテゴリの表示名・予算・ホーム表示枠を `categories` に集約し、UI の直書きを廃止 | 2026-09-08 |
| M2-7 | 通知メールの取り込み(ラベル辞書の解析 / IMAP 検索の組み立て / 2段の重複排除 / Gmail 設定画面 / 貼り付けの逃げ道)ADR-018 | 2026-09-08 |
| M2-7a | 読めなかったメールを `claude-haiku-4-5` で救済(抽出は AI、リボ検知は正規表現のまま)ADR-019 | 2026-09-08 |
| M2-7b | `ImapMailSource` の実装(imapflow + mailparser で `MailSource` を満たす。テストは偽クライアントで資格情報なしに実行) | 2026-09-08 |
| M0-2 | Supabase プロジェクト作成(Tokyo リージョン)、マイグレーション9本を Management API 経由で適用、本人のアカウントで `seed_defaults()` 実行(カテゴリ9・ルール3・振替ルール4・シナリオ2)、`gen types typescript` で `src/lib/supabase/types.ts` 生成 | 2026-09-08 |
| M0-3 | 認証(Magic Link、`shouldCreateUser: false` でサインアップ拒否)、`proxy.ts` による認証ガード(画面は `/login` へリダイレクト、API は 401)、`src/lib/supabase/{client,server,admin}.ts` の分離。副作用として T-10(`/api/import/email` の無認証呼び出し)を解消 | 2026-09-08 |
| T-14 | リファクタ:取り込み3画面(CSV/貼り付け/Gmail設定)の重複排除と mail-sync の分割 | `DETECTION_RULES` の複製を `DEFAULT_DETECTION_RULES`(rules.ts)に一本化、共通 `<Card>`(components/ui)を切り出して9箇所のインライン複製を解消、「分類→StoredTransaction化→保存」を `features/transactions/import-pipeline.ts` に集約(CSV・貼り付け・mail-sync の3箇所が同じロジックを持っていた)。`syncFromMailbox`(82行)を `parseMessage` / `pushWarnings` に分割し、`import-pipeline.ts` の `buildPreview` を呼ぶ形にして3つ目の複製も解消。テスト10件追加、既存317件は無変更で通過。2026-09-08 |
| D-8 | `docs/glossary.md` — DB列名↔TS識別子↔画面表示名の対応表、接尾辞規約(`...Yen`/`...On`/`...At`/`...Id`/`is...`/`...Rate`)、型の置き場所、データアクセス層の命名規約(list/get/create/update)を制定。既存コードは無改修、以後のコードが従う基準 | 2026-09-08 |
| M1-2 | 負債の登録・編集画面。`src/features/debts/store.ts`(Supabase 読み書き、この機能で最初に本物の DB を使う層)、`src/domain/debt.ts`(借入先・返済日の検証)、`money.ts` に `parseAnnualRate()` を追加。Server Action(`app/(app)/debts/actions.ts`)経由で作成・更新。実際の Supabase プロジェクトに対して認証込みで読み書き・RLS を検証済み(本セッション内で一時セッションを発行して確認)。あわせて `seed_defaults()` に ADR-006 の負債3件が投入されていない欠落を発見・修正(新規マイグレーション、schema.sql 反映、decisions.md 追記)。本番へ適用し、既存ユーザーに対して `seed_defaults()` を再実行してカードA・カードB・消費者金融Cの3件を投入済み | 2026-09-08 |
| ADR-012 | Vercel(Hobby)へ初回デプロイ。プロジェクト作成、環境変数3件(`NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY`)を production に設定、CLI から本番デプロイ。認証ガードが本番でも効くことを確認(未ログインで `/login` へリダイレクト)。公開 URL: `https://git-study-lemon.vercel.app` | 2026-09-08 |
| T-15 | Vercel プロジェクトと GitHub リポジトリを連携(本人が GitHub App をインストール)。`main` への push で自動デプロイになることを確認 | 2026-09-08 |
| T-17 | Magic Link のリンク先が localhost になるバグを修正。原因は Supabase の Site URL が既定値 `http://localhost:3000` のままだったこと。`config/auth` API で `site_url` を本番 URL へ、`uri_allow_list` に本番 URL とローカル開発用ポートを設定。実際にリンクを発行して本番 URL へリダイレクトされることを確認 | 2026-09-08 |
| T-18 | 負債タブ等の切り替えが遅い不具合を修正。原因は Vercel の Function リージョンが `iad1`(米国東部)、Supabase が Tokyo で、動的ページ1回の描画のたびに太平洋を2往復していたこと。`serverlessFunctionRegion` と `functionDefaultRegions` を `hnd1`(東京)へ変更し再デプロイ | 2026-09-08 |
| T-19 | タブ切り替え直後にローディング表示を出す。`src/components/ui/skeleton.tsx` と、ホーム・`/debts` それぞれの `loading.tsx`(Next.js の規約、データ取得中は自動でこちらが出る)を追加。Playwright で実際にクリック直後スケルトンが出て、データ到着後に本来の内容(カードA・推定バッジ)へ差し替わることを確認 | 2026-09-08 |
| M2-4 | `src/features/classification/ai.ts` — Claude Haiku 4.5 によるバッチ分類。**関数自体のみ**(本人の選択により、取り込み経路への接続は M2-3b で別途行う)。`ClaudeTransactionClassifier.classifyMany()` が既定40件ずつに分けてリクエストし(100件は3リクエスト以内)、カテゴリは uuid でなく `code` でやり取り(トークン節約・改名耐性)。応答件数が入力と不一致なら当て推量せずバッチ全体を確認待ちにする、選択肢に無い code は null 扱いにするなど、モデルの出力を信用しきらない設計はメール抽出(ADR-019)と同型。`applyConfidenceThreshold()` で「AI がどう思ったか」と「閾値を信用するかどうか」を分離(ADR-010)。テスト13件追加(偽 client によるバッチ分割・失敗時の握り潰さない挙動・閾値境界)。あわせて `app_settings.classification_model` の既定値に日付サフィックス付きモデル ID(`claude-haiku-4-5-20251001`)が紛れ込んでいた誤りを発見し、修正マイグレーションと `docs/schema.sql` を追加(本番 DB への適用は未実施。既存の分類コードはこの設定値を参照せず定数で動くため、現時点でこのバグが機能をブロックすることはない) | 2026-09-08 |
| M1-3 | `/debts` に完済シミュレーション区画を追加。`domain/payoff.ts` の `comparePlans()`(既存・純粋関数)をそのまま使い、Client Component(`payoff-simulation.tsx`)でスライダーを動かすたびサーバーを介さず即時再計算。アバランチ/スノーボールの切り替え、最低返済のみとの比較(短縮月数・削減利息・完済見込み日)を表示。スライダーの下限は最低返済額の合計、上限は残高合計と初期値の3倍のいずれか大きい方。あわせて `app_settings` の読み出しを `src/features/settings/store.ts` に切り出し(`home/summary.ts` の重複を解消)、`debts/store.ts` に `toPayoffDebt()` を追加して `Debt → domain/payoff.ts の Debt` への変換ロジックの重複も解消。実データ(残債合計1,000,000円・最低返済合計28,000円)で実際に画面を描画し、スライダー操作・戦略切り替えで数字が往復なしに変わることを Playwright で確認(月10万円: 45ヶ月短縮・289,723円削減、月50万円に変更: 53ヶ月短縮・346,876円削減) | 2026-09-08 |
| M4-2 | ホームの残額表示を実データに接続。`loadHomeSummary()` が Supabase から debts / app_settings / categories / budgets(当月分。無ければ `default_monthly_budget_yen` で代用)/ transactions(当月・対象カテゴリ)/ debt_payments(当月の元本減少)を読むよう差し替え、`PLACEHOLDER_*` を削除。あわせて `debts.original_principal_yen` をフォームに追加(任意入力。進捗ゲージの分母。未入力なら現在残高で代用し、その負債単体の進捗は0%からになる)。`loadHomeSummary()` は Supabase(`next/headers` の `cookies()`)に触れる関数になったためユニットテストの対象から外し(純粋関数の `buildHomeTiles`/`computePayoffSummary` は継続してテスト)、実データでの検証は本セッション内で一時セッションを発行してホーム画面を実際に描画し確認した(残債合計1,000,000円、生活費・聖域タイルとも実データ(0円)で表示) | 2026-09-08 |
| T-20 | ログインをパスワード優先に変更(ADR-011 改定) | 毎回メールを開いてリンクを押す手間が継続利用を阻害していたため。`/login` にパスワード/メールリンクの2タブを用意し、パスワードを既定に。`src/domain/auth.ts` に `assertPassword`/`assertPasswordConfirmed` を追加(8文字以上・確認一致)。`/settings/password` に Server Action(`updateUser({password})`)を新設し、Magic Link ログイン後は毎回ここへ誘導して次回からパスワードで入れるようにした。実際に Node 側で Magic Link セッションを発行してブラウザへ注入し、実サーバー(localhost)相手にパスワード設定フォームを送信 → ホームへリダイレクトを確認。設定したパスワードでの `signInWithPassword` 成功・誤ったパスワードでの拒否も実際の Supabase プロジェクトに対して確認済み(ブラウザから Supabase への直接リクエストはこのサンドボックスのプロキシ制約で検証できないため、そこだけ Node 側の直接呼び出しで代替検証)。テスト4件追加 | 2026-09-08 |
| M6-1 | 口座(カード)の登録・編集画面(FR-16、実運用フィードバックで追加した S6 の初手)。`/accounts`。`src/features/accounts/store.ts`(`accounts` の list/create/update)、`src/domain/account.ts`(口座名・締め日・支払日の検証)。締め日・支払日の 1〜31 範囲チェックは `domain/debt.ts` の `assertPaymentDay` と全く同じロジックだったため、`src/lib/date.ts` に `isValidDayOfMonth()` として切り出し、両ドメインから参照する形に統一(既存の返済日テストは無改修で通過)。`/transactions` に「口座」への導線を追加。実際に Supabase セッションを発行し、口座の新規登録・一覧表示・リロード後の永続確認・編集・空文字での更新拒否まで実ブラウザ操作で確認済み。テスト8件追加(account 5件、既存 debt テストは回帰なし) | 2026-09-09 |
| M7-1 | 投資額の自動算出ロジック(FR-50, FR-52。本人の希望で S7 として着手)。`src/domain/investment.ts` の `computeInvestmentPlan()` — 返済目標額 × `investment_ratio_of_repayment` を投資総額とし、`is_high_risk_unlocked` が立つまでは全額インデックス枠、立った後は `high_risk_allocation_ratio` でインデックス/高リスクに分ける(純粋関数)。`features/settings/store.ts` の `AppSettings` にこの3列を追加。`/investments` で「今月の投資目安」を表示し、`/debts` から導線を追加。実データ(返済目標10万円・比率20%)で実際に画面を描画し、20,000円と表示されること、`/debts` からのリンク遷移を Playwright で確認。テスト5件追加 | 2026-09-09 |
| M1-4 | 借り換えシミュレーション(FR-04)。`domain/payoff.ts` に `compareRefinance()` を追加 — `comparePlans()`(返済額を変えた効果)と対になる、金利だけを変えた効果を見る純粋関数。`/debts` に「借り換えを試す」区画(`refinance-simulation.tsx`)を追加し、年利入力に応じて即時再計算。`repayment_scenarios` への保存(`src/features/scenarios/store.ts`、`user_id,name` の unique 制約に upsert)・削除・一覧を実装し、`src/domain/scenario.ts` にシナリオ名の検証を追加。実際に Supabase セッションを発行し、年利変更で数字が変わること、保存したシナリオがリロード後も残ること、削除で消えることを実ブラウザ操作で確認済み(検証用シナリオは削除済み、既存の`最低返済のみ`/`月10万円返済`シードは対象外)。テスト4件追加(compareRefinance 2件、assertScenarioName 2件、既存 payoff テストは回帰なし) | 2026-09-09 |
| M4-3 | 振替ルール編集画面(FR-15)。`/payday` を置きページから実画面へ(T-5)。`src/features/transfer-rules/store.ts`(list/create/update/delete、`listCategoryOptions()`)、`src/domain/transfer-rule.ts`(ルール名・金額指定方式ごとの検証。fixed/percentage/remainder で必要な列だけ埋める)。並び替えは隣接2件の `execution_order` を負の一時値を経由して入れ替え、`ux_transfer_rules_order`(一意制約)に一時的にも触れないようにした。DB 制約違反(`ux_transfer_rules_remainder` など)を本人に伝わる文言に変換する `describeConstraint()` を追加。実際に Supabase セッションを発行し、シードの4ルール(返済→投資→聖域枠→生活費)の表示、並び替え(↑/↓ボタン、DB の `execution_order` で確認)、2件目の「残り全額」ルール作成が拒否されメッセージが出ること、新規作成・改名・削除の一連の流れを実データで確認済み(検証はすべて DB の実値で確認。Server Action 後の DOM 読み取りは revalidate のタイミングにより不安定だったため、断定は DB クエリで行った)。テスト8件追加 | 2026-09-09 |
| M3-2 | 残りの検知(FR-22 未取込 / FR-23 返済日前日)。`src/domain/alerts.ts`(純粋関数)に `detectInactivity()`/`detectPaymentDueTomorrow()` を追加。返済日は 29〜31 日指定をその月の実際の末日に丸めて比較(`domain/payoff.ts` と同じ考え方)。`src/features/alerts/store.ts` に `recordAlerts()`(`alerts` の `(user_id, dedup_key)` 一意制約へ `upsert` + `ignoreDuplicates` で重複を静かに無視)と、実データで動く `detectAndRecordPaymentDueAlerts()` を追加。FR-22 側は「取り込みの空白日数」の判定ロジックは完成しているが、読み出す先の `transactions` テーブルが T-7 まで空のため接続は見送り(T-21 に記録)。実際の Supabase プロジェクトに対し、実セッションでの `debts` 取得 → 候補生成 → `alerts` への upsert → 同じ候補での再実行が重複を作らないこと(0件挿入)を確認し、検証用データは削除済み。テスト10件追加 | 2026-09-09 |
| M0-6 | keepalive ジョブ(NFR-05)。`app/api/cron/keepalive/route.ts` — `Authorization: Bearer ${CRON_SECRET}` を `timingSafeEqual` で検証(不一致・欠落は401)、全ユーザー分の `job_runs` 行を `trigger_source='github_actions'` で書き込む。`.github/workflows/keepalive.yml`(日次 `18:00 UTC` = 03:00 JST。pg_cron 側の 03:15 JST とはずらして二重化、ADR-009)。副作用として `src/lib/env.ts` の `getServerEnv()` を発見・修正:1つの必須スキーマに `SUPABASE_SERVICE_ROLE_KEY`/`ANTHROPIC_API_KEY`/`DISCORD_WEBHOOK_URL`/`CRON_SECRET` をまとめていたため、未設定の Discord/Anthropic キーに `CRON_SECRET` だけを使いたい呼び出し元まで巻き添えでエラーになる欠陥があった(このルートで初めて顕在化するところだった)。`getGmailEnv()` と同じ「機能ごとに検証する」方針で `getSupabaseServiceRoleKey()`/`getCronSecret()`/`getAnthropicApiKey()`/`getDiscordWebhookUrl()` の4関数に分割、`tests/domain/env.test.ts` を新API向けに書き換え。ローカルに `CRON_SECRET` を新規発行して実際の Supabase プロジェクトに対して検証:認証なし→401、誤った秘密→401、正しい秘密→200 かつ `job_runs` に1行追加されることを確認。検証中、`started_at` を DB のデフォルト(INSERT実行時刻)任せにすると `finished_at` より後になり得る不整合を発見・修正(アプリ側で計測した開始時刻を明示的に渡す形に変更)。検証用の行は削除済み(pg_cron 側の実行記録は温存)。`docs/env.example` の `CRON_SECRET`/`APP_BASE_URL` はそのまま流用 | 2026-09-09 |
| M2-3b | 分類エンジンを取り込み経路へ接続する(T-8)。CSV 取り込み・メール貼り付けの両画面で、ルール(`DEFAULT_DETECTION_RULES`)に当たらず「確認待ち」のまま残る明細を AI 分類(M2-4)へ回せるようにした。`src/features/classification/store.ts`(新規、`'server-only'`)— `listCategoryOptions()`(カテゴリを `code`/`name` で取得)、`classifyUnclassified()`(AI を呼び `applyConfidenceThreshold()` で確信度判定、`code`→`categoryId` への変換もここで行う。`ANTHROPIC_API_KEY` 未設定なら全件「確認待ち」のまま返す、エラーにはしない)。`app/api/classify/route.ts`(新規。認証は proxy.ts の関所が担う。1回300件までの上限)。`features/settings/store.ts` の `AppSettings` に `classificationConfidenceThreshold` を追加。画面側は `src/features/transactions/classify-client.ts`(2画面共通の fetch ラッパー)と、各ページの「AI に回す」ボタン(メール貼り付けの AI 救済ボタンと同じ、明示的に押されたときだけ呼ぶ設計)。AI の結果は `id` をキーにしたローカルの Map で重ね合わせ、`useEffect` 内での `setState` を避けて `useMemo` だけで導出(react-hooks/set-state-in-effect の指摘を受けて設計を直した)。実際の Supabase プロジェクトに対し、認証済みセッションで `/api/classify` を叩いて401→200になること、`ANTHROPIC_API_KEY` 未設定時に全件「確認待ち」+警告文が返ること、`categories`(code 9件)・`app_settings.classification_confidence_threshold`(0.8)の新しい読み出しが実際に動くことを確認(このサンドボックスに Playwright が依存関係として入っておらず、ブラウザでのクリック確認は未実施。API 契約はボタンが送るのと同じ形で確認済み)。なお `classification_rules`(DB 保存の学習済みルール)を取り込み経路へ実際に読み込ませる部分は本タスクの対象外(M2-5「確認待ちキューと修正のルール化」の責務。現状 `DEFAULT_DETECTION_RULES` はカテゴリを一切設定しないため、ルールで分類が付く明細は今のところ無い) | 2026-09-09 |
| M4-4 | 給料日チェックリスト(FR-15)。`/payday` にチェックリスト区画を追加(M4-3のルール編集と同じページ、上に表示)。`domain/transfer-rule.ts` に `computeTransferPlan()` を追加 — 給与は変動するため固定の月収設定を持たず、実際の入金額を本人に入力してもらい、それを execution_order 順に fixed/percentage/remainder へ按分する純粋関数(remainder は残り全部、fixed 合計が入金額を超える場合は以降を0円に切り詰めてマイナスにしない)。`src/features/transfer-runs/store.ts`(新規)の `resolvePaydayChecklistState()` が「①未完了の実行があればそれを最優先」「②無ければ今月分の run_on が既に存在するか」「③今日が今月の給料日(`app_settings.payday`、新規追加)以降か」の順で判定し、`needs_amount`(入金額の入力を促す)/`checklist`(消化中)/`none`(何も出さない)の3状態を返す。`createPaydayRun()` が振替ルールを取得して按分し `transfer_runs`+`transfer_run_items` を作成、`setTransferRunItemDone()` がチェックのたびに `actual_amount_yen` を予定額で埋め、全項目完了で実行自体も `status='completed'` にする。実際に Supabase セッションを発行し(`app_settings.payday` を検証のため一時的に本日の日付へ変更→検証後25へ復元)、入金額300,000円を入力→シードの4ルールが100,000/20,000/40,000/140,000円に按分されて表示→4項目を順にチェック→全完了メッセージと `status='completed'` を確認、再訪問時に同じ完了済み実行が再表示されない(ルール編集画面のみに戻る)ことをブラウザ操作とDBクエリの両方で確認。検証用データは削除済み。テスト5件追加(computeTransferPlan) | 2026-09-09 |
| M1-6 | 返済実績の記録と計画差分(FR-05)。`/debts` の各負債カードに「返済実績」区画を追加。`src/features/debts/payments-store.ts`(新規)の `recordDebtPayment()` — 元本・利息の内訳は本人に入力させず、現在残高×年利から利息分を自動算出(`monthlyInterest()`)し、残りを元本の減少に充てる。支払額が利息にも満たない月は元本を据え置く(残高は減らないがエラーにはしない)。過払い分は利息側に寄せて `principal_yen + interest_yen = amount_yen`(DB制約)を常に満たす。残高が0円になれば `status='paid_off'`・`paid_off_on` を設定(`ck_debts_paid_off_zero`/`ck_debts_paid_off_date` を満たす)。`domain/debt-payment.ts` に `computePlanActualDelta()`(純粋関数)を追加 — `original_principal_yen` を起点に `simulateDebtPayoff()`(既存)で計画を再現し、これまでの返済回数の行と実際の残高を比較して差分を表示(`original_principal_yen` 未入力時は計画の起点が定まらないため差分を出さない)。実際に Supabase セッションを発行し、①最低返済額どおりの返済(差分「計画どおり」)、②追加返済(差分「計画より12,000円進んでいます」、`is_extra=true`)、③残高5,000円の一時的な負債を作って全額+利息を返済し `status='paid_off'` への遷移と一覧からの除外を確認 — 利息・元本の自動按分を3ケースとも手計算と一致することを DB クエリで確認。検証用データは削除済み(一時負債は削除、シードのカードBは残高・当初元本を復元)。テスト5件追加(computePlanActualDelta) | 2026-09-10 |
| M2-5 | 確認待ちキューと修正のルール化(学習)。「確認待ち」のまま残る明細を本人が一括で見て修正できる画面と、その修正から学習ルールを増やす仕組みを追加。`/transactions/review`(新規、Server Component が `listCategoryOptions()` を渡し `ReviewQueue` に描画させる)— `reviewStatus === 'pending'` の明細だけを一覧し、カテゴリ選択+「確定」で `TransactionStore.update()`(新規メソッド)により修正、続けて `createLearnedRuleAction()`(新規 Server Action)を呼んで学習ルールを1件増やす。ルール作成の失敗は分類の確定自体をブロックせず、警告として蓄積して表示。`src/features/classification/store.ts` に `listActiveClassificationRules()`(DB保存のルールを画面向け `ClassificationRule` 型へ変換)と `createLearnedRule()`(既存の `buildLearnedRule()`/`extractKeyword()` を使い、摘要からキーワードを抽出して `classification_rules` へ `is_learned=true` で insert。キーワードが短すぎる場合は `RuleError` で拒否)を追加。`app/api/classification-rules/route.ts`(新規)と `src/features/transactions/rules-client.ts`(新規、`fetchLearnedRules()`)で、CSV 取り込み・メール貼り付けの両画面が起動時にこの DB 保存ルールを取得し `DEFAULT_DETECTION_RULES` と合わせて `buildPreview()` に渡すようにした(M2-3b で「対象外」と明記していた、学習ルールを取り込み経路へ実際に読み込ませる部分)。実装中に発見・修正したバグ:`buildPreviewRow()` がルールに当たって `categoryId` が設定された場合でも `categoryName` を常に `null` のまま返していたため、学習ルールが効いても画面には「未分類」と表示され続けていた(`DEFAULT_DETECTION_RULES` が `categoryId` を一切設定しないため今まで顕在化しなかった)。`buildPreview()`/`buildPreviewRow()` に `categoryNameById` マップ引数を追加して修正。実際に Supabase セッションを発行し、①確認待ちの明細をカテゴリ確定→`classification_rules` に正しい `pattern`/`category_id` で1行増えることを DB クエリで確認、②同じ店名の2件目の明細を貼り付け→`classifiedBy: 'rule'`(「AI に回す」ボタンが出ないことで確認)かつカテゴリ名が「未分類」ではなく正しい名称(修正前は失敗していた)で表示されることを Playwright のブラウザ操作で確認。検証用ルールは削除済み。テスト2件追加(categoryNameById の解決あり/なし) | 2026-09-10 |
| M2-6 | カテゴリ・ルール編集画面(FR-13, NFR-02, 設計原則1)。`/rules`(置きページを実画面化)。`src/features/categories/store.ts`(新規)— `listCategories()`(統廃合済みも含めて全件)、`createCategory()`(code は画面に出さない不変の識別子(ADR-016)のため本人には入力させず自動発行)、`updateCategory()`(改名・月次予算・ホーム表示枠。kind・code は対象外、is_system の分岐を壊さないための UI 側の抑止)、`mergeCategory()`(統廃合。削除ではなく `merged_into_id` で付け替え、`is_active=false` に落とす。統合先は有効なカテゴリのみ、システムが使うカテゴリ自体は統合できない、のバリデーションを追加)。`src/domain/category.ts`(新規、純粋関数)に `assertCategoryName`/`assertCategoryBudgetYen` と、統廃合を辿るための `resolveCategoryRoot()`/`expandMergedCategoryIds()` を追加。分類ルール側は `src/features/classification/store.ts` に `listClassificationRules()`(無効化済みも含めて全件、カテゴリ名を解決)、`setClassificationRuleActive()`、`deleteClassificationRule()`、`moveClassificationRuleUp/Down()`(隣り合う2件の priority を入れ替え。`classification_rules.priority` には一意制約が無いため、`transfer_rules`(M4-3)の負の一時値を経由する手当ては不要で直接入れ替えで済む)を追加。ルールの新規作成 UI は本タスクの成果物の記述に無いため対象外(一覧・優先度変更・有効無効・削除のみ)。**実装中に対応した設計上の穴**:カテゴリを統合しても `transactions.category_id` は書き換えない方針(docs/schema.sql)のため、ホームの残額タイル(`features/home/summary.ts`)が統合先のカテゴリだけで明細を絞ると旧カテゴリの明細が集計から漏れる。`expandMergedCategoryIds()` で旧カテゴリの id も検索条件へ足し、取得後は `resolveCategoryRoot()` で明細側の `categoryId` を統合先へ揃え直すよう `listMonthTransactions()`/`loadHomeSummary()` を修正。実際の Supabase プロジェクトに対して一時セッションを発行し確認:①検証用カテゴリを作成し改名・月次予算(5,000円)・ホーム表示を編集 → 画面・DB とも反映を確認、②同カテゴリに紐づく明細(−1,234円)を作った状態で「生活費」へ統合 → 統合前はホームの生活費タイルが「0円 / 60,000円」、統合後は「1,234円 / 60,000円」に変化し、旧カテゴリの明細が新カテゴリの集計に含まれることを確認、③検証用の分類ルール2件(実際の FR-21 検知ルールには触れず)で優先度の入れ替え(DB の priority が入れ替わることを確認)・無効化・削除(一覧から消え DB からも削除)を確認。検証用データ(カテゴリ・口座・明細・ルール)はすべて削除済み。テスト12件追加(assertCategoryName/assertCategoryBudgetYen/resolveCategoryRoot/expandMergedCategoryIds) | 2026-09-11 |
| T-5 | `/rules` の置きページを実画面に置き換える | M2-6 で解消。ナビのリンク切れ検出用の暫定ページだった `/rules` が実画面になったことで、対象4画面(`/debts` M1-2、`/payday` M4-3、`/rules` M2-6、`/transactions` 既存)すべて置きページを脱した。`/transactions` の保存先が sessionStorage のままである点(T-7)は別課題として残る | 2026-09-11 |
| M7-3 | 完済検知による高リスク枠解禁(FR-52)。`/investments` へのアクセスごとに全負債の完済を判定する処理を追加。`src/domain/investment.ts` に `isFullyPaidOff()`(純粋関数)を追加 — `status !== 'active'` が全件で成立すれば完済(refinanced・closed も返済負担が残っていない扱い)、ただし負債を一度も登録していない(0件)状態は完済に含めない。`src/features/investments/store.ts`(新規)の `checkAndUnlockHighRisk()` が、未解禁のときだけ負債を読み、完済していれば `app_settings.is_high_risk_unlocked` を立てて `alerts`(`kind='debt_paid_off'`、新規追加した alert 種別)へ記録する(dedup_key は固定文字列。一生に一度きりの事象なので、毎回のアクセスで呼んでも実際に書き込まれるのは最初の1回だけ)。`/investments` はこの戻り値(今回のアクセスで解禁したか)を使って「高リスク枠が解禁されました」の通知を表示する。**実装中に発見した不具合**:解禁直後の同一アクセス内で `getAppSettings()` を呼び直しても、Next.js のリクエストメモ化により `checkAndUnlockHighRisk()` 内で読んだ更新前の応答が再利用され、解禁直後の1回だけ配分表示が古いまま(全額インデックス枠)になっていた。`settings.isHighRiskUnlocked || justUnlocked` で当該レンダー内の実効値を作り解消。実際の Supabase プロジェクトに対し、実在する3件の負債(カードA・カードB・消費者金融C)を一時的に完済状態(残高0・`status='paid_off'`)にして検証:解禁前は「完済前は全額インデックス枠です」の表示、解禁直後のアクセスで通知バナー表示 + `is_high_risk_unlocked=true` + `alerts` に1行 + 配分がインデックス7:高リスク3に即座に切り替わることを確認、2回目のアクセスでは通知バナーが出ず配分表示のみ継続すること・`alerts` が重複記録されないことを確認。検証後、3件の負債の残高・status・paid_off_on と `app_settings.is_high_risk_unlocked` はすべて元の値に復元済み。テスト4件追加(isFullyPaidOff) | 2026-09-11 |
| M5-1 | 配信内容の生成(FR-30, FR-31)。ADR-020(朝配信「最小版」の1トピックは固定の定型文バンクにする。AI 生成・実データ検索は P2-2 で行う)を決めた上で実装。`src/features/briefs/store.ts`(新規)の `generateDailyBrief()` — 冒頭の数字(完済まで残り日数・使える残額)はホーム画面と必ず一致させる必要があるため、計算式を複製せず `features/home/summary.ts` の `loadHomeSummary()` をそのまま呼ぶ。`ux_briefs_user_date`(user_id, brief_on の一意制約)を利用し、同じ日に何度呼んでも重複生成しない(既存の1件をそのまま返す)。`src/domain/briefs.ts`(純粋関数)に `filterBriefTopics()`(FR-31:情報商材・根拠不明な高収入案件・詐欺性・アフィリエイト目的を示す文言パターンを検出し、`brief_excluded_items` へ理由付きで回す)と `pickDailyTopic()`(`daysBetween('1970-01-01', 当日) % 件数` で決定的に1件選ぶ、乱数不使用)を追加。`src/features/briefs/tips.ts`(新規)に「収入増のヒント」の固定バンク5件(ADR-020の方針により、具体的な案件名・金額・期限は書かず、本人の行動を促す一般論に留めた)。`daily_briefs` + `brief_items`(kind='headline'/'income_tip')への保存に加え、`body_md` も組み立てて保存(FR-32 のアーカイブ表示に備える)。実際の Supabase プロジェクトに対し一時セッションを発行して確認(呼び出し口が無いため検証用に一時的な API ルートを追加し、確認後に削除):①生成した `daily_briefs` 行の `remaining_debt_yen`(1,000,000円)が実際の active な負債残高の合計と一致、`spendable_living_yen`/`spendable_sanctuary_yen`(60,000円/40,000円)がホーム画面の表示と一致することを確認、②`brief_items` に headline・income_tip の2件が正しい `sort_order` で保存されることを確認、③固定バンクは事前に目視で安全と確認済みのため `brief_excluded_items` が0件であることを確認(フィルタ自体は機能している。危険な文言を含む候補が除外されることは `tests/domain/briefs.test.ts` で確認)、④同日に2回目を呼んでも新規行が増えず同じ `briefId` が返ることを確認。検証用データは削除済み、検証用に追加した一時ルートも削除済み。テスト9件追加(filterBriefTopics 6件・pickDailyTopic 3件) | 2026-09-11 |
| M5-3 | 配信アーカイブ画面(FR-32)。`/briefs`(一覧、新しい順)と `/briefs/[id]`(詳細)を追加。`src/features/briefs/store.ts` に `listDailyBriefs()`(daily_briefs を brief_on 降順で返す)と `getDailyBrief()`(1件の詳細。`brief_items` を sort_order 順、`brief_excluded_items` も合わせて返す)を追加。詳細画面は `body_md`(将来の配信チャネル向けの整形済みテキスト)ではなく `brief_items` を構造化して表示する方針にした(他画面と見た目が揃う)。除外ログがあれば「載せなかった項目」として理由(FR-31 の `brief_exclusion_reason`)付きで表示。ホーム画面の末尾に「朝配信のアーカイブ →」の導線を追加(`/briefs` はボトムナビには入れず、他の非中核画面と同じくリンク経由)。実際の Supabase プロジェクトに対し一時セッションを発行し、検証用に2件の配信データ(項目・除外ログ付き)を作成して確認:①ホーム画面のリンクから `/briefs` へ遷移できること、②一覧が新しい順(2026年9月5日→2026年9月1日)に並ぶこと、③詳細画面で見出し・収入増のヒント・除外ログ(理由ラベル込み)が正しく表示されること、④存在しない id で `notFound()` の404画面が表示されること(HTTPステータスは `next dev` 特有の挙動で200のまま返るが、本番ビルドでは正しく404になる。表示内容自体は正しいことを確認済み)。検証用データは削除済み。ストアの読み出し関数のみのためドメインテストの追加は無し(純粋関数を含まない) | 2026-09-11 |
| M7-2 | 投資記録画面(FR-51)。`/investments` に拠出(フロー)と残高(ストック)の手入力・一覧を追加(証券口座連携は当面対象外)。`src/features/investments/store.ts` に `listInvestmentContributions()`/`createInvestmentContribution()`(投資額日順の一覧・新規記録)と `listInvestmentSnapshots()`/`upsertInvestmentSnapshot()` を追加。`upsertInvestmentSnapshot()` は「同じ日・同じ商品なら上書き」(DoD)を満たす必要があるが、実テーブルの一意制約(`ux_snapshots_user_account_product_date`)が `coalesce()` を挟んだ式インデックスのため PostgREST の `upsert(onConflict:)` がそのまま使えないと判明。既存行の有無を先に確認してから insert/update を分ける実装に変更した(単一ユーザーのアプリのため TOCTOU の実害は無い)。`src/domain/investment.ts` に `assertContributionAmountYen`/`assertSnapshotValueYen`/`assertSnapshotCostBasisYen`/`assertProductName` を追加。account_id は扱わず常に null(証券口座連携を持たないため)。実際の Supabase プロジェクトに対し一時セッションを発行し、①拠出30,000円(高リスク枠指定)を記録 → 一覧に反映されDB行と一致することを確認、②残高10万円(取得額9万円)を記録 → 一覧に反映、③同じ日・同じ商品名で残高12万円を再記録 → 行が増えず(id 同一)値だけ12万円に上書きされることをDBクエリとリロード後の画面表示の両方で確認。検証用データは削除済み。テスト9件追加(assertContributionAmountYen/assertSnapshotValueYen/assertSnapshotCostBasisYen/assertProductName)。**S7(投資、FR-50〜52)は M7-1・M7-2・M7-3 ですべて完了** | 2026-09-11 |
| T-7 / M6-2 | 明細(transactions)の Supabase 実装と、取り込み時の実口座選択。`src/features/transactions/store.ts` を全面書き換え — 以前の `TransactionStore` インターフェース・`SessionTransactionStore` クラス(sessionStorage 実装。タブを閉じると消える、サーバー側ジョブから保存できない、NFR-06 違反)を廃止し、`docs/glossary.md` のレイヤー命名(list/create/update)に従う plain 関数(`listTransactions`/`listImportBatches`/`importTransactions`/`updateTransaction`)へ置き換えた。`importTransactions()` は M3-2/M7-2 と同じ「DB の一意制約を取り込み済み判定に使う」パターンで、`transactions` の実索引(`ux_transactions_fingerprint`、`user_id,fingerprint` の素の複合索引)に対する `upsert(..., ignoreDuplicates: true)` が使え、返った行数から重複件数を逆算する。Server Action(`app/(app)/transactions/actions.ts` の `saveImportBatchAction`/`updateTransactionAction`)を新設し、`/transactions`・`/transactions/review` を Server Component 化、`review-queue.tsx`(M2-5)を新しい Server Action 経由に差し替え。M6-2(`accountId` の `'pending'`/`'email'`/`'gmail'` 固定を解消)も同時実装 — `/api/accounts` + `src/features/transactions/accounts-client.ts` で口座選択肢を取得し、CSV取り込み・メール貼り付けの両画面に「口座を選ぶ」区画を追加、`mail-sync.ts` にも実際の `accountId` を渡すよう変更。**実装中に発見・解消したビルド境界のバグ**:`import-pipeline.ts`(プレビュー計算のためクライアント側でも読み込まれる)が `fingerprintOf()`/型を `store.ts` から直接 import していたため、`store.ts` の `createClient`(`@/lib/supabase/server` 経由で `next/headers` に依存)がクライアントバンドルへ引き込まれ、`next build` が `You're importing a module that depends on "next/headers"` で失敗していた。クライアント安全な型と `fingerprintOf()` だけを持つ `src/features/transactions/types.ts` を新設して分離し、`store.ts` はそこから re-export、`import-pipeline.ts` はそちらから import するよう修正して解消。実際の Supabase プロジェクトに対し実セッションで検証:①検証用口座を作成し CSV 取り込み画面から1件取り込み → `transactions`/`import_batches` に正しい `account_id`/`source='csv'`/`review_status='pending'` で保存されることを確認、②同じ CSV を再取り込み → 0件取り込み・1件重複(`ux_transactions_fingerprint` による重複排除が本番相当の経路で機能)を確認、③確認待ちキューでカテゴリを確定 → `category_id`/`classified_by='manual'`/`review_status='corrected'`/`reviewed_at` が正しく更新され、画面からも消えることを確認。検証用データ(口座・明細・取り込みバッチ・学習ルール)はすべて削除済み | 2026-09-12 |
| M2-7c | `/api/cron/import-gmail` と GitHub Actions ワークフロー(毎朝取得)。`app/api/cron/import-gmail/route.ts`(新規)— `keepalive` と同じく `createAdminClient()` + Bearer 認証(`CRON_SECRET`)。取り込み先の口座は `app_settings` に新しい列を足さず、環境変数 `GMAIL_IMPORT_ACCOUNT_ID` で指定する方式にした。**理由**:このジョブに本人のセッション(cookie)は無く、口座選択の UI もこのタスクの範囲外な上、`app_settings.gmail_enabled` 等を編集する画面自体がまだ無い(ADR-018 は環境変数名だけを持つ設計で、口座という概念自体 M6-2 より後に生まれた)。`GMAIL_ADDRESS`/`GMAIL_APP_PASSWORD` と同じ Vercel 環境変数 + GitHub Secrets の経路に乗せれば、スキーマ変更なしで完結する(`src/lib/env.ts` に `getGmailImportAccountId()` を追加、uuid 形式を検証)。処理の流れ:`app_settings.gmail_enabled` が false なら即スキップ、口座が本人のものか確認、`transactions`(source='gmail')の既存 `source_ref` を集めて `knownMessageIds` にし(再実行での二重読み込み防止)、`listActiveClassificationRulesForUser()`(新規、`listActiveClassificationRules()` の管理クライアント版。cron は RLS に頼れないため user_id を明示)で学習ルールも取得、`ImapMailSource` + `syncFromMailbox()` を呼んで `transactions`/`import_batches` へ直接 upsert、`app_settings.gmail_last_synced_on` を更新、`job_runs`(job_name='import_gmail')に記録。**あわせて解消した設計の欠け**:`transactions.source_ref`(Gmail の message-id 用に以前から列と一意索引(`ux_transactions_source_ref`)は存在した)が `StoredTransaction`/`importTransactions()` のどこにも配線されていなかった。`StoredTransaction` に `sourceRef` を追加し、CSV/貼り付けでは null、メール取り込みでは `mail-sync.ts` の新規 `buildSourceRef(messageId, indexInMessage)`(1件目は messageId そのまま、1通から複数件抽出された場合の2件目以降は連番を足して一意性を保つ)が埋めるようにした。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(436件全通過、`buildSourceRef` と source_ref の伝播にテスト3件追加)、`npx next build` 成功。実機検証は2段構え——①cron route の認証・分岐(未認証401、Gmail未設定時のスキップ、`gmail_enabled=false` 時のスキップ、`GMAIL_IMPORT_ACCOUNT_ID` が本人の口座に無い場合のエラー)は実際に立てた dev サーバーへ本物の `CRON_SECRET` で HTTP リクエストして確認。②実 IMAP 接続(`imap.gmail.com:993`)はこの開発サンドボックスのネットワーク制約で到達不能(`CONNECT_TIMEOUT`。本番の Vercel 環境には影響しないサンドボックス固有の制約、ADR-011 改定時の Supabase 直叩きと同種の既知の制約)だったため、`syncFromMailbox()` 以降の実 DB 書き込み(`transactions.source_ref` の保存、fingerprint による重複排除、`job_runs` 記録)は `StaticMailSource` で同じ経路を実際の Supabase プロジェクトに対して検証した(検証用口座・一時テストファイルを作成し確認後にすべて削除、`app_settings` は元の既定値に復元済み)。**本人への確認待ちに追加**:`/accounts` で実口座を作った後、その `accounts.id` を Vercel の環境変数と GitHub Secrets に `GMAIL_IMPORT_ACCOUNT_ID` として設定し、`app_settings.gmail_enabled` を true にする必要がある(後者を編集する画面がまだ無いため、当面は SQL や Supabase ダッシュボードでの操作が必要。UI 化は別タスクとして Backlog に起こす価値があるが、本タスクの範囲外) | 2026-09-12 |
| M6-3 | 給料日〜給料日の期間ビュー(FR-17)。保存は暦月のまま(ADR-015)、表示だけを給料日基準に変換する。`src/lib/date.ts` に `paydayCycleFor(today, payday)` を追加(純粋関数)— today が今月の給料日以降なら「今月の給料日〜来月の給料日前日」、まだなら「先月の給料日〜今月の給料日前日」を返す。あわせて `features/transfer-runs/store.ts` に private で重複していた `thisMonthsPayday()` を廃止し、共通の `paydayInMonthOf()` を切り出して両方から参照する形に統一(重複解消)。`src/domain/payday-period.ts`(新規、純粋関数)— `domain/budget.ts` の `isCountable` と同じ除外基準(口座間振替・`review_status='ignored'`・収入)で口座別/カテゴリ別/合計の使用金額を集計。`src/features/transactions/period-summary.ts`(新規)の `loadPaydayPeriodSummary()` が `app_settings.payday` から期間を求め、その期間の明細だけを読んで集計・口座名/カテゴリ名を解決する。`/transactions` の一覧上部に期間カード(合計・口座別・カテゴリ別)を追加。使用額が無い口座も「0円」として全件表示(まだ使っていないことも情報)、カテゴリは使用額があるものだけを表示。実際の Supabase プロジェクトに対し、給料日サイクル内・サイクル外・振替(is_transfer)の3明細を作成して `/transactions` を描画 → 期間カードがサイクル内の1件(3,000円)だけを合計・口座別・カテゴリ別に正しく集計し、サイクル外(9,999円)と振替(5,000円)を除外することを確認。あわせて `thisMonthsPayday` → `paydayInMonthOf` への置き換えが `/payday` の給料日チェックリストを壊していないことも確認。検証用データは削除済み。テスト14件追加(`paydayInMonthOf`/`paydayCycleFor` 7件、`sumByAccount`/`sumByCategory`/`totalSpending` 7件) | 2026-09-12 |
| M6-4 | 請求金額メールとの突合(FR-18)。カード会社から届く「ご利用金額のお知らせ」(締め日単位の合計)を貼り付け、同じ締め期間に取り込んだ明細の合計と突き合わせ、差額があれば `alerts` に記録する。`/transactions/reconcile`(新規)。`src/features/import/statement-email.ts`(新規、純粋関数)の `parseStatementEmail()` — `email.ts`(1件ごとの利用通知)とは別の語彙(「ご請求金額」「お支払金額」等)で合計金額と対象期間を読み取る。期間は年月日が揃った記載のみ対象(「9月10日締め」のような年省略の書式は対象外、口座の締め日で補う)。`src/lib/date.ts` に `billingCycleStartFor(endOn, closingDay)`/`mostRecentClosingOnOrBefore(today, closingDay)` を追加。**ついでの重複解消**:M6-3 で作った `paydayInMonthOf()` が「毎月の n 日目」という給料日専用でない計算だったため、汎用の `nthDayOfMonth()` へ改名し、締め日の計算(billingCycleStartFor 等)もこれを共用する形にした(呼び出し側の `features/transfer-runs/store.ts` も追従)。`src/domain/statement-reconciliation.ts`(新規、純粋関数)— `billingPeriodFor()`(メールの期間 → 無ければ口座の締め日から自動計算 → どちらも無ければ null)と `reconcileTotals()`(差額の算出)。`src/features/transactions/reconciliation-store.ts`(新規)の `reconcileAccountStatement()` が対象期間の明細を読んで `domain/payday-period.ts` の `totalSpending()`(M6-3 と同じ除外基準を再利用)で合計し、差額があれば `features/alerts/store.ts` の `recordAlerts()`(M3-2)をそのまま使って `alerts`(kind は新しいDB列挙値を増やさず既存の `import_needed` を再利用。`AlertKind` の TS 型だけ拡張)に記録する。dedup_key は `import_gap:{accountId}:{periodEndOn}` で同じ期間の再突合が重複記録を作らない。`/api/accounts` のレスポンスに `closingDay` を追加(既存の CSV/貼り付け画面は無視するだけで影響なし)。実際の Supabase プロジェクトに対し、口座(締め日10日)と明細(3,000円、期間内)を作成して検証:①期間付きメール(合計45,678円)を貼り付け→突合→差額42,678円・「取り込み漏れの疑い」表示・`alerts` に正しい title/body/dedup_key で1件記録されることを確認、②同じ突合を再実行→`alerts` が重複記録されない(dedup)ことを確認、③金額を実際の合計(3,000円)に直して再突合→「一致しています」表示を確認、④期間の記載が無いメールを貼り付け→口座の締め日から自動計算した期間(①と同じ8/11〜9/10)で正しく突合できることを確認。検証用データ(口座・明細・alerts)はすべて削除済み。テスト14件追加(`billingCycleStartFor`/`mostRecentClosingOnOrBefore` 5件、`parseStatementEmail` 3件、`billingPeriodFor`/`reconcileTotals` 6件)。**S6(口座管理と請求突合、FR-16〜18)は M6-1・M6-2・M6-3・M6-4 ですべて完了** | 2026-09-12 |
| M3-1 / M3-3 / T-21 | Discord 通知基盤・毎時アラートジョブ・FR-22の実データ接続(FR-20〜24)。`src/lib/discord.ts`(新規)— Discord Webhook への POST のみを担う汎用クライアント(`docs/architecture.md` の層分離に従い、severity→色のようなアラート固有の業務判断は持たせない。実装中に一度 `AlertSeverity` を import しかけて `lib/ → domain/` の初めての依存になりかけたため、`features/alerts/notify.ts` 側へ寄せ直した)。`src/domain/alerts.ts` に `detectRiskyTransaction()`(FR-21、リボ・キャッシング・分割払いを検知。`CandidateAlert` に `transactionId` を追加)を追加。`src/features/alerts/store.ts` に cron 向けの `*AsAdmin(client, userId, ...)` 群を追加(T-7/M6-4 で確立した管理クライアントパターン)— `detectAndRecordPaymentDueAlertsAsAdmin()`(既存ロジックの移植)、`detectAndRecordInactivityAlertAsAdmin()`(T-21。`import_batches.created_at` の最新行を「最終取り込み日」とする)、`detectAndRecordRiskyTransactionAlertsAsAdmin()`(FR-21。DB の `.in('payment_method', [...])` は `isRiskyPaymentMethod()` の正を崩さないための重複フィルタである旨コメントで明記)。`src/features/alerts/notify.ts`(新規)の `sendPendingAlerts()` が `status='pending'` の alerts を古い順に処理し、`sent`/`failed` を個別に記録(dedup は既存の DB 一意制約のまま)。`app/api/cron/detect-alerts/route.ts` + `.github/workflows/detect-alerts.yml`(毎時)— `keepalive`/`import-gmail` と同じ Bearer 認証、`DISCORD_WEBHOOK_URL` 未設定(B-3 待ち)の間は検知だけ行い送信をスキップする設計。実際の Supabase プロジェクトに対し二段構えで検証:①cron route の認証・多重検知(未認証401、実在の負債・実在のリボ払い明細1件・未取込状態から3種の alert が正しく記録され `transaction_id` FK も正しいこと、再実行で重複しないこと)を実際に dev サーバーへ HTTP リクエストして確認、②実 Discord Webhook は本人が未発行(B-3、後で用意するとのこと)のため、ローカル `node:http` サーバーを Webhook 代わりに立てて `sendPendingAlerts()` の成功パス(`status='sent'`・`sent_at` 設定・Embed の色/形が正しい)と失敗パス(`status='failed'`・`error_message` に「ステータス 500」を含む)を実際の Supabase `alerts` テーブルに対して検証(検証用の一時テストファイルは確認後に削除)。検証用データ(テスト口座・テスト明細・テスト alerts・ストリーク検証で入れた合成 `app_checkins` 3行)はすべて削除済み。テスト8件追加(`detectRiskyTransaction` 4件、`buildEmbedForAlert` 4件)。**S3(リボ・キャッシング検知と通知、FR-21, FR-24)は M3-1・M3-2・M3-3 ですべて完了**(ただし FR-20 の配線漏れを T-23 として、M3-3 DoD の `job_failure` 未実装を T-24 として Backlog へ記録) | 2026-09-12 |
| FR-62 | 連続確認日数(ストリーク)の表示。本人の希望で MVP 完了前に前倒し実装(旧 P2-3、`docs/mvp-plan.md` は本来フェーズ2扱い)。スキーマ(`app_checkins`/`v_checkin_streak`)は M0-2 の時点から存在していたが、アプリ側から一度も配線されていなかった。`src/domain/streak.ts`(新規、純粋関数)の `streakBadgeFor()` が `none`/`active`/`restart` の3状態を返す(3日未満は非表示、3日以上の実績が途切れた場合だけ「今日から再開」を出す。設計原則3「途切れても責めず再開だけを提示する」)。`src/features/checkins/store.ts`(新規)の `recordCheckin()`(ホームを開くたびに当日分を upsert。失敗しても画面は止めない)と `getCheckinStreak()`(ビューを読むだけ)。`app/(app)/page.tsx` に `StreakBadge` を追加、完済カウントダウンの見出し行の右側に表示。実際の Supabase プロジェクトに対し、`app_checkins` を直接操作して3状態(none: 2日間のみ→非表示、active: 3日連続→「🔥3日連続」、restart: 3日連続の実績はあるが今日を含め途切れている→「今日から再開」)をすべて Playwright のスクリーンショットで確認。検証用の合成データ(2026-09-01〜03)は削除し、本人の実際のチェックイン(2026-09-12、このセッション中にホームを開いたことで記録された本物のデータ)は残した。ドメインテスト4件追加 | 2026-09-12 |
| M5-2 | 朝配信ジョブ(FR-30、07:00 JST)。`app/api/cron/morning-brief/route.ts` + `.github/workflows/morning-brief.yml`(`0 22 * * *` UTC = 07:00 JST)— 他の cron と同じ Bearer 認証。`src/features/briefs/notify.ts`(新規)の `deliverDailyBriefAsAdmin()` が `daily_briefs.status`(pending/generated/delivered/failed)を送信済みの正として扱い、`delivered` なら何もしない(Actions の遅延で同じ日に複数回走っても二重送信しない、DoD)。**実装中に発見した設計の穴**:`generateDailyBrief()`(M5-1)が内部で使う `loadHomeSummary()`・`listDebts()`・`getAppSettings()` はいずれも「RLS が本人の行に絞る」前提でセッション付きクライアントを自前で生成しており、cron(セッション無し)から呼ぶと該当行が一切見えず `.single()` が「0行」で例外になっていた。T-7/M6-4 で確立した管理クライアントパターンに倣い、3つとも `*AsAdmin(client, userId, ...)` 版を追加し(`src/features/debts/store.ts` の `listDebtsAsAdmin()`、`src/features/settings/store.ts` の `getAppSettingsAsAdmin()`、`src/features/home/summary.ts` の `loadHomeSummaryAsAdmin()` — 内部の4つの private ヘルパー全てに `client`/`userId` を通し、全クエリに `eq('user_id', userId)` を追加)、既存のセッション版はそれぞれ薄いラッパーへ変更(振る舞いは無変更)。`src/features/briefs/store.ts` の `generateDailyBrief()` も同じ形で `generateDailyBriefAsAdmin()` の薄いラッパーに変更。実際の Supabase プロジェクトに対し検証:①cron route(未認証401、生成+送信スキップ、同日2回目は `created:false` で新規生成しないこと)を実際に dev サーバーへ HTTP リクエストして確認、②実 Discord Webhook 未発行(B-3)のため、`M3-1` と同じ手法でローカル `node:http` サーバーを Webhook 代わりに立て、`deliverDailyBriefAsAdmin()` の成功(`status='delivered'`)・失敗(`status='failed'`+`error_message`)・二重送信防止(`already_delivered`)の3パスを実際の Supabase に対して検証。**検証中に発見・修正したバグ**:失敗後に再送信して成功しても `error_message` が消えずに残ってしまう欠陥を発見し、成功時の更新に `error_message: null` を追加して解消。③リファクタの影響確認として `/`・`/debts`・`/briefs` を実セッションで Playwright 描画し、既存表示(残債・カードA・配信一覧)が壊れていないことを確認。検証用データ(テスト用に生成した当日分の `daily_briefs` 行)は削除済み。ストアの配線のみのためドメインテストの追加は無し(M5-3 と同型)。**`docs/mvp-plan.md` の M0〜M7(全28タスク)がこれで完了** | 2026-09-12 |
| T-23 / T-24 | FR-20(浪費70%)の alerts 配線と、cron 失敗時の `job_failure` 記録。`src/domain/alerts.ts` に `detectWastefulBudget()`(FR-20。`domain/budget.ts` の `hasReachedAlertThreshold()` が正、ここは月単位の dedup_key と非難しない文言だけを組み立てる)と `buildJobFailureAlert()`(NFR-06。日単位の dedup_key で同日の連続失敗を1件にまとめる)を追加。`AlertKind` に `waste_budget_70`/`job_failure` を追加(DB の `alert_kind` enum には D-3 の時点から両方とも存在しており、マイグレーション不要だった)。`src/features/alerts/store.ts` に `detectAndRecordWastefulBudgetAlertsAsAdmin()`(`categories.kind='waste'` のカテゴリだけを対象に当月の予算消化率を計算。`home/summary.ts` の `listHomeCategories`/`listMonthTransactions` と同型のクエリだが対象条件が違うため独立実装)と `recordJobFailureAlertAsAdmin()` を追加し、`detect-alerts`・`morning-brief` 両 cron ルートの catch 節から呼ぶ(記録自体の失敗は握り潰し、元のエラー応答は変えない)。実際の Supabase プロジェクトに対し、検証用の `waste` カテゴリ(予算10,000円)と明細(8,000円、80%消化)を作成して cron ルートを叩き、`waste_budget_70` アラートが正しい残額(2,000円)付きで記録されることを確認。テスト8件追加(`detectWastefulBudget` 4件、`buildJobFailureAlert` 2件、既存 alerts テストと合わせて20件)。 | 2026-09-12 |
| T-22 | `/settings/gmail` に Gmail 自動取得の設定編集フォームを追加(有効フラグ・差出人ドメイン絞り込み・1回の取得件数上限)。`src/domain/gmail-settings.ts`(`assertGmailFromAddresses`/`assertGmailFetchLimit`)、`src/features/settings/gmail-store.ts`(`getGmailSettings`/`updateGmailSettings`)、Server Action(`app/(app)/settings/gmail/actions.ts`)。資格情報(`GMAIL_ADDRESS`/`GMAIL_APP_PASSWORD`)と取り込み先口座(`GMAIL_IMPORT_ACCOUNT_ID`)は従来どおり環境変数のみで扱い、この画面からは触れない(NFR-04, ADR-018)。実際の Supabase プロジェクトに対し、実ブラウザ操作で有効化・差出人2件・件数上限150への変更→保存→DBに正しく反映されることを確認し、検証後は既定値(無効・差出人なし・200件)に復元済み。テスト6件追加。 | 2026-09-12 |
| T-6 / T-9 | `ImportAdapter`(TS)と `import_adapters`(DB)の対応を型で保証し、列マッピングを保存・再利用できるようにした。`import_adapters` テーブルは M0-2 の時点からスキーマに存在したが、アプリのどこからも読み書きされていなかった(コメントで参照されるだけ)。`src/features/import/adapter-store.ts`(新規)の `fromRow()`/`saveImportAdapterForAccount()` が `Database['public']['Tables']['import_adapters']` の Row/Insert 型を直接使うため、DB 側の列名・型が変わればここがコンパイルエラーになる(T-6)。1口座につき1件の上書き保存(一意制約は無いため、既存行の有無を先に確認してから insert/update を分岐)。`/api/import-adapters`(新規)+ `src/features/transactions/import-adapter-client.ts` 経由で、CSV 取り込み画面(`app/(app)/transactions/import/page.tsx`)がファイル読み込み時に「その口座で前回保存した列対応」を先に試し、ヘッダに存在しなければ `guessMapping()` へフォールバックする。取り込み成功時に使った列対応を保存(失敗しても取り込み自体は完了しているため待たない)。実際の Supabase プロジェクトに対し、検証用口座を作成して POST→GET の往復(保存した値がそのまま返る)、2回目の POST が新規行を増やさず上書きになることを確認。検証用データは削除済み。 | 2026-09-12 |
| T-11 | AI が救済したメールの書式をラベル辞書へ還元する仕組み(ADR-019)。ラベル辞書(`email.ts`)で読めず AI に回ったメールの本文を `rescued_emails`(新規テーブル)に残し、`/settings/rescued-emails`(新規)で見返せるようにした。新規マイグレーション2本(`20260908001200_rescued_emails.sql`、`20260908001300_rescued_emails_rls.sql`)を `docs/schema.sql` と両方に追加し、`./scripts/verify-schema.sh`/`./scripts/verify-migrations.sh`(911項目一致)でローカル検証済み。`src/features/import/rescue-store.ts`(`recordRescuedEmailAsAdmin`/`listRescuedEmails`/`deleteRescuedEmail`)。`mail-sync.ts`(Gmail cron 経路)は DB に触れない設計(ADR-019 当初からの方針)を保つため、新しく `onRescued` コールバックを追加してもらう形にし、実際の保存は呼び出し側(`app/api/cron/import-gmail/route.ts`)が担う。貼り付け画面側(`app/api/import/email/route.ts`)は元々 Supabase に触れていなかったため、記録用に `createClient()`+`auth.getUser()` を新規に追加。**両経路とも記録の失敗を `.catch()` で握り潰し、取り込み本体には一切影響させない設計**にした——これは単なる防御的プログラミングではなく、このセッションには Supabase Management API の資格情報が無く新規マイグレーションを本番プロジェクトへ適用できなかったため(B-3 の Discord Webhook と同種の制約)、本番ではこの2マイグレーションが適用されるまで `rescued_emails` テーブルが実在せず、素朴に実装すると本番相当の環境でこの機能が触れられるたびに例外を投げてしまうところだった。実際にこの状態(本番 Supabase に対してテーブル未作成のまま insert)を再現し、握り潰しが機能して呼び出し元の処理が継続すること、`/settings/rescued-emails` もクラッシュせず「まだ利用できません」と表示することを確認(修正前は Server Component が例外を投げて画面全体が落ちていたバグを発見・修正)。本番への適用自体は T-25(新規 Backlog、B-4 待ち)として記録。テスト2件追加(`onRescued` コールバックが AI呼び出しのたびに本文を渡すこと、失敗時も extractedCount:0 で渡ること)。 | 2026-09-12 |
| P3-1 | 副業トラッカー(FR-40, FR-42。本人の希望で MVP 完了前に前倒し)。`side_projects`/`side_work_logs`/`side_incomes` は D-3 の時点でスキーマ用意済み(未配線)。`/side-hustle`(新規)— プロジェクトの登録、作業時間の記録(分単位)、入金の記録。`src/domain/side-hustle.ts` の `computeHourlyRateYen()`(FR-40:合計入金÷合計時間)と `computeIncomeAllocation()`(FR-42:`app_settings.side_income_repayment_ratio`、既定7:3で返済/投資へ自動振り分け。端数は返済側に寄せ、常に合計が入金額と一致するようにして DB 制約 `ck_side_incomes_alloc_sum` を満たす)。実際の資金移動はこのアプリの対象外(他の資金移動と同じく手動。振り分け額を「指示」として画面に表示するところまで)。`getAppSettings()`/`getAppSettingsAsAdmin()` に `sideIncomeRepaymentRatio` を追加。実際の Supabase プロジェクトに対し、検証用プロジェクトを作成し①120分の作業ログ記録→②20,000円の入金記録(返済14,000円/投資6,000円に自動振り分け、画面のプレビューと保存後表示が実際の計算と一致)→③時給換算(10,000円/時、合計2時間から算出)が正しく表示されることを確認。検証用データは削除済み。テスト14件追加。 | 2026-09-12 |
| P3-2 | 転職準備チェックリスト(FR-41。本人の希望で MVP 完了前に前倒し)。`job_change_milestones` は D-3 の時点でスキーマ用意済み(未配線)。`/job-change`(新規)— 市場調査/職務経歴書/応募/面接/内定の5段階(`milestone_phase`)ごとに項目を一覧し、状態(未着手/進行中/完了/見送り)を1タップで変更、削除できる。`src/features/job-change/store.ts` の `setMilestoneStatus()` が DB 制約(`ck_milestones_done`:status='done' と done_on の有無が一致すること)を満たすため、done へ移るときだけ `done_on` を立てそれ以外は落とす。手動並び替えは FR-41 の要件に無いため対象外(sort_order は既定値のまま作成順)。実際の Supabase プロジェクトに対し、検証用項目を作成→状態を「完了」に変更→`done_on` が正しく設定されることを確認。検証用データは削除済み。テスト2件追加。 | 2026-09-12 |
| T-12 | `src/domain/payoff.ts` の `simulateTotalPayoff()`(110行)を分割。充当順の並び替え(`orderDebtsForStrategy()`)、利息の計上(`accrueInterest()`)、予算の充当(`applyBudget()`、最低返済と余剰充当の両方で共有。`capFor` で上限だけが違う)の3つの純粋なヘルパー関数に分離し、本体は「毎月これらを順に呼ぶ」ループだけになった。SQL 版との golden fixture 一致検証(M1-1、`tests/domain/payoff.test.ts` 33件)が計算結果の一言一句を保証しており、分割前後で全件通過(数値の変化なし)を確認済み。 | 2026-09-12 |
| P5-2 | 学習ルールの誤爆検知と自動無効化(FR-12, FR-13)。**着手前に実装を再確認し、当初の Backlog 案(「N回連続で修正なしなら自動確定」)の前提が誤りだったと判明**:`applyRules()` は既にルール一致時に確認待ちを経ず即時確定しており、追加の「自動昇格」は不要だった。代わりに、これまで一度も使われていなかった `transactions.matched_rule_id`(スキーマにはあったが取り込み経路が書いていなかった)を配線し、「後から本人が修正した割合が高いルールを検知して自動的に無効化する」方向へ作り直した。`StoredTransaction`/`buildPreviewRow()`/`importTransactions()` に `matchedRuleId` を通し、`domain/alerts.ts` に `isMisfiringRule()`(既定:ヒット3件以上かつ修正率50%以上)と `buildRuleMisfireAlert()` を追加(`alert_kind` は既存の `other` を再利用、マイグレーション不要)。`src/features/classification/store.ts` の `detectAndDeactivateMisfiringRulesAsAdmin()` が誤爆ルールを検知し `is_active=false` にした上で `alerts` へ記録、`detect-alerts` cron に配線。実際の Supabase プロジェクトに対し、ヒット4件中3件が修正されたルールを作成→cron 実行→ルールが無効化され正しい alert(title/body/dedup_key)が1件記録されることを確認、再実行で重複記録されない(冪等)ことも確認。検証用データは削除済み。テスト6件追加。 | 2026-09-12 |
| P5-3 | 予算消化ペースの先回り通知(FR-20)。70%到達の一点判定だけでは、月の前半で既にペースが速いカテゴリに気づくのが遅れる。`domain/alerts.ts` に `isAheadOfPace()`(経過日数に対する消化率が既定1.5倍以上速ければ発火。70%到達済みなら発火しない=同じ状況を二重通知しない。月初数日は分母が小さく比率が跳ねやすいため最低経過日数を設ける)と `buildBudgetPaceAlert()`(`alert_kind` は `other` を再利用)を追加。`features/alerts/store.ts` の `detectAndRecordWastefulBudgetAlertsAsAdmin()`(T-23)にペース判定を組み込み、既存の categories/budgets/transactions クエリを再利用(クエリの重複無し)。**検証中に発見した罠**:最初の検証はJST日付がセッション中に9/12→9/13へ日を跨いだ影響で、狙ったペース比(1.56倍)が実行時には1.44倍まで下がり閾値未満になって不発——バグではなくテストデータの余裕不足だったと判明し、消化額を調整して再検証。実際の Supabase プロジェクトに対し、経過日数13日/30日・消化率67.5%(閾値70%未満)のカテゴリで cron を実行し、正しい `budget_pace` アラート(残額・dedup_key とも正しい)が1件記録されること、再実行で重複しない(冪等)ことを確認。検証用データは削除済み。テスト8件追加。 | 2026-09-12 |
| P6-1 | 月次振り返り配信(今月の返済実績・副業収入・浪費枠消化を月末に Discord へ)。新規スキーマ不要——既存の `debt_payments`/`side_incomes` と、P5-3 で共通化した浪費カテゴリの当月消化状況(`loadWasteCategoryStatuses()`)から集計するだけ。`domain/alerts.ts` に `isLastDayOfMonth()`(翌日の月が変わるかで判定。31日固定にすると30日までの月を誤判定するため)と `buildMonthlyRecapAlert()`(`alert_kind` は `other` を再利用)を追加。`features/alerts/store.ts` の `detectAndRecordMonthlyRecapAlertAsAdmin()` が月末にだけ候補を積み、既存の `detect-alerts`(毎時)cron に相乗りさせた(新しい GitHub Actions ワークフローを増やさずに済む。dedup_key が月単位のため月末に何度走っても1件のまま)。実際の Supabase プロジェクトに対し、返済実績(20,000円)を作成した上で基準日を月末(9/30)に固定して関数を直接呼び出し検証:①正しい本文(返済額・副業収入0円・浪費カテゴリの消化率)で1件記録、②同じ基準日での再実行は0件(冪等)、③月末以外の基準日では何もしない、の3点を確認。検証用データは削除済み。テスト7件追加。 | 2026-09-12 |
| P6-2 | `/reports`(新規):カテゴリ別支出推移グラフ(直近6ヶ月、FR-14)。`domain/spending.ts`(新規)の `summarizeMonthlySpendByCategory()` が全月×全カテゴリの組を0円で埋めて返す(`domain/budget.ts` の `summarizeBudgets()` と同じ考え方。集計対象の定義=`isCountable()` はそちらから export して再利用し、「支出」の定義を2箇所で別々に決めない)。`features/reports/store.ts` の `loadCategorySpendingTrend()` が直近6ヶ月分の `transactions`/`categories` を読んで集計し、期間中に一度も支出が無いカテゴリは除外する(`period-summary.ts` の既存の考え方に合わせた)。**画面側でカテゴリに色を割り当てない判断**:`app/globals.css` 冒頭のコメントが「カテゴリごとに色を割り当てる案は捨てた(本人が増減できるため固定の色順が成立せず、暗いサーフェスでは隣接色相が識別できない組み合わせが出る)」と明記しているため、本グラフもそれに従い、カテゴリ数分の small multiples(1カテゴリ=1枠)に分け、色は「支出」の役割一色(`var(--over)`)だけを使い、識別は名前(ラベル)で行う設計にした(dataviz skill の「9個目以降は Other/small multiples へ折り込む」を最初から全カテゴリに適用した形)。各枠はミニバーチャート(6ヶ月分、`title` 属性でホバー時に月・金額を表示、最大月だけ直接ラベル)+ 下部にアクセシビリティ用の表(dataviz: table view は必須)。ホームからのリンクを追加(FR-61 の「ホームは3つの数字だけ」を守り、詳細画面は別ページに閉じ込める)。**検証方法についての制約**:実際の Supabase プロジェクトのデータでクエリ・集計ロジック(振替/ignored 除外、収入除外、複数カテゴリ×複数月の合算)は直接検証したが、`/reports` 画面自体を実ブラウザで描画する検証は、このサンドボックスのエージェントプロキシがブラウザ→Supabase Auth 間の接続を確立できない(`ws_closed_mid_exchange`、プロキシ側のトンネルが handshake 途中で切れる既知の制約)ため断念し、`next build` の型検査・静的生成が通ることで代替した。検証用データは削除済み。テスト7件追加。 | 2026-09-12 |
| P6-3 | 資産推移グラフ(残債総額 + 投資評価額の時系列、FR-02, FR-51)。`debts.current_balance_yen` は現在値のみで履歴を持たないため、新規テーブル `net_worth_snapshots`(マイグレーション2本:`20260912000100_net_worth_snapshots.sql` / `20260912000200_net_worth_snapshots_rls.sql`、`docs/schema.sql` にも反映)を追加し、月末に1行ずつ記録し始める設計にした。投資評価額側は既に `investment_snapshots`(商品ごとに本人が好きな時に記録)があるため新規スキーマは不要——`domain/investment.ts` に `totalInvestmentValueAsOf()`(商品ごとに指定日以前で最新の記録を1件選び合算。記録の無い商品は0として無視)を追加して合算した。`features/net-worth/store.ts` の `recordNetWorthSnapshotAsAdmin()`(P6-1 と同じ `isLastDayOfMonth()` で月末だけ判定、`(user_id, as_of)` の一意制約に upsert)を既存の `detect-alerts` cron に相乗りさせ、`loadNetWorthTrend()` を `/reports` に追加(2本目のグラフ:残債総額=`var(--over)`「支出・負債」の役割色、投資評価額=`var(--income)`「収入・資産」の役割色をそのまま再利用。globals.css が明記する通り緑×赤は通常視ΔEが境界帯のため、凡例・直接ラベルに必ず数値と文字を添える設計)。**このセッションの制約により新規マイグレーションを本番へ適用できず**(T-25/B-4 と同種、Supabase Management API 資格情報が無い)、`docs/schema.sql` との整合はローカル PostgreSQL で `verify-schema.sh`/`verify-migrations.sh` 双方検証済み(926項目一致)。未適用の間の失敗経路(cron 側の記録・画面側の読み出し双方)を実際の本番 Supabase(テーブル未作成の状態)に対して検証し、両方とも握り潰されて他機能に影響しないことを確認(T-26/B-5 として本番適用待ちを記録)。テスト5件追加。 | 2026-09-12 |
| P7-1 | レシート撮影による取り込み(新機能、本人発案、ADR-021)。現金・電子マネーなど CSV・メール通知(FR-10)のどちらにも記録が残らない支払いを、レシート写真から記録できるようにした。`src/features/import/receipt-ai.ts`(新規)の `ClaudeReceiptExtractor` — `claude-haiku-4-5` の画像入力でレシートから日付・支払合計・店名・支払方法として印字された文字列を書き写させる(email-ai.ts/ADR-019 と同じ役割分担:AI は書き写すだけ、リボ・分割の判定は `readPaymentMethod` の正規表現)。メール通知と異なり画像には辞書に相当する費用ゼロの経路が無いため、この経路は呼ばれた時点で必ず AI を使う設計にした(詳細は ADR-021)。`app/api/import/receipt/route.ts`(新規、認証は proxy.ts が担う。1時間あたりの呼び出し回数上限あり、email/route.ts と同じ歯止め)。`/transactions/receipt`(新規画面)— カメラ起動(`capture="environment"`)またはギャラリーから画像を選び、ブラウザ側で長辺1600px・JPEG品質0.85にリサイズしてから送信(画像トークン数と通信量を抑える)。「AI に読み取らせる」ボタンを押したときだけ課金APIを呼ぶ(貼り付け画面と同じ配慮)。取り込みパイプライン(`buildPreview`/`saveImportBatchAction`)は既存のCSV・貼り付け画面とそのまま共有し、`transaction_source` は新しい値を追加せず既存の `manual` を流用(スキーマ変更には本番 Supabase への適用手段がこのセッションに無いため、T-25 と同じ制約を理由に見送り。ADR-021 に却下理由を記録)。`/transactions` の一覧画面ヘッダとカード0件時の導線にリンクを追加。テスト14件追加(`buildFromAiRows` の検証・失敗時のフォールバック・画像ブロックが実際にモデルへ渡ることを偽クライアントで確認)。**未検証**:このセッションには本番 Supabase 資格情報が無く、実際のスマートフォン撮影・実物のレシートでの読み取り精度・Playwright でのブラウザ操作確認は行っていない(型チェック・lint・本番ビルド・ユニットテスト517件は全通過)。 | 2026-09-12 |
| P7-2 | LINE Messaging API 連携(本人発案)。Discord に加えて LINE へも通知を送れるようにした。**新規マイグレーション不要**——`notification_channel` enum には D-3 の初期スキーマの時点から既に `'line'` が存在しており(`alerts.channel`/`daily_briefs.channel`/`app_settings.brief_channel` の3箇所)、未配線のまま残っていただけだった(現状これらの列自体はどのチャネルへ送るかの判定にはまだ使っておらず、「設定されている全チャネルへ送る」という単純な方針にした。列を判定に使う本格的な「送信先を選ぶ」UIは今回のスコープ外)。`src/lib/line.ts`(新規、`lib/discord.ts` と同じ役割分担の薄い fetch ラッパー、LINE Push Message API)、`src/lib/env.ts` に `getLineEnv()`(Gmail と同じ「片方だけの設定はエラーにする」方式)と、両notify.tsが共有する `NotificationChannels` 型を追加。`features/alerts/notify.ts` の `sendPendingAlerts()` と `features/briefs/notify.ts` の `deliverDailyBriefAsAdmin()` を、Discord・LINE それぞれへ `Promise.allSettled` で並行送信し、設定されているチャネルのうち1つでも成功すれば sent/delivered とする形に変更(NFR-06:1チャネルの失敗で残りを止めない、を複数チャネルに拡張)。LINE はテキストのみで Discord の Embed のような色を持てないため、`buildLineTextForAlert()` が severity を絵文字(🔵🟠🔴)で表現する。`detect-alerts`/`morning-brief` 両 cron ルートは「Discord・LINE のどちらかが設定されていれば送信を試みる」判定に変更。**本人はまだ LINE のチャネルアクセストークン・userId を持っていない**(発行手順を `.env.example` に記載、B-6として記録)ため、実際の LINE API に対する送信は検証できず、`global.fetch` を差し替えて LINE 宛のリクエストだけ偽装する形で実際の Supabase プロジェクトに対して検証した:①LINEのみ設定・送信成功→alertsが正しくsent、②LINEのみ設定・送信失敗(401)→failed+error_messageに理由、③Discord失敗+LINE成功の混在→1チャネル成功で正しくsentになる、の3パターンを確認(検証中に誤って本物の pending alert 2件を巻き込んで sent にしてしまうミスをしたが、直後に気づいて pending へ復元済み)。テスト9件追加(`lineSchema` 3件、`buildLineTextForAlert` 4件、`postLineMessage` 2件)。 | 2026-09-13 |
| P8-1 | 完済シミュレーションの残高推移グラフ(本人発案、FR-02)。`/debts` の「月々の返済額を試す」区画がこれまで総月数・総利息という結果の数字だけを比較していたのに対し、「どう減っていくか」の過程を可視化した。新しい計算ロジックは増やさず、既に月次の残高を計算している `domain/payoff.ts` の `simulateTotalPayoff()` の出力(`closingBalanceYen`)をそのまま線にしただけ(`comparePlans()` はテスト済みの契約を崩さないためそのまま残し、グラフ用に同じ入力で明細を取り直す形にした)。`app/(app)/debts/payoff-curve-chart.tsx`(新規、Client Component)——「最低返済のみ」(`var(--ink-muted)` の破線)と「この金額で返済」(`var(--accent)`。globals.css が定める「返済・予算の残り(進捗)」の役割色をそのまま使う)の2本を1つの軸(残高)に重ねる。ホバーで任意の月の両プランの残高を線形補間で読み取れるクロスヘアと、1年ごとの節目に絞った表(dataviz: table view)を実装。実際の Supabase プロジェクトの本番債務データ(3件、合計残高100万円)を使い、`comparePlans()` の要約(56ヶ月/11ヶ月)と `simulateTotalPayoff()` の月次明細の行数が一致すること、両プランの残高系列が単調非増加(グラフがガタつかない)であることを実際の計算で確認。UIコンポーネントのため既存の慣例どおりユニットテストは追加せず(型チェック・lint・本番ビルドで検証、`/reports` の既存チャートコンポーネント群と同じ扱い)。 | 2026-09-13 |
| P8-2 | 明細の複数カテゴリ分割(本人発案)とサブスク(定期支払い)検知(本人発案)を1つのMRにまとめて実装。**分割**:新規テーブル `transaction_splits`(マイグレーション2本:`20260913000100_transaction_splits.sql`/`20260913000200_transaction_splits_rls.sql`、`docs/schema.sql` にも反映)を追加し、1件の明細(例:スーパーのレシートに食費と日用品が混ざる)を複数カテゴリへ配分できるようにした。`transactions.category_id`/`amount_yen` 自体は書き換えず(分割される明細は少数派)、`domain/transaction-splits.ts` の `assertValidSplits()`(2件未満・0円行・合計不一致を拒否)と `expandTransactionsWithSplits()`(集計前に分割済み明細を複数行へ展開してから既存の純粋関数へ渡す。集計ロジック側は変更不要)という「読み出し時に展開する」設計にした。展開は `features/reports/store.ts`(P6-2 カテゴリ別支出推移)と `features/alerts/store.ts` の `loadWasteCategoryStatuses()`(FR-20/P5-3/P6-1/T-23 が共有)に配線——**`features/home/summary.ts`(ホームの3指標)と `features/transactions/period-summary.ts`(FR-17 給料日区間ビュー)は今回のスコープ外**(最も見られる数字・複雑なカテゴリ合算ロジックへの影響を避けるための判断。分割した明細がある場合、この2画面の数字はまだ分割前の主カテゴリのままで、要 follow-up)。`/transactions` の各行(`split-editor.tsx`、新規 Client Component)から分割の追加・編集・解除ができる(符号は `Math.sign(amountYen)` で保存時に自動補正し、入力は常に正の金額)。**サブスク検知**:新規テーブルは持たず、`domain/subscriptions.ts` の `detectSubscriptions()` が毎回 `transactions` を集計し直す設計(`domain/spending.ts` と同じ都度計算の考え方)——同じ店・同じ金額の支出が25〜35日間隔(月1回相当)で2回以上連続すれば検知、価格改定があれば別グループとして扱う(price hike も再通知される仕様)。新規検知分だけ `alerts` テーブルへ記録する `detectAndRecordNewSubscriptionAlertsAsAdmin()`(`features/subscriptions/store.ts`)を既存の `detect-alerts` cron に追加(dedup_key の一意制約により再実行しても二重に積まれない、P5-2/P6-1 と同じ「積むのは冪等」パターン)。`/transactions` に検知結果のカード(`SubscriptionsCard`)を追加。**このセッションの制約により `transaction_splits` の新規マイグレーションを本番へ適用できず**(T-25/T-26/B-4/B-5 と同種、Supabase Management API 資格情報が無い)、`docs/schema.sql` との整合はローカル PostgreSQL で `verify-schema.sh`/`verify-migrations.sh` 双方検証済み(943項目一致)。分割機能を毎回無条件に読む3経路(`/transactions` の表示・`/reports` の集計・浪費アラートの集計)はテーブル未作成のエラー(PGRST205)を「分割は無い」として握り潰す設計にし、実際に本番 Supabase(テーブル未作成の状態)に対してこの失敗経路(読み書き両方)を検証し、他機能に影響しないことを確認(本番適用待ちは B-7 として記録)。サブスク検知は新規テーブル不要のため実際の本番 Supabase に対してフル検証済み:テスト用口座・3ヶ月連続の同一店舗同一金額の明細を作成→cron と同じ `detectAndRecordNewSubscriptionAlertsAsAdmin()` を実行→期待通り1件のアラート(正しい dedup_key・title・body)が記録されることを確認→再実行しても0件追加(冪等)であることを確認→テストデータはすべて削除済み。テスト24件追加(`assertValidSplits`/`expandTransactionsWithSplits`/`detectSubscriptions`/`buildNewSubscriptionAlert`)。**未検証**:実際のスマートフォン・PCブラウザでの `/transactions` 画面操作(分割エディタの開閉・保存、サブスクカードの表示)は本人のログインパスワードがこのセッションに無く、Playwright でのブラウザ確認は行っていない(型チェック・lint・本番ビルド・ユニットテスト580件・スキーマ検証はすべて通過)。 | 2026-09-13 |
| P8-3 | 本人からの不具合報告2件への対応(ADR-011改定)。①スマホで入力欄をタップするたびに画面が自動拡大される — iOS Safari が `font-size` 16px 未満の `input` をズーム対象にする既知の挙動が原因。`app/globals.css` に `input, select, textarea { font-size: 16px }` を追加して修正(見た目の `text-sm` クラスはそのまま)。Playwright(iPhone 13 エミュレーション)で `/login` の全 input が 16px になったことを確認。②Magic Link(`/login` の「メールでログイン」タブ)が「リンクが無効です」で機能しない — 実運用に耐えないため完全に廃止し、ログインをパスワードのみにした。`signInWithOtp`/`/auth/callback`(削除)・`proxy.ts` の該当 `PUBLIC_PATHS` エントリも削除。代わりに「新規登録」タブ(`app/login/actions.ts` の `registerPasswordAction()`)を追加したが、ここでの「登録」は新しいアカウントを作るものではない——`admin.auth.admin.updateUserById()` で**既存の**(本人の)アカウントのパスワードを直接書き換えるだけで、`admin.createUser()` は一切呼ばない(メールアドレスが既存アカウントと一致しない場合はエラーになり、新規アカウントは作られない。ADR-011 が元々守っていた「他人はログインできない」を保つ)。メールアドレスだけでは誰でも知っている前提の情報のため、本人しか知らない合言葉(新規環境変数 `REGISTRATION_SECRET`、`getRegistrationSecret()` を `lib/env.ts` に追加、`CRON_SECRET` と同じ位置付け・`timingSafeEqual` で比較)をもう1つの認証要素として要求する。成功後はセッションが無いため、クライアント側で続けて `supabase.auth.signInWithPassword()` を呼びログインする。`docs/decisions.md` の ADR-011 に改定を追記。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(582件全通過、`registrationSecretSchema` のテスト2件追加)/`npx next build` すべて成功。実際の本番 Supabase プロジェクトに対し、一時的なテスト用アカウントを作成して検証(本人の実アカウントのパスワードには一切触れていない):誤った合言葉→拒否、存在しないメールアドレス→拒否かつアカウントが作られないことを確認、パスワード確認不一致→拒否、正しい入力→パスワードが書き換わり新パスワードでのサインインに成功・旧パスワードでのサインインは失敗することを確認、テストアカウントは削除済み。Server Action 自体も実際に開発サーバー上で実行させ成功を確認(ログ確認済み)。**未検証**:ブラウザからの `supabase.auth.signInWithPassword()` 呼び出し(クライアント側)は、このサンドボックスの Chromium プロセスが直接外部 HTTPS(Supabase)へ到達できない制約(M2-7c の実 IMAP 接続と同種のサンドボックス固有の制約。本番の Vercel 環境には影響しない)のため、実ブラウザでのログイン成功までの一気通貫の確認はできなかった——ただし同一の `supabase.auth.signInWithPassword()` 呼び出し自体は Node 側(実際の Supabase JS クライアント)で成功を確認済みのため、ロジックとしては検証済み。**本人への確認待ちに追加**:Vercel の環境変数に `REGISTRATION_SECRET` を設定する必要がある(`.env.example` に発行手順を記載)。 | 2026-09-13 |
| P8-4 | 「新規登録」タブが「設定しています…」のまま応答なしで固まる不具合を修正。本番で実際に発生(本人の実機で確認)。原因:`registerPasswordAction()` がサーバー側の設定不備(`REGISTRATION_SECRET` 未設定など)で例外を投げると、呼び出し元(`login-form.tsx`)がそれを catch しておらず、ボタンの「送信中」状態から戻らなくなっていた。`app/login/actions.ts` の `registerPasswordAction()` 内で例外を catch し分かるエラーメッセージへ変換、`app/login/login-form.tsx` 側にも念のため try/catch を追加(二重の防御)。検証:`REGISTRATION_SECRET` を意図的に未設定にした状態で、修正後の処理フローが例外を投げずに分かるエラーを返すことを実際のコードで確認。`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(582件全通過)/`npx next build` すべて成功。 | 2026-09-13 |
| P8-5 | 本人の意向により、P8-3 で追加した「新規登録」タブの合言葉(`REGISTRATION_SECRET`)を撤回。`app/login/actions.ts`(`isValidSecret()`・`secret` 引数を削除)、`app/login/login-form.tsx`(合言葉の入力欄を削除)、`src/lib/env.ts`(`getRegistrationSecret()`/`registrationSecretSchema` を削除)、`.env.example`(該当項目を削除)、`tests/domain/env.test.ts`(該当テストを削除)、`docs/decisions.md`(ADR-011 に改定を追記)。**この変更の影響を明記**:メールアドレスが既存アカウント(1件のみ)と一致しさえすれば、誰でもパスワードを変更してログインできる状態になる(新規アカウントは作られないという制約は維持)。本人への確認待ちリストから「Vercel に `REGISTRATION_SECRET` を設定する」項目を削除(不要になったため)。検証:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`/`npx next build` すべて成功。 | 2026-09-13 |
| P9-1 | 家計簿の「ちりつも」機能群(本人発案)。**なぜ作ったか**:既存の家計簿(ホームの「あと◯円使える」FR-64・`/reports` のカテゴリ別推移 P6-2)はどちらも **引き算** の見せ方で、1回480円のような支出はどの画面にも現れない。小さい支出が積もっていることを実感させるには **掛け算**(回数・年換算・完済への影響)への翻訳が要る、という設計判断で新しい切り口を1画面に集めた。**換算エンジン**:`domain/payoff.ts` に `payoffImpactOfExtraPayment()` を追加——`comparePlans()`(FR-02、最低返済 vs 目標額)に対し、こちらは「今の返済額 vs 今の返済額+上乗せ分」を比べる。既存の完済シミュレータをそのまま支出側へ転用しており、新しい計算ロジックは足していない。実装中に、債務ゼロ(=このアプリが目指す状態)で `summarizePayoff()` が例外を投げることに自分のテストで気づき、先に畳んで {0,0} を返すよう修正した。**集計**:`domain/accumulation.ts`(新規、純粋関数)に4つ——`summarizeSmallSpends()`(1,000円未満を店ごとに合計。金額より回数が刺さるため count を必ず持たせ、店名の表記ゆれは空白除去+小文字化で吸収。domain/subscriptions.ts と同程度の正規化)、`summarizeNoSpendDays()`(FR-62 のストリークと対になる「貯まる側」。平均を**支出があった日だけ**で割るのが肝で、全日数で割ると無支出日が自分で自分の価値を小さく見せてしまう)、`compareToPreviousMonthPace()`(前月同日比。月末を待たずに差が見える。先月に同じ日が無い場合は月末に丸める)、`annualizedPaceYen()`(「合計×12」ではなく1日あたりに均してから365倍。月初の数日で大きく過小評価しないため)。**画面**:`/spending`(新規、ちりつも)。ホームと `/transactions` ヘッダから導線。棒は単一系列の大きさを表すだけなので支出の役割色 `var(--over)` 一色(P6-2 と同じ判断。店に色を割り当てない)、各行が数値を直接持つため一覧がそのまま table view を兼ねる。文言は設計原則5(責めない)に従い「使いすぎ」と判定せず事実だけを出し、警告色の面は使わない。**新規テーブル不要**(毎回 transactions を集計。分割 transaction_splits は展開しない——集計軸が「どの店でいくら」であり、1回の買い物を複数カテゴリへ按分しても店から見た支払いは1回・1金額のままのため)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(600件全通過、新規テスト20件)/`npx next build` すべて成功。実際の本番 Supabase に対して、しきい値の境界(8,000円の大口が小口に混ざらないこと)・平均の分母・前月同日比・年換算を実データで検証し、期待値と一致することを確認。さらに一時ユーザーに `seed_defaults()` + 現実的な小口データを投入し、Node 側でサインインして得たセッション cookie をブラウザへ注入する方法で `/spending` を実際に描画して確認(このサンドボックスのブラウザから Supabase へ直接到達できない制約を回避した)——ヒーローが「小口 7,200円 → このペースが1年続くと 202,154円 → 同じ額を返済に回すと利息が 11,025円 減る・完済も1ヶ月早まる」と出ることを実機相当で確認し、ライト/ダーク両方・横スクロール無し(NFR-07)も確認。テストデータはすべて削除済み(残ユーザー1・transactions 0・accounts 0 を確認)。**判明した仕様上の限界**:完済の短縮は月単位でしか動かないため、月5,000円程度の上乗せでは0ヶ月と出る。そのときも利息だけは必ず動くので、行が単独でも意味が通る文言にしてある。 | 2026-09-13 |
| P9-2 | 複数経路から入った同じ買い物(二重計上)の検知(本人発案)。**なぜ必要か**:取り込み経路は CSV(FR-10)・メール通知(ADR-018)・レシート撮影(ADR-021)の3つあるが、重複排除は `transactions.fingerprint`(口座\|日付\|金額\|摘要を正規化)一本しかない。同じ1回の買い物でもレシートは「セブン-イレブン渋谷1丁目店」(画像から読んだ店名)、メールは「ｾﾌﾞﾝｲﾚﾌﾞﾝ」(カード利用通知の表記)と摘要が一致せず、口座も別になりうる(レシートは本人が画面で選ぶ / メールは `GMAIL_IMPORT_ACCOUNT_ID` 固定)ため、fingerprint では原理的に拾えない。**実際に本番 DB のトリガが付けた fingerprint が別物になること(`bdcfa90…` vs `cb03204…`)を実データで確認済み**。今はメール自動取り込みが停止中(`app_settings.gmail_enabled` が false、M2-7c 待ち)なので顕在化していないが、有効化した瞬間に二重計上が始まり、予算・ちりつも・アラートの数字が実際より膨らむ。**検知方法**:`domain/duplicate-match.ts`(新規、純粋関数)の `findDuplicateCandidates()`——摘要と口座は当てにできないので、①金額の一致 ②日付の近さ(既定±3日)③**取り込み経路が違うこと**の3条件で候補を出す。③が誤検知を抑える一番効く条件(同じ経路どうしの同額は「同じ店で2回買った」の方が起こりやすいため候補にしない)。1件が複数の候補に現れると本人が判断しにくいので、1件につき1ペアまでに絞る。どちらを残すかは決めない(同じ買い物かの最終判断は本人にしかできない)。**画面**:`/transactions/duplicates`(新規)。ペアごとに両方を並べ、日付・取り込み経路・口座名を添えて「これを残す」を押してもらい、押されなかった側を `review_status='ignored'` にする。違うものなら「別の買い物なので、どちらも残す」で候補から消すだけ。`/transactions` 一覧の上部に候補数のバナー(確認待ちキューと同じ形)。**新規テーブル不要**——除外の記録は既存の `review_status='ignored'` を使う(`domain/budget.ts` の `isCountable()` が全機能で除外するため、予算・レポート・ちりつも・アラートから一斉に消える)。行は削除しない(消すと次の取り込みでまた入ってきたときに「前にも同じことがあった」が分からなくなる)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(610件全通過、新規テスト10件)/`npx next build` すべて成功。実際の本番 Supabase に対し、レシート(manual・カード口座)とメール(gmail・現金口座)で摘要違いの同額同日付近の明細を作り、①DB が付けた fingerprint が実際に別物であること ②`findDuplicateCandidates()` が1組だけ検知すること ③同じ経路どうしの同額(カフェ2回)は誤検知しないこと ④片方を ignored にすると候補から消えること、を確認。さらに一時ユーザーに `seed_defaults()` + 2組の重複データを投入してブラウザで一気通貫に確認(一覧に「二重に入っていそうな明細が 2 組」のバナー → 重複確認画面で両方が経路名付きで並ぶ → 「これを残す」で1組消える → **DB 側も押されなかった側だけ `ignored` + `reviewed_at` になっていることを確認**)。テストデータはすべて削除済み(残ユーザー1・transactions 0・accounts 0 を確認)。 | 2026-09-13 |
| P9-3 | レシートの情報を最大限活かす2点(本人発案、ADR-021 の却下事項を採用へ切り替え)。**①商品行の複数カテゴリ分割**:`receipt-ai.ts` の抽出スキーマに商品行(品名・金額)を追加。行の合計が明細の金額と一致し2件以上ある場合だけ `ParsedReceiptTransaction.items` に入る(合わない・1件だけなら黙って空配列に落とし、取り込み自体は今までどおり1件のまま続けられる)。商品行は**店名向けのルールではなく商品名ごと**に既存の分類パイプライン(ルール→本人操作でのAI)へ通し、保存時に `transaction_splits`(P8-2)を自動生成する。親(明細本体)の分類は変えないが、`review_status='pending'` のままだと確認待ちキューで本人が1カテゴリだけ選んだ瞬間にその店名の学習ルールができ、次の同じ店の買い物(実際は混在でも)を丸ごと誤分類する事故になるため、保存時に `auto_ok` へ倒す(receipt/page.tsx の save() 参照。isCountable() は reviewStatus を見ないため予算・重複検知には影響しない)。**②レシート画像の保存**:`receipts` バケット(private)へ `{user_id}/{uuid}.拡張子` で保存(`features/import/receipt-storage.ts`)。抽出時ではなく本人が取り込みを確定した時点でだけアップロードする(結局使わなかった写真まで溜めない)。パスは新規列 `import_batches.receipt_image_path` に記録(1回の撮影=1バッチのため。1枚の写真に複数の買い物が写っていても複製しない)。重複確認画面(`/transactions/duplicates`)からレシート側の写真を開けるようにし、Gmail 側の通知と同じ買い物かどうかを本人が見比べられるようにした。**マイグレーション**:`20260913000300_import_batches_receipt_image.sql`(列追加)・`20260913000400_receipt_storage_rls.sql`(`storage.objects` のフォルダ単位 RLS ポリシー)の2本、`docs/schema.sql` 3.7節・7.1節にも反映。`receipts` バケット自体は本セッション中に Storage REST API で実際に作成・確認済み(private、file_size_limit 6MB、image/jpeg・png・webp のみ許可)。**本番でまだ列が無いことを実際に確認し、握り潰す設計にした**:`import_batches.receipt_image_path` を含む INSERT を本番の service_role キーで試したところ `PGRST204`(SELECT では `42703`)が返ることを確認したため、`importTransactions()` はまず列付きで insert を試し、この2つのコードのときだけ列無しへ自動フォールバックする(CSV・メール取り込みは元々この列を渡さないため無関係。**この確認をしなければ、レシート機能以外を含む全取り込みが本番で壊れるところだった**——最初の実装は無条件に列を含めていた)。**Gmail重複検知との整合性**:`domain/duplicate-match.ts`/`isCountable()` は reviewStatus・splits を見ないため無改修で動く(確認済み)。`docs/decisions.md` の ADR-021 に、却下していた2案を採用に切り替えた旨を改定として追記。テストデータはすべて削除済み(残ユーザー1・transactions 0・accounts 0 を確認、作成した import_batches テスト行も削除済み)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(620件全通過、新規テスト25件)/`npx next build`/`scripts/verify-schema.sh`/`scripts/verify-migrations.sh`(944項目一致)すべて成功。実際の本番 Supabase に対し、バケット作成・列無しでの INSERT フォールバック・`transaction_splits` 未作成時の握り潰し(PGRST205、既存経路の再確認)を検証。**未検証**:①・②とも実際のスマートフォンでの撮影・保存・重複確認画面での写真表示は、B-9(列・RLSポリシー未適用)の間は機能そのものが働かない(握り潰されて「今までどおり」になるだけ)ため確認できない。適用後、実機での一気通貫確認が必要。**追記(2026-09-13、同日中)**:本人が両マイグレーションを Supabase SQL Editor で実行。1本目(列追加)は成功したが、2本目(RLSポリシー)が `must be owner of table objects`(42501)で失敗した(`storage.objects` の所有者は `supabase_storage_admin` で、SQL Editor の `postgres` ロールは所有権を持たないため、元々不要だった `alter table storage.objects enable row level security;` が ALTER TABLE の所有権チェックに引っかかっていた)。その1行を削除しポリシー作成だけの形に修正して再度実行してもらったところ成功。`import_batches.receipt_image_path` 列が実際に読めることを本番で確認済み(B-9 解消)。実際のスマートフォンでの撮影・保存・重複確認画面での写真表示は依然未確認(次回の実機確認が必要)。 | 2026-09-13 |
| P9-4 | ナビゲーションの動線改善(本人発案)。**なぜ**:「家計簿(ちりつも)に飛ぶ動線が難しい」「レシートの取り込み口がわかりづらい」という指摘。`/spending`(ちりつも)は下タブに無く、`/transactions` の見出しに並ぶ小さい文字リンク(口座・ちりつも・突き合わせ・レシート・取り込む)の1つとして埋もれていた。レシート撮影は毎回の記録行動そのもの(設計原則2:記録の手間を最小化)なので、埋もれていると本人の言うとおり「めんどくさくて撮らなくなる」。**対応**:①下タブ(`app/(app)/layout.tsx`)に「家計簿」(`/spending`)を追加し、5タブ(ホーム・家計簿・明細・負債・給料日)に構成し直した。②レシート撮影(`/transactions/receipt`)はタブに埋めず、タブの隣に常設の丸ボタン(📷 撮る)を独立させ、どの画面からでも1タップで開けるようにした。③重複した動線を整理——`/transactions` の見出しから「ちりつも」「レシート」の文字リンクを削除(下タブ・専用ボタンに統合)、ホーム画面下部の「ちりつも」リンクも削除(下タブと重複するため)。**検証**:下タブ+丸ボタンの実測(HTML/CSSモックアップをPlaywrightで360px・400px幅のスマホ相当でスクリーンショット)で、5タブ+丸ボタンが折り返しなく収まることを確認。このセッションには本人の実アカウントでログインする手段が無く(パスワード未提供)、実アプリでの認証後の画面は確認できないため、静的モックアップでの目視確認に留めた。`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(620件全通過、UIのみの変更のためテスト追加なし)/`npx next build` すべて成功。 | 2026-09-13 |
| P9-5 | 外部サービス連携3点(本人発案。「他なんか外部サービス使って便利よくできないかな」への提案から「1、2、3いいねやろう」で承認)。**②通知にグラフ画像を添える**:`src/lib/quickchart.ts`(新規)がQuickChart.io(無料・認証不要の画像化API)のURLを組む。既定はChart.js v2互換のためv3/v4記法(`indexAxis`/`plugins.legend`/`scales.x`)が効かず、実際にレンダリングして初めて気づいた——`version=4` クエリパラメータを付けて解決(`buildChartUrl()`)。`buildPayoffProgressChartUrl()`(完済進捗の横棒)を `features/briefs/notify.ts` の朝配信(Discord embedの `image`・LINEは `postLineTextWithImage()` を新設)に配線。画像生成に失敗しても配信自体は止めない(`.catch(() => null)`)。**①Googleカレンダー同期・③Googleスプレッドシート自動バックアップ**:初めてOAuth2をこのアプリに導入(ADR-022、認証情報の扱いはADR-014をそのまま適用——`refresh_token`はDBに一切保存せず環境変数のみ)。`src/lib/google-auth.ts`(トークン交換・更新)、`src/lib/google-calendar.ts`/`google-sheets.ts`(薄いAPIクライアント、discord.ts/line.tsと同じ役割分担)、`/api/auth/google/start`・`/callback`(認可コードフロー、state検証)、`/settings/google`(同意ボタン+セットアップ手順)を追加。①`domain/calendar-sync.ts` が給料日(3ヶ月先まで)・サブスク更新日・完済予定日の予定を計画する純粋関数——イベントIDはGoogle Calendarの文字集合制約(`^[a-v0-9]{5,1024}$`)のため`toCalendarEventId()`でSHA-1ハッシュ化。**設計中に気づいた罠**:サブスク・完済予定日の鍵に日付を含めると、見積もりが更新されるたびに別イベントが作られカレンダーに残骸が積み重なる——鍵を日付なしにし`upsertCalendarEvent()`(POST→409ならPUTへフォールバック)で同じイベントを更新する形に直した(ADR-022に詳細)。`features/google/calendar-store.ts` の `syncCalendarAsAdmin()` が配線。③`features/google/sheets-store.ts` の `backupTransactionsToSheetAsAdmin()` が月末(`isLastDayOfMonth()`)だけ当月の明細をスプレッドシートへ書き出す。バックアップ先IDは新規列 `app_settings.google_backup_spreadsheet_id`(マイグレーション`20260913000500_app_settings_google_backup.sql`)に記録し以後同じシートへ書き続ける。**P9-3の劣化パターン(列が無ければ諦めて処理続行)をそのまま使わなかった理由**:ここで列が無い間に処理を続けるとIDを記憶できず月末ごとに新規スプレッドシートを作り続けてしまうため、列欠落を検知した時点でバックアップ全体を中止する設計にした(ADR-022)。①③は1つの日次cron `/api/cron/sync-google`(`sync-calendar`から改名、`.github/workflows/sync-google.yml`、07:30 JST)にまとめ、P6-3の相乗りパターンを踏襲——2つのジョブは互いに独立で片方の失敗がもう片方を止めない。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(640件全通過、新規テスト20件:quickchart 4件・calendar-sync 7件・calendar-store 3件・sheets-store 4件・env の googleSchema 2件)/`npx next build`/`scripts/verify-schema.sh`/`scripts/verify-migrations.sh`(945項目一致)すべて成功。OAuthトークン交換・Calendar/SheetsAPIの各エンドポイントは無効な認証情報で実際のGoogle APIに疎通しエラー形状のみ確認済み。**未検証**:本人によるGoogle Cloud ConsoleでのOAuthクライアント作成と`/settings/google`での同意フローの実行手段がこのセッションに無く、実際のカレンダー反映・スプレッドシート作成は一気通貫では確認できていない(B-9として記録)。 | 2026-09-13 |
| P9-6 | `/reports` の分析系を拡充(本人発案「分析系をもっと充実させたい」)。**①年間収支サマリー**:`domain/spending.ts` に `summarizeMonthlyIncomeExpense()`(直近12ヶ月の月次収入・支出)と `savingsRateOf()`(貯蓄率=(収入-支出)/収入、収入0円の月は0%と誤読させないため null)を追加。カテゴリ別集計(`summarizeMonthlySpendByCategory`)と違い**未分類(categoryId: null)の明細も含める**——収支の全体像を出す集計であり、1円でも漏れると貯蓄率がずれるため(コメントで理由を明記)。`features/reports/store.ts` の `loadIncomeExpenseTrend()` は分割(transaction_splits)を展開しない(分割はカテゴリの按分であって金額の合計は変わらず、収支全体には影響しないため、P6-2 のような display expand が不要と判断)。画面は `income-expense-chart.tsx`(新規)——net-worth-chart.tsx と同じ2本線のSVG(収入=`var(--income)`、支出=`var(--over)`)。貯蓄率(%)は金額(円)と単位が違うため同じ軸には乗せず、直接ラベルと表だけで示す(dataviz「1つのグラフは1つの軸」)。**②店舗別支出ランキング**:`domain/spending.ts` に `rankMerchantsBySpend()` を追加——`domain/accumulation.ts` の `summarizeSmallSpends()`(ちりつも「小口の山」、1,000円未満限定)と対になる切り口で、金額を絞らず家賃・保険のような大口も含めた全体ランキングを出す。分割を展開しない理由・表記ゆれの正規化(空白除去+小文字化)も accumulation.ts と同じ考え方(型 `AccumulationTransaction` をそのまま import して再利用し、同じ形の型を2箇所で定義しないようにした)。`features/reports/store.ts` の `loadMerchantSpendingRanking()`(直近3ヶ月、上位10件)。画面は `merchant-ranking-card.tsx`(新規)——単一系列(支出額)のため店ごとに色を割り当てず「支出」の役割色 `var(--over)` 一色の横棒ランキング(P6-2 と同じ判断)。`/reports` のヘッダ説明文を「直近6ヶ月のカテゴリ別支出」から「収支・カテゴリ別支出・店舗別支出・資産推移」に更新。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(651件全通過、新規テスト11件)/`npx next build` すべて成功。新規マイグレーション不要(既存列のみで完結)。実際の本番 Supabase に対し、一時的な検証用口座・収入1件(25万円)・支出3件(同一店舗2件+別店舗1件、計1.3万円)を作成し、`loadIncomeExpenseTrend()`/`loadMerchantSpendingRanking()` と同じクエリを実行して想定どおりの形の行(収入・支出・店名)が返ることを確認。検証用データはすべて削除済み(残transactions 0・accounts 0 を確認)。 | 2026-09-13 |
| P9-7 | ホームの予算タイルから当月の内訳を見られるようにする(本人発案「ホームのやつからクリックしたときにその詳細を見れるようにしたい 生活費って押したら一覧バーみたいなんで」)。**なぜ**:ホームの2枠(FR-61、例:生活費・聖域)は残額の数字だけで、何がその数字を作っているか(今月何にいくら使ったか)を見るには `/transactions` の全件一覧から自分で探す必要があった。**実装**:`features/categories/category-detail-store.ts`(新規)の `loadCategoryMonthDetail(categoryId)` が、指定カテゴリの当月の予算状況(`domain/budget.ts` の `budgetStatusFor()`)と、集計対象(振替・ignored除外)の当月明細一覧を返す。**ホームの数字と必ず一致させる設計**:統廃合カテゴリの扱い(`expandMergedCategoryIds`/`resolveCategoryRoot`)・予算額の解決(`budgets` テーブルの当月行 → 無ければ `categories.default_monthly_budget_yen`)を `features/home/summary.ts` の `listMonthTransactions()`/`listHomeCategories()` と同じロジックで揃え、表示する明細一覧の合計が `status.spentYen` と必ず一致するようにした(タイルを押して開いた画面の合計がタイル自体の数字と違うと、本人が数字を信じなくなるため)。画面は `/budget/[categoryId]`(新規)——上部はホームと同じ `StatTile` を再掲(数字が一致することを見た目でも裏付ける)、下は当月の明細を日付の新しい順に並べ、各行に金額の大きさに応じたバーを添えた(本人の要望どおり「一覧」を「バー」で見せる。支出は `var(--over)`、収入相当の行(返金等)は `var(--income)` で色分け——`app/globals.css` に `--income-track` を新規追加、既存の `--over-track` と対になる薄いトラック色)。ホーム(`app/(app)/page.tsx`)の各タイルを `<div>` から `<Link href="/budget/{categoryId}">` に変更。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(651件全通過、UIとデータアクセス層のみの変更のためユニットテスト追加なし)/`npx next build` すべて成功。新規マイグレーション不要。実際の本番 Supabase に対し、一時的な検証用カテゴリ(予算3万円)・支出2件(計7,000円)・振替1件(1,000円)を作成し、`loadCategoryMonthDetail()` と同じロジック(`tsx` で実際の domain 関数を直接呼び出し)を実行:①`status.spentYen` が振替を除いた7,000円になること、②表示対象の明細が2件(振替は除外)であること、③明細の合計金額が `status.spentYen` と完全に一致することを確認。検証用データ(カテゴリ・口座・明細)はすべて削除済み。 | 2026-09-13 |
| P9-8 | AI相談(目標設定・買う前相談、本人発案「目標設定する必要があるかも。AIと対話しながら次の目標を明確にして。あと、買う前相談」)。着手前に本人へ確認し、①目標設定と買う前相談を1つのチャット画面にまとめる、②目標設定の結果は構造化して保存し進捗を追える、の2点を決めた(詳細はADR-023)。**初めての対話型AI機能**:既存のAI利用(分類・レシート抽出)はすべて機械的な処理のみで、本人と自由に会話する機能はこれが最初。新規テーブル `goals`(マイグレーション2本:`20260914000100_goals.sql`/`20260914000200_goals_rls.sql`、`docs/schema.sql` にも反映。タイトル・目標金額・期限はいずれも null 許容——金額や期限を持たない目標もあるため)。`domain/goals.ts` に検証関数(`assertGoalTitle`等)と `goalProgressRatio()`(進捗率、目標が無ければnull・貯めすぎたら1を超える値をそのまま返す)、`features/goals/store.ts` にCRUD。**進捗はなぜ自動計算しないか**:P9-6の年間収支サマリーから機械的に引き当てることはできない(本人が複数の目標を並行して持てる上、返済に回した分と目標のために貯めた分は別物)ため、`current_amount_yen` は本人が更新する前提にした。目標金額に達したら `updateGoalProgress()` が自動で `status='achieved'` へ倒す(FR-64の肯定形、debts/job_change_milestonesの節目と同じ考え方)。**チャットの実装**:`features/advisor/chat.ts` の `AdvisorChat` がclassification/ai.ts・receipt-ai.tsと同じ `messages.parse`+`zodOutputFormat` を使い、1回の呼び出しで会話の返答(reply)と目標の提案(goalProposal、任意)を同時に得る——ツール呼び出し(tool use)の往復を増やさずに済む設計。モデルはADR-010のHaiku方針から外れ`claude-sonnet-5`を使う(本人と直接やり取りする対話の質を、頻度の低さと引き換えに優先する判断)。`features/advisor/context.ts` の `buildAdvisorContextText()` が `loadHomeSummary()`(完済状況・ホームの予算枠)と進行中の目標一覧を文章化してシステムプロンプトへ渡し、AIには「ここに無い数字は断定しない」よう指示する(具体的な金額計算はAIにやらせない、既存のAI機能と同じ役割分担)。目標の提案はAIが即保存せず、画面の確認カードで本人が「この目標を保存する」を押した時点で `createGoal()` を呼ぶ。**会話は保存しない**:ブラウザの状態にだけ持ち、画面を離れると消える(まず小さく作る判断、TASKS.md参照)。画面は `/advisor`(新規、目標カード一覧+チャット)、ホームに「AI相談」リンクを追加。`goals` テーブルは他の新規テーブルと同じくこのセッションから本番へ適用できないため(B-10)、`listActiveGoals()` はテーブル未作成のエラー(PGRST205)を「目標はまだ無い」として握り潰し(`transaction_splits`と同じ考え方)、書き込み系(`createGoal`等)は「目標機能はまだ利用できません」と分かりやすいメッセージを返す。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(670件全通過、新規テスト19件:domain/goals 13件・AdvisorChat 6件)/`npx next build`/`scripts/verify-schema.sh`/`scripts/verify-migrations.sh`(970項目一致)すべて成功。実際の本番SupabaseがPGRST205を返すことを直接確認し、握り潰し設計が正しく機能することを検証。**未検証**:このセッションには `ANTHROPIC_API_KEY` を持つ実行環境が無く、実際のAI応答(会話の質・goalProposalの精度)は確認できていない(AdvisorChatのユニットテストは「モデルが何を返してきても正しく扱うか」のみを担保)。 | 2026-09-13 |
| P10-1 | ボトムナビの「もっと」ドロップアップメニュー(新機能、本人発案「スクショの部分のメニューがわかりずらい。もっとメニューをわかりやすくできないかな…下のメニューからドロップアップしてメニュー一覧とかの方がいいかも…アニメーションにもこだわって欲しい」)。着手時点のボトムナビは4件(ホーム/負債/明細/給料日)で、それ以外の画面(口座・ルール・投資・副業・転職準備・レポート・朝配信・設定群・メール貼り付け・突き合わせ・重複確認・AI相談)は各画面に散らばった導線からしか辿れず把握しづらかった。**P9-4(ナビゲーションの動線改善)と同時期に並行して着手したため、マージ時に合流させた**:P9-4 が追加した5タブ+レシート撮影の丸ボタンは維持しつつ、`src/components/ui/more-menu.tsx`(新規)としてタブの6つ目に「もっと」を追加。iOS の sheet と同じ曲線(`cubic-bezier(0.32,0.72,0,1)`)でドロップアップするグループ分けメニュー(記録を増やす/相談する/分類を育てる/この先に向けて/振り返る/設定)。開閉のたびに DOM を作り直すと閉じるアニメーションが再生されないため、常時マウントしたまま `transform`/`opacity`/`pointer-events` を切り替える設計にした。リンクを踏む・戻るボタン・Escapeキー・背景タップのいずれでも閉じる。画面遷移での自動クローズは `useEffect` 内での `setState` を避け(M2-3b と同じ理由でカスケードするレンダーを避ける react-hooks/set-state-in-effect 対応)、レンダー中に前回の pathname と比較して閉じる形にした。`prefers-reduced-motion` でアニメーションを無効化。メニューの中身は P9-1〜P9-8 で増えた `/spending`(下タブに統合済みのため対象外)・`/advisor`・`/transactions/duplicates`・`/settings/google` も反映済み。テストは無し(レイアウト・アニメーションのみで純粋関数を含まないため、既存の類似コンポーネント(skeleton.tsx 等)と同型)。 | 2026-09-14 |
| P10-2 | ルールをAIに相談する(新機能、本人発案「Ai相談からルール変更したりとかしたい」、ADR-024)。分類ルールの編集(/rules、M2-6)は一覧から選んで直す画面だったが、「スタバは浪費にして」のように話しかけるだけでルールを作成・変更・削除できる会話画面を追加した。`src/features/classification/chat-tools.ts`(新規、純粋関数のみ)— 会話メッセージの検証(`parseChatMessages()`)、カテゴリ名の解決(`resolveCategoryByName()`、実在するカテゴリと完全一致でしか解決せず勝手に読み替えない)、システムプロンプトの組み立て(`buildRuleChatSystemPrompt()`、カテゴリ・ルールの現在値をそのつど埋め込む。list 用の tool は持たせず往復を減らした)。`app/api/rules/chat/route.ts`(新規)— `claude-haiku-4-5` に `create_rule`/`update_rule`/`delete_rule` の3 tool を持たせ、本人との会話を毎回全文送り直すステートレス設計(新規テーブルを増やさずに済ませる。T-25/T-26 と同種の制約)。**要点はFR-21(リボ・キャッシング・分割払い)の検知ルールを会話から触れさせない二重の砦**:①`create_rule` には支払方法を設定する経路がそもそも無い、②`update_rule`/`delete_rule` は実行直前に対象ルールを取り直し `setPaymentMethod` が設定されていれば `ChatToolError` で拒む(`assertNotProtected()`)。システムプロンプトでの指示だけでは「重ねて頼めば通る」経路を塞ぎきれないため、tool の形と実行直前の再チェックの両方で守る設計にした(ADR-024 に詳細)。`src/features/classification/store.ts` に `createClassificationRule()`/`updateClassificationRule()`(新規、パターン・カテゴリ・有効無効のみを対象とし `setPaymentMethod` を設定する経路を持たない)と、`ClassificationRuleSummary` への `setPaymentMethod` フィールド追加(保護対象の判定に使用)。`/rules/chat`(新規画面)— チャットUI、吹き出しは `pop-in` アニメーション(新規、`app/globals.css`)、送信中は3点の `typing-dot` アニメーション。`/rules` 画面ヘッダとボトムナビの「もっと」メニュー(P10-1)にリンクを追加。**P9-8(AI相談・目標設定/買う前相談)とは別物**:あちらは `/advisor` で家計全体の相談・対話を行う機能、こちらは `/rules` の分類ルールという1つの対象に絞った編集アシスタント(モデルもP9-8のSonnetに対しHaiku 4.5を使用、ADR-010のコスト方針を踏襲)。テスト17件追加(`chat-tools.ts` の純粋な部分のみ。route.ts 自体は他の `app/api/*/route.ts` と同じく実際の Supabase プロジェクトに対して手動で検証する既存方針に倣う)。**未検証**:このセッションには本番 Supabase 資格情報が無く、実際の会話でモデルがどの程度的確に tool を選ぶかは未検証(型チェック・lint・本番ビルド・ユニットテスト全件は全通過)。 | 2026-09-14 |
| P10-3 | 本人からの不具合報告「メイン画面に戻ってきたときにフリーズしてなかなか表示されないことがある」に対応。ホーム(`/`)は ADR-001 により `force-dynamic` で、戻るたびに必ずサーバーで組み立て直す。`app/(app)/page.tsx` はこれまで `await recordCheckin()` を `loadHomeSummary()`/`getCheckinStreak()` の**前に直列で** await していたが、`recordCheckin()`(今日確認した記録の upsert)は summary/streak のどちらの結果も使っておらず、本来並列にできる処理だった。直列だったせいで、ホームに戻るたびに「確認記録の書き込みの往復」+「3つの数字の読み込みの往復」が単純合算され、特に復帰直後(スマホの画面ロック解除・タブ切り替え直後で回線が再接続中)は Supabase Auth への `getUser()` 呼び出し(この1リクエストだけで `proxy.ts`・`recordCheckin()`・`loadHomeSummary()` の計3回発生)が重なりフリーズして見えていたと判断した。`recordCheckin()` と `loadHomeSummary()` を `Promise.all()` で並列化(合計待ち時間が `recordCheckin` 分だけ短縮される)。ただし `getCheckinStreak()` は `app_checkins` の行数を数えるビュー(`v_checkin_streak`)を読むため、`recordCheckin()` の upsert より先に走らせると「今日の分」を含め損ねてストリークバッジの日数が1日ずれる恐れがあり、そちらは従来どおり `recordCheckin()` の後に残した。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(687件全通過)/`npx next build` すべて成功。実際の本番 Supabase・実機での体感速度の計測はこのセッションの手段では行えないため未検証(型・テスト・ビルドでの確認に留まる)。 | 2026-09-14 |
| P10-4 | P10-1(その他メニュー)への本人フィードバック4点に対応。**①メニューが閉じない**:`backdrop-blur-xl` を持つボトムナビの `<ul>` の内側にオーバーレイを描画していたため、一部ブラウザでは `backdrop-filter` が子孫の `position: fixed` の containing block になり、背景タップで閉じるはずの範囲がナビバーの矩形内に閉じ込められていた(「開くとどこを押しても閉じない」の原因)。`createPortal` で `document.body` 直下に描画するよう `src/components/ui/more-menu.tsx` を改修し、バツボタンは廃止して背景タップ・Escape・リンク遷移のいずれでも閉じる形にした(サーバー/クライアント判定は `useEffect`+`setState` ではなく `useSyncExternalStore` で行い、react-hooks/set-state-in-effect を回避)。**②「もっと」はダサい**:タブ名を「その他」に変更し、シート見出しも「その他の機能」+「外側をタップで閉じる」のヒント文言に統一、各グループ見出し・項目名も含め全体の文言を整えた。**③その他メニュー以外からの導線が無い**:`/transactions/review` の説明文に `/rules`・`/rules/chat` へのリンクを追加(その他メニューを開かなくてもルール編集・ルール相談に辿れるようにした)。**④ホームの残額タイルを押すとその場で内訳を見たい(遷移せずに)**:以前は `/budget/{categoryId}`(P9-7)へ遷移していたが、本人の要望どおりホームに留まったまま開閉できるよう作り直した。`src/features/categories/category-detail-types.ts`(新規)に画面向けの型だけを分離(`category-detail-store.ts` は `next/headers` 依存の `createClient()` を使うため、そこから型だけ import してもクライアントバンドルに引き込まれてしまう。T-7 と同型の問題)。`src/components/ui/expandable-budget-tile.tsx`(新規、Client Component)が `StatTile` をトグルボタンで包み、展開時に明細一覧(旧 `/budget/[categoryId]` と同じ棒グラフ表現)をその場に描画する。ホーム側(`app/(app)/page.tsx`)でタイルの数(高々2枠、FR-61)だけ `loadCategoryMonthDetail()` を並列に事前取得し、クライアント側での追加 fetch を無くした。`ExpandableBudgetTile` が `StatTile`→`Meter` 経由でクライアントバンドルに入ったことで、`Meter` が `features/home/summary.ts`(`next/headers` 依存)から runtime import していた `MILESTONES` 定数がビルドを壊す2つ目の境界違反として発覚(1つ目は上記の型分離で解消)。`MILESTONES`/`nextMilestone()` を DB非依存の `src/domain/budget.ts` へ移設し、`meter.tsx`・`features/home/summary.ts` の両方をそちらの import に差し替えて解消。用済みになった `/budget/[categoryId]` ルートは削除(参照が残っていないことを grep で確認済み)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(687件全通過)/`npx next build` すべて成功。**未検証**:実機・実ブラウザでのタップ操作(背景タップでの閉じ方・アニメーションの体感)はこのセッションの手段では確認できない。 | 2026-09-14 |
| P10-5 | レシート画像の受信をDiscord/LINEで自動化したい・複数枚まとめて送りたい(本人発案「discodeとlineでレシート画像送信。あとは複数レシート送信」、ADR-025)。**Discordは見送り**:受信にはGatewayに常時接続するBotが要り、Vercelサーバーレス+GitHub Actions構成(ADR-012)と噛み合わないと説明し、本人が「Discordは諦めてLINEのみにする」を選択。**LINE受信**:`app/api/webhooks/line/route.ts`(新規)がLINEのWebhookを受ける。`proxy.ts`の認証関所は本人のセッションcookieを前提にしておりLINEのサーバーはそれを持たないため、`api/webhooks`を`api/cron`と同じく関所の対象外にし(`matcher`)、`X-Line-Signature`(チャネルシークレットで生bodyをHMAC-SHA256した値、`timingSafeEqual`で比較)を自前検証する。加えて`source.userId`が`LINE_USER_ID`と一致しない画像は無視する(他人がこのアカウントを見つけて送っても取り込ませない、なりすまし対策)。LINEは1メッセージ=1画像で`events`配列に積むため、複数枚まとめて送っても配列を順に処理するだけで対応でき「複数レシート送信」に新しい仕組みは要らなかった。`src/features/import/line-receipt.ts`(新規)の`importLineReceiptAsAdmin()`がAI抽出(`ClaudeReceiptExtractor`)→ルールのみの分類(`buildPreview()`、AI分類は呼ばない。confirm前に誤分類が確定する経路を作らないためADR-019と同じ判断)→画像保存(`uploadReceiptImage`、admin client)→保存(`importTransactionsAsAdmin`)→LINEへの要約返信(`postLineMessage`)までを1画像分行う。商品行(items)の分割(`transaction_splits`)は確認画面が無いこの経路では作らない(対象外、ADR-025に詳細)。**`importTransactions()`の分割**:本人のセッション前提だったロジックを`importTransactionsAsAdmin(supabase, userId, ...)`に切り出し、`importTransactions()`はセッションから`user_id`を取り出すだけの薄いラッパーにした(`app/api/cron/keepalive/route.ts`と同じ管理クライアントパターン)。`src/lib/env.ts`に`getLineChannelSecret()`/`getLineReceiptAccountId()`(既存の`getLineEnv()`とは独立した任意項目、既存のLINE送信専用ユーザーを壊さないため)、`src/lib/line.ts`に`fetchLineImageAsBase64()`(LINE Content APIから受信画像を取得、常にJPEG)を追加。`job_runs`に`job_name: 'import_line_receipt'`/`trigger_source: 'webhook'`で実行結果を記録(監視画面から他のジョブと同じ形で見える)。**Web画面の複数枚対応**:`/transactions/receipt`を全面書き換え、単一の`imageBase64`/`extracted`から`ReceiptEntry[]`の配列に変更(`<input multiple>`)。抽出(`extractAll()`)は各エントリを並列実行、AI分類(`classify()`)は全エントリの未分類行をまとめて1回に集約(枚数が増えてもAPI呼び出しは増やさない)、保存(`save()`)はエントリごとに逐次実行して`saveImportBatchAction`を1枚ずつ呼ぶ——「1回の撮影(受信)=1バッチ」の原則(`import_batches`1行=`receipt_image_path`1件)は崩さず、1エントリの失敗が他のエントリの保存を止めない設計にした。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(687件全通過)/`npx next build`(`/api/webhooks/line`のルート生成を確認)すべて成功。新規マイグレーションは無し(既存テーブルのみで完結)。**未検証**:このセッションにはLINEの資格情報(`LINE_CHANNEL_SECRET`・`LINE_CHANNEL_ACCESS_TOKEN`)が無く、実際のLINEからのWebhook配信・署名検証・画像取得は未確認(B-11として記録)。**追記(2026-09-14、PR #53レビュー中)**:本人から「カメラマーク押したときに写真を撮るか写真を選択するか選択できるようにして」と指摘。`accept="image/*" capture="environment"` を1つの `<input>` に両方持たせていたため、モバイルブラウザは選択肢を出さずカメラを直接開いてしまい、ギャラリーから選ぶ経路が実質無かった。カメラ起動専用(`capture="environment"` あり)とギャラリー選択専用(`capture` なし)の2つの `<input>` に分け、「撮る」「選ぶ」の2ボタンとして本人が明示的に選べるようにした(本人の指摘で絵文字は外し、テキストのみに統一)。`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(687件全通過、UIのみの変更のためテスト追加なし)/`npx next build` すべて成功。 | 2026-09-14 |
| P10-6 | 家計簿(/spending)の作り直し(本人発案「ちりつもだけ表示されてて微妙です。どちらかというと普通に一般的な家計簿みたいなものを表示し補助でちりつもの項目を作るべきです。ふつうに今月分使った一覧あとはソート順変更、グラフ表示、予測などの機能を追加して充実させてください」)。着手前の `/spending` はちりつも(小口支出の山)専用で、収支の全体像・カテゴリ別の内訳・今月の明細・予測が無かった。`/spending` を「今月使った額・収入・先月同日比」のサマリー、カテゴリ別内訳(グラフ)、今月の明細一覧(並び替え可能)、月末着地予測の4点セットへ作り直し、ちりつもは補助として `app/(app)/spending/pile/page.tsx`(旧 `/spending` をそのまま移設)へ格下げし、新 `/spending` からカード1枚の要約で誘導する形にした。**予測**:新しい判断ロジックは `domain/spending.ts` の `projectedMonthTotalYen()`(経過日数あたりのペースを月の総日数まで伸ばす。年換算の `annualizedPaceYen()` と同じ考え方を月内着地に転用)だけを追加し、他は既存の純粋関数(`summarizeMonthlyIncomeExpense`・`summarizeMonthlySpendByCategory`・`compareToPreviousMonthPace`・`budgetTone`)をそのまま再利用した。**カテゴリ別内訳**:`src/features/spending/store.ts`(新規)の `loadMonthlyLedger()` が当月のカテゴリ別支出(統廃合されたカテゴリは `resolveCategoryRoot()` で現在のカテゴリへ解決、category-detail-store.ts と同じ考え方)・当月予算(`budgets` テーブルの当月行、無ければ `default_monthly_budget_yen`)・カテゴリ予算合計を組み立てる。`app/(app)/spending/category-breakdown-chart.tsx`(新規)は予算のあるカテゴリは既存の `Meter`(予算に対する消化率、ホームの予算タイルと同じ色判断)、予算の無いカテゴリ(投資・返済など)は単純な大きさ比較のバーで描く。**今月の明細+ソート順変更**:`app/(app)/spending/transaction-list.tsx`(新規、Client Component)が新しい順/古い順/金額が大きい順/金額が小さい順のプルダウンで並べ替える。明細の編集自体は既存の `/transactions` が担うため、ここは表示専用にとどめた。**次/server境界**:`loadMonthlyLedger()` は `next/headers` 依存の `createClient()` を使うため、Client Component である `transaction-list.tsx` が直接 import しないよう `src/features/spending/ledger-types.ts`(新規)へ画面向けの型だけを分離した(T-7・P10-4 と同型の対策)。テスト4件追加(`projectedMonthTotalYen()` の0除算・四捨五入・境界値、`tests/domain/spending.test.ts`)。`store.ts` 自体は他の feature store と同じく実際の Supabase プロジェクトに対して手動で検証する既存方針に倣い、単体テストは追加していない。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(691件全通過)/`npx next build`(`/spending`・`/spending/pile` のルート生成を確認)すべて成功。**未検証**:このセッションには本番 Supabase 資格情報が無く、実データでの表示・並び替えの実機確認は行えない(型・テスト・ビルドでの確認に留まる)。 | 2026-09-14 |
| P10-7 | 画面デザインの標準方針を全画面へ適用(本人発案、8点の指示、ADR-026)。**遷移即表示・fetch中ローディング**:既存の `loading.tsx`(Next.js App Router の規約、`page.tsx` を自動的に `<Suspense>` で包む仕組み)が5画面(`/`・`/accounts`・`/debts`・`/investments`・`/payday`)には既にあったため、同じパターンを残り13画面(`/advisor`・`/briefs`・`/briefs/[id]`・`/job-change`・`/reports`・`/rules`・`/settings/gmail`・`/settings/rescued-emails`・`/side-hustle`・`/spending`・`/transactions`・`/transactions/duplicates`・`/transactions/review`)へ展開した(`src/components/ui/skeleton.tsx` の `Skeleton` を共有)。タイトルの `<h1>` をスケルトンより先に置くことで「タイトルは遷移後すぐ表示」も同時に満たす(`/briefs/[id]` はタイトル自体がデータ(配信日)のため、その位置にも骨格ブロックを置いた)。静的画面(`/login`・`/settings/{google,password}`・`/transactions/{import,paste,receipt,reconcile}`・`/rules/chat`)はサーバー側の都度フェッチを持たないため対象外。**アニメーション**:全25画面のうち `.rise`(既存)が付いていなかった `/reports`・`/login` の2画面にだけ追加(残り23画面は既に付いていた)。**説明文・導線の整理**:実際に全画面を読んだところ、直近のセッション(P9-4・P10-1・P10-4)で既に「見出しの文字リンクが多い」「説明文が長い」という同種の指摘を受けて大半の画面が整理済みだったと判明(`/transactions` のヘッダーコメント参照)。方針から外れていたのはホーム末尾の6件の文字リンク列(朝配信・副業・転職準備・支出レポート・AI相談・Google連携)だけで、副業・転職準備・Google連携は「その他」メニュー(P10-1)から既に辿れるため重複させず削除し、今見ている数字(完済・予算タイル)と関係の深い朝配信・支出レポート・AI相談だけをボタンとして関連カードのすぐ下に残した(機能は一切削っていない、露出面をその他メニューへ一本化しただけ)。本番接続後も残っていた古い注記(`数値は仮置き(ADR-006)。Supabase 接続後に実データへ切り替わります。`)も削除。`/transactions/review` の文中リンク(分類ルール・ルール相談)も、確認キューのすぐ下の小さなボタン列に変えた。**なぜ全画面を書き直さなかったか**:既に整理済みの画面にまで手を入れると差分だけが増えるため、実際に方針から外れていた箇所(loading.tsx 未導入の13画面・rise 未適用の2画面・ホームの文字リンク列・確認待ちの文中リンク)だけを直す設計にした(詳細な判断根拠はADR-026)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(691件全通過)/`npx next build`(新規ルートの loading.tsx が正しく認識されることを確認)すべて成功。新規マイグレーション不要。**追記(2026-09-14、同日中)**:main へ合流させたところ、別セッションが並行して `/spending` を「家計簿」へ作り直し、旧ちりつも画面を `/spending/pile`(P10-6)へ移設していたため、`/spending/loading.tsx` を新タイトルへ直し、新設の `/spending/pile` にも `loading.tsx` を追加(対象は14画面)。**未検証**:実機・実ブラウザでの遷移体感(loading.tsx の表示時間、アニメーションの見え方)はこのセッションの手段では確認できない。 | 2026-09-14 |
| P10-8 | Material Design 3 の本格導入(本人発案「Google material designを取り入れて先進的なデザインにして欲しい」、ADR-027)。事前確認の上、①本格導入(Material 3/Material You相当)、②お金の役割色(青=返済・赤=超過・緑=収入)は維持、の2点を決めた。**トークン基盤**:`app/globals.css` に Material 3 のロール名(`--md-primary`等)・elevation(0〜5、dark は面を明るくする方式)・shape scale(4/8/12/16/28px+full)・motion(standard/emphasized easing)・タイポグラフィスケール(`.md-*` クラス)を追加。既存の役割色トークンは一切変更せず、その上に「言い換え」を重ねただけ(唯一の真実源は維持)。**書体**:ADR-017 の日本語書体(M PLUS 2)は維持しつつ、英数字を Inter から Roboto Flex(Material の標準書体)に差し替え。**新規コンポーネント**(`src/components/ui/`):`ripple.tsx`(`useRipple()` フック、タップ位置から広がるMaterial名物のリップル)、`button.tsx`(Filled/Tonal/Outlined/Text/Elevatedの5variant)、`fab.tsx`(Floating Action Button)、`chip.tsx`。**既存コンポーネント改修**:`Card`(elevated/filled/outlinedの3variant)、`StatTile`をshape-lg/elevation-1へ。`app/(app)/layout.tsx` のボトムナビを Material 3 Navigation Bar 仕様(アクティブ項目の背後にpill型インジケータ、ラベル常時表示)に、レシート撮影ボタンを正式なFabコンポーネントに置き換え。`more-menu.tsx` のシートもshape-xl/elevation-3/emphasized-decelerate easingに寄せた。**適用範囲**:ホーム(朝配信・支出レポート・AI相談ボタン)、レシート撮影(撮る・選ぶ)、明細一覧の空状態(CSV取り込む・レシートを撮る)、確認待ち(分類ルール・AIにルール相談)など高頻度画面のボタンを新コンポーネントへ置き換え。全フォーム(input/select)のMaterial Text Field化は範囲外とし、次に触れた画面から順次移行する方針(T-13と同じ)。**検証中に発見・修正**:ナビの pill インジケータに `px-4` を使うと400px幅で6項目のラベルが折り返す不具合を、Playwrightで実際のCSSトークンを使った静的モックアップをスクリーンショットして発見し、`px-2`+`whitespace-nowrap` に直した。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(691件全通過)/`npx next build`すべて成功。ログイン画面(唯一ログイン無しで到達できる画面)をPlaywrightで実際にスクリーンショットしRoboto Flexの描画を確認、Material コンポーネント群は実際のglobals.cssトークンを使った静的モックアップで light/dark 両方の見た目を確認した。**未検証**:このセッションには本人の実アカウントでログインする手段が無く、認証後の画面(ホーム・明細等)での実際の見た目・リップルの触感はこのセッションの手段では確認できない。 | 2026-09-20 |
| P10-9 | P10-8(Material Design 3)への本人フィードバック「アイコンダサい 絵文字にしてほしくなくてもっと無料のライブラリでアイコン使って」に対応。絵文字ベースのアイコン(📷🔥⌄↑↓✓▲▼)がMaterial 3の見た目と噛み合わず古臭く見えるとの指摘。`react-icons`(v5.7.0、MITライセンス、アイコン単位のtree-shaking)を導入——ADR-027で採用したMaterial Design 3と一貫させるため、Googleの Material Icons を含む `react-icons/md` サブパッケージを使用。React で描画されるUI上のアイコンのみを対象に置き換えた:①`app/(app)/layout.tsx` のレシート撮影FAB(📷→`MdCameraAlt`)、②`app/(app)/page.tsx` の連続確認ストリークバッジ(🔥→`MdLocalFireDepartment`)、③`src/components/ui/stat-tile.tsx` の内訳展開シェブロン(⌄→`MdExpandMore`、回転アニメーションは維持)、④`app/(app)/rules/rule-row.tsx`・`app/(app)/payday/rule-row.tsx` の優先度入れ替え矢印(↑↓→`MdKeyboardArrowUp`/`MdKeyboardArrowDown`)、⑤`app/(app)/payday/checklist.tsx` のチェックリスト完了マーク(✓→`MdCheck`)、⑥`app/(app)/debts/payment-history.tsx` の返済実績の開閉インジケータ(▲▼→`MdKeyboardArrowUp`/`MdKeyboardArrowDown`)。**対象外とした判断**:`src/features/alerts/notify.ts`(🔵🟠🔴)・`src/features/briefs/notify.ts`(📋)・`src/features/import/line-receipt.ts`(⚠)の絵文字はDiscord埋め込み・LINEプッシュメッセージの本文に直接埋め込むプレーンテキストであり、外部サービスへ送る文字列はReactコンポーネントに置き換えようがないため対象外とした。全画面に散在する「→」の道案内矢印(遷移リンクの末尾、15箇所以上)も、本人の指摘した「アイコン」(絵文字的な記号)とは性質が異なるタイポグラフィ上の慣用表現と判断し、対象範囲の拡大を避けるため据え置いた。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(691件全通過、UIのみの変更のためテスト追加なし)/`npx next build` すべて成功。**未検証**:このセッションには本人の実アカウントでログインする手段が無く、認証後の画面での実際のアイコン表示はこのセッションの手段では確認できない。 | 2026-09-20 |
| P10-10 | Material Design 3(P10-8/ADR-027)から Apple の Liquid Glass 風デザインへ全面移行(本人が別アプリのスクリーンショットを提示し「下の選択部分が水っぽい・透明でいい感じ」「アニメーションも水みたいでiPhoneのOSっぽいのにしたい」と要望、ADR-028)。事前確認の上、①適用範囲はボトムナビだけでなく「アプリ全体の主要な操作系」まで広げる、②Material 3のトークンは維持せずApple寄りへ全面移行する、の2点を決めた。**トークン層の全面置き換え**(`app/globals.css`):Material 3のロール名・elevation(0〜5)・shape scale・motion・state layerをすべて削除し、Apple風の新トークン(`--radius-sm/md/lg/xl/full`、`--ease-standard`/`--ease-spring`(バネの弾みをcubic-bezierのオーバーシュートで表現)/`--ease-sheet`、`--glass-tint`/`--glass-border`/`--glass-blur`等のガラス素材)に置き換え。既存の役割色(`--accent`/`--income`/`--over`)は一切変更していない——Material のロール名という間接層を撤去し、各コンポーネントが役割色トークンを直接参照する形に戻しただけ。**ガラス素材はナビゲーションchromeだけに限定**:Apple自身のアプリに倣い、ボトムナビ・その他メニューのシート・Buttonのelevated variantの3箇所だけを半透明+backdrop-filterのガラス素材にし、Card・StatTile・Chip・その他のボタンvariantは不透明な塗り+Apple的な角丸(20px)のみとした(全部を透過にすると本文が読みにくくなるため、詳細はADR-028)。**タッチフィードバックの刷新**:Materialのリップル(`ripple.tsx`・`useRipple`)を完全に撤去し、Appleの「軽く縮んでバネで戻る」スケールアニメーション(`active:scale-*` + `--ease-spring`)に置き換えた。**ボトムナビ**:`<ul>`をガラス素材(`blur(24px) saturate(180%)`)にし、アクティブタブのピルは`scale(0.9)→scale(1)`を`--ease-spring`で遷移させて弾む見た目にした(本人提示のスクリーンショットの帯の質感に直接寄せた)。**書体**:Roboto FlexからInterへ差し戻し(`app/layout.tsx`)——Appleの標準書体(San Francisco)はnext/font/googleで配布されておらず、字幅の近いInter(ADR-017で採用実績あり)を使う。日本語(M PLUS 2)は変更なし。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(691件全通過)/`npx next build`すべて成功。実際のglobals.cssトークンを読み込む静的モックアップをPlaywrightでlight/dark両方スクリーンショットし、ボトムナビの半透明+ぼかし・Buttonのelevated variant・Chip・Cardの見た目を確認した。**未検証**:このセッションには本人の実アカウントでログインする手段が無く、認証後の画面での実際のバネのアニメーション(タブ切り替え時の弾み・ボタン押下時の縮み)の体感は確認できない。 | 2026-09-20 |
| P10-11 | ボトムナビのカメラ FAB を押した瞬間にカメラアプリが開くようにする(本人発案「カメラマーク押した時にすぐカメラアプリになるようにして」)。従来は FAB が `/transactions/receipt` への `<Link>` で、遷移後にその画面の「撮る」ボタンをもう1回押す必要があった。`src/components/ui/fab.tsx` を `href`(通常のリンク)と `onFiles`(カメラ起動用の `<input type="file" capture="environment">` を持つ `<label>`)の2モードを持つ discriminated union に拡張し、`app/(app)/layout.tsx` の FAB を `onFiles` モードに変更(見た目・押下フィードバックは共通の `SHARED_STYLE` で完全に同じ)。撮影・選択されたファイルは `/transactions/receipt` の画面ローカル state(`useState`)でしか処理できないため、`src/features/import/pending-receipt-files.ts`(新規)というモジュール変数の一時置き場を作り、FABの`onFiles`コールバックがそこにファイルを置いてから`router.push('/transactions/receipt')`で遷移し、遷移先の`useEffect`(マウント時に1度だけ)が`takePendingReceiptFiles()`で取り出して既存の`onFiles()`(抽出・分類プレビュー)にそのまま渡す。**react-hooks/set-state-in-effect対応**:`onFiles()`は内部で同期的にsetStateを呼ぶため、effect内で直接呼ぶと「effect内での直接setState」としてlintエラーになる(M2-3b等と同じ理由)。`fetchAccounts()`/`fetchLearnedRules()`と同じく、Promiseのコールバック内で呼ぶ形(`Promise.resolve().then(() => onFiles(pending))`)に変えて回避した。`/transactions/receipt`への直接遷移(`/transactions`の空状態ボタン等)は従来どおり「撮る」/「選ぶ」の2ボタンを提示する画面のまま変更していない——pendingファイルが無ければ`takePendingReceiptFiles()`はnullを返すだけで何も起きない。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(691件全通過、UIとファイル橋渡しのみの変更のためテスト追加なし)/`npx next build`すべて成功。**未検証**:このセッションには実機のカメラ・本人の実アカウントでログインする手段が無く、実際にカメラアプリが開くこと自体はこのセッションの手段では確認できない(`capture="environment"`属性はモバイルSafari/Chromeでカメラを直接開かせる標準的な実装で、ADR-021の「撮る」ボタン(既存の同じ属性を使う実装)と技術的には同一)。 | 2026-09-20 |
| P10-12 | 一度読み込んだ画面はそのまま保持し、pull-to-refresh でだけ最新化する(本人が別アプリのスクリーンショット2枚を提示し「読み込み中は画面全体に対してローディングを表示すること。一回読み込んだページはそのまま保存しておき、最新の情報を読み込みたい時は下側に引っ張ってローディング出して」と要望、ADR-029、ADR-001を一部上書き)。事前確認の上、①適用範囲はホームを含む全画面、②ホームの完済カウントダウン・予算タイルが古いまま気づかれないリスクは最終更新時刻の表示で対処、の2点を決めた。**クライアント側キャッシュを伸ばす**:`next.config.ts`に`experimental.staleTimes.dynamic`(既定0秒)を24時間に設定——Next.jsのClient Router Cacheの設定で、サーバー側の`force-dynamic`(ADR-001)やDBクエリには一切影響しない、純粋にブラウザが前回の内容をどれだけ使い回すかだけの設定。これにより画面へ戻るたび(タブ切り替え・戻るボタン)に毎回`loading.tsx`のスケルトンを出して作り直す動き(ADR-026)がなくなり、前回の内容がそのまま表示され続ける。**pull-to-refresh**:`src/components/ui/pull-to-refresh.tsx`(新規)を`app/(app)/layout.tsx`の`<main>`に1箇所だけ差し込み、全画面に効かせる。Reactの合成イベントはtouchmoveをpassive登録するため`preventDefault()`が効かず、ネイティブの`addEventListener('touchmove', ..., { passive: false })`でタッチを追跡。画面最上部でだけ下方向のジェスチャーを検知し、ゴムのような抵抗をかけながらLiquid Glass素材(ADR-028と統一)の円形インジケータを追従させ、閾値(64px)を超えて離すと`useTransition`の`startTransition`で包んだ`router.refresh()`を呼ぶ——素の`router.refresh()`を直接呼ぶとReactが表示中の内容を隠して`loading.tsx`のフォールバックを出してしまうため、Transitionで包み`isPending`で自前のスピナー表示を制御する。**ホームの最終更新時刻**:`src/lib/date.ts`に`formatTimeJa()`(JST、'13:40'形式)を追加し、`app/(app)/page.tsx`のServer Componentが実際に実行された時刻を「完済まで」の下に表示——画面を保持する以上本人が古い数字を見ている可能性が生まれるため、ADR-001の「常に最新」という保証をこの表示で代替する(古いままなら本人が見て気づける)。`export const dynamic = 'force-dynamic'`はホームに残したまま(サーバー側は今回も必ず最新値を計算する)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(691件全通過)/`npx next build`(`staleTimes` experimentが有効になっていることをビルドログで確認)すべて成功。pull-to-refreshのガラス素材インジケータは実際のglobals.cssトークンを読み込む静的モックアップをPlaywrightでlight/dark両方スクリーンショットして見た目を確認した。**未検証**:このセッションには本人の実アカウントでログインする手段が無く、①実機でのタッチジェスチャー(引っ張りの抵抗感・離したときの挙動)、②staleTimesにより実際にrevisit時のloading.tsxが抑制されること、③pull-to-refresh後に本当に最新のDB値へ差し替わること、はこのセッションの手段では確認できない。 | 2026-09-20 |
| P10-13 | ボトムナビの主タブを4つまでに絞る(本人発案「悪くないんだけどメニューが少し大きくてタップしにくい pairsみたいにめにゅー4つまで」)。以前はホーム/家計簿/明細/負債/給料日の5タブ+「その他」で計6項目が1本のピルに詰まっており、1項目あたりの幅が狭くタップしにくかった。事前に確認し、主タブに残す2つ(残りはホーム固定+その他へ)を本人に選んでもらい、①家計簿、②明細、③給料日を残すと決定(負債は「その他」メニューへ移動)。`app/(app)/layout.tsx`のNAV配列を5件→4件(ホーム/家計簿/明細/給料日)に縮小。**「その他」をタブから独立した丸ボタンへ**:タブを4つに絞るだけでは「その他」を入れると結局5項目になるため、`src/components/ui/more-menu.tsx`のトリガーをピルの6項目目(`<li>`+テキストラベル)から、ピル本体の隣に置く独立した丸いガラス素材ボタン(`MdMoreHoriz`アイコン、44px)に作り直した——タブ数を増やさずに押しやすい大きさを両方確保できる。`app/(app)/layout.tsx`はナビの`<ul>`と「その他」ボタンを横並びの`<div className="flex ... gap-2">`で包む形に変更。**負債の移動先**:`more-menu.tsx`のGROUPS「記録する」グループの先頭に`{ href: '/debts', label: '負債' }`を追加(口座と並ぶ「管理する対象」として)。ホームの完済カウントダウンから負債の概要は確認できるため、詳細操作(追加・編集・借り換えシミュレーション)への導線は「その他」経由で十分と判断。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(691件全通過、UIのみの変更のためテスト追加なし)/`npx next build`すべて成功。実際のglobals.cssトークンを読み込む静的モックアップをPlaywrightでlight/dark両方スクリーンショットし、4タブ+独立した丸ボタンの見た目・間隔を確認した。**未検証**:このセッションには本人の実アカウントでログインする手段が無く、認証後の画面での実際のタップしやすさの体感はこのセッションの手段では確認できない。 | 2026-09-20 |
| P10-14 | P10-12(ADR-029)への本人からの不具合報告「読み込み中に画面全体にローディング表示されない」に対応。確認したところ初めて開く画面でも発生するとのことで、ADR-029の第一要件(初回は必ず全画面ローディングを出す)が満たせていなかった。**原因**:ボトムナビ(`app/(app)/layout.tsx`)は5画面すべてで常にマウントされ続け、リンクが常にビューポート内にあるため、Next.jsの自動プリフェッチ(`<Link>`がビューポートに入ると自動で発火)がほぼ即座に効く。ADR-029で`next.config.ts`の`staleTimes.dynamic`を24時間に伸ばした結果、この自動プリフェッチが「その場限りのローディング境界(シェル)だけ」ではなく実際のページ内容まで先読みして温めるようになり、本人が実際にタップする頃には内容が揃っていて`loading.tsx`の表示ごとスキップされていた(Next.js公式ドキュメント`node_modules/next/dist/docs/01-app/01-getting-started/04-linking-and-navigating.md`の「Client-side transitions」の節にある「新しいページを prefetch されたローディング状態に置き換えるか、内容がすでに利用可能ならそのまま新しいページに置き換える」という記述と整合)。**対処**:ボトムナビの`<Link>`(`app/(app)/layout.tsx`)と「その他」メニューの各項目`<Link>`(`src/components/ui/more-menu.tsx`)に`prefetch={false}`を追加し、自動プリフェッチを止めた——タップした瞬間に初めてサーバーへ取りに行く(=毎回`loading.tsx`が挟まる)ようにした。一度実際に開いた画面がキャッシュされたまま保持される挙動(ADR-029の本題、staleTimes側の設定)自体には影響しない——prefetchを止めても、実際にnavigateした後のRouter Cache保持は変わらない。**対象範囲**:常に画面上に存在するボトムナビ・その他メニューのみを対象とした。ページ内のCTAボタン(Buttonコンポーネントのhrefモード等)はその画面を開いている間しかビューポートに入らないため同じ問題の再現性が低いと判断し、今回は対象外(引き続き問題が出るようであれば追加対応)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(691件全通過)/`npx next build`すべて成功。**未検証**:このセッションには本人の実アカウントでログインする手段が無く、実際にloading.tsxが毎回表示されるようになったかはこのセッションの手段では確認できない。 | 2026-09-20 |
| P10-15 | P10-14の`prefetch={false}`では直らず、本人から「いやーローディングアイコンでない」と再報告。憶測での再修正を避け、まず現象を確認(「タブを押したあと画面の中身自体はどうなるか」)したところ「新しい画面に切り替わる(ローディングの表示のみない)」と判明——画面は正しく遷移しており、固まっているわけではない。**実機検証で原因を特定**:このセッション内に実プロジェクトのnode_modules・next.config.tsをそのまま使う最小構成のNext.js環境を作り(`proxy.ts`の認証を一時的にバイパスする使い捨てルート、検証後に完全に削除・復元)、Playwrightで検証。結果、①prefetch有無どちらでも初回ナビゲーションは`loading.tsx`が正しく表示されてから本体に切り替わる(1.5秒の人工遅延ダミーページで確認)、②`pull-to-refresh.tsx`と同じ`useTransition`を使うクライアントラッパーを重ねても挙動は変わらない、③revisitはstaleTimesの設計どおり`loading.tsx`を挟まず即表示——という、Next.jsの仕組み自体は壊れていないという結論に至った(P10-14の「自動プリフェッチが実際のページ内容まで先読みしている」という仮説は誤りだったと判明)。**真因**:本番(Vercel+Supabase)の実際の往復が数十〜百数十msと速く、`loading.tsx`のスケルトンが描画されてから本体に置き換わるまでが人の目には一瞬すぎて「出ていない」ように見えていた(「新しい画面には切り替わる」という本人の報告と整合)。**対応**:`src/lib/min-loading-duration.ts`(新規)に`withMinDuration(promise, minMs=400)`を追加——実際のデータ取得が何msで終わっても、呼び出し全体が最低400msはかかったことにする(先に完了していれば残り時間だけ`setTimeout`で待つ)。`loading.tsx`を持つ全19画面(`app/(app)/page.tsx`・`accounts`・`advisor`・`briefs`・`briefs/[id]`・`debts`・`investments`・`job-change`・`payday`・`reports`・`rules`・`settings/gmail`・`settings/rescued-emails`・`side-hustle`・`spending`・`spending/pile`・`transactions`・`transactions/duplicates`・`transactions/review`)の、そのページの表示を待つ最初の`await`/`Promise.all`をこの関数で包んだ。revisit(pull-to-refreshでキャッシュから即表示される経路)はpage.tsx自体が再実行されないため、この遅延の影響を受けない——ADR-029の「一度読み込んだ画面はそのまま保持」という本題は変えていない。**検証**:上記の実機的な再現検証に加え、`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(691件全通過)/`npx next build`すべて成功。**未検証**:本番Vercel+Supabaseの実際の往復時間・本人の体感はこのセッションの手段では確認できない(400msという値は経験則で選んだものであり実測値ではない)。 | 2026-09-20 |
| P10-16 | P10-15の`withMinDuration`をマージした後も本人から「見えないわ」と再報告(ただし「でももういいかな」と一旦は引き下がりつつ「マウントに時間がかかっているのに、なぜその間loadingが出ないのか」という技術的な疑問を提示)。これに対する自分の最初の説明(「loading.tsxのコードがpage.tsxより後に届くのでは」)を本人から「そもそもマウントしてないものにloadingを付けても意味がない(同じルートセグメントとしてまとめて配信されるはずで、片方だけ遅れる理由がない)」と筋の通らなさを指摘され、誤りと認めて撤回。決め手になったのは本人の具体的な観察「タイトル(例:『明細』)のみ先に表示され、そのコンテンツは表示されないけどloadingもされていない」——これは表示タイミングではなく、**loading.tsx自体は描画されているが見た目上何も無いように見えている**ことを示す手がかりだった。**真因**:全`loading.tsx`が共通利用する`src/components/ui/skeleton.tsx`の背景が`var(--plane)`固定で、`app/globals.css`の`body { background: var(--plane); }`と完全に同色だった。`loading.tsx`は各画面の`<main>`(=bodyと同じ`--plane`背景)に直接置かれるため、骨格の矩形が背景と完全に同化して見えなくなっていた(`animate-pulse`の不透明度アニメーションも同色同士では見た目上変化しない)。ADR-029のpull-to-refresh・staleTimes・P10-15の`withMinDuration`はいずれも「いつ表示するか」を正しく制御できていたが、表示対象自体が常に透明に等しかったため「何も出ない」ように見え続けていた。**対応**:`skeleton.tsx`の背景を`color-mix(in srgb, var(--ink) 10%, transparent)`(`--ink`を10%重ねた透過ティント)に変更——`--plane`(ページ本体)の上でも`--surface`系(カード内)の上でも、light/dark両テーマで周囲と必ず区別できる。**検証**:実際の`globals.css`を読み込む静的HTMLモックアップ(旧`--plane`背景/新`color-mix`ティントを並べたもの)をPlaywrightでlight/dark両方スクリーンショットし、旧版は両テーマで背景に完全同化して見えないこと・新版は両テーマで明確に視認できることを確認。`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(691件全通過)/`npx next build`すべて成功。**未検証**:本番での実機確認(実アカウントでログインする手段がこのセッションに無い)。ただし本件は往復時間やブラウザ差に依存しない決定的なCSSの色衝突であり、`--plane`/`--ink`の値が変わらない限り全環境で再現するため、静的検証で十分に説明できる不具合だった。 | 2026-09-20 |
| P10-17 | 本人からの不具合報告「カテゴリと分類ですが編集できるけど削除できない」に対応。調査の結果、分類ルール(`/rules` の「分類ルール」セクション)は元々 `deleteRuleAction`/`deleteClassificationRule()` で削除でき、`classification_rules.category_id` の外部キーが `on delete cascade`(docs/schema.sql §3.9)なのでルール削除自体に不具合は無かった。実際に削除の手段が無かったのは**カテゴリ**側で、`category-row.tsx` には「編集」「統合」しか無く、統廃合(`mergeCategory()`、docs/schema.sql §3.3 の元々の方針:安全のため削除ではなく `merged_into_id` への付け替えを推奨)だけが用意されていた——使われたことのあるカテゴリを安全に片付ける手段としては正しいが、間違って作った・結局使わなかったカテゴリまで一覧に残り続ける不便さは別問題だった。`src/features/categories/store.ts` に `deleteCategory()`(新規)を追加:対象が `is_system` なら拒否(統合と同じ抑止)、そのうえで `transactions`/`transaction_splits`/`classification_rules` がこのカテゴリを参照していないか、他のカテゴリがこのカテゴリへ統合済み(`merged_into_id` がこの id)でないかを確認し、1件でも参照があれば削除させず統合を促すメッセージを返す。参照が無ければ実際に `delete` する。**なぜ参照チェックが要るか**:`transactions.category_id` は `on delete set null`、`classification_rules.category_id` は `on delete cascade` のため、チェック無しで削除を許すと本人の知らないうちに明細が「未分類」化したり、大事に育てたルールが黙って消えたりする——「壊れた状態を防ぐ」のではなく「そもそも壊れる操作を実行させない」設計にした。`app/(app)/rules/actions.ts` に `deleteCategoryAction(id)`(新規、`deleteRuleAction` と同じくフォーム不要の1タップ操作として `useActionState` を使わない薄いラッパー)を追加。`category-row.tsx` に確認ステップ付きの「削除」ボタンを追加(`is_system` 以外は常に表示——統合済み・無効化されたカテゴリも対象。使用中なら `deleteCategory()` がエラーメッセージを返すのでボタン自体は出しっぱなしにできる)。テストは追加していない(`deleteCategory()` は他の categories/classification ストア関数と同じく DB 呼び出しのみで純粋関数を含まないため、既存方針に倣い実際の Supabase プロジェクトに対して手動で検証する対象とした)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(691件全通過)/`npx next build` すべて成功。**未検証**:このセッションには本番 Supabase 資格情報が無く、実データに対する削除・参照チェックの実機確認は行えない(型・lint・ビルドでの確認に留まる)。 | 2026-09-21 |
| P10-18 | 本人からの不具合報告「レシート読み取り機能バグっています。今画像渡してなぜかなんの反応もありません」に対応。**調査**:`/api/import/receipt/route.ts` は画像が無い・形式非対応・`ANTHROPIC_API_KEY` 未設定・呼び出し過多のどのケースでも 200 で warnings 付きの JSON を返し、クライアント側 `extractAll()` も fetch 失敗時に画面へ警告文を出す作りなので、AI 呼び出し自体が失敗していれば必ず何かしらの表示が出るはずだった。「何も起きない」という報告は、そもそも画像がこの画面の state に届いていないことを示唆しており、ボトムナビのカメラ FAB(`app/(app)/layout.tsx`)からの経路を疑って`src/features/import/pending-receipt-files.ts` を確認。**真因**:ADR-029(`next.config.ts` の `staleTimes.dynamic` を24時間に延長)により、一度開いた画面は Router Cache に残ったまま再利用される。`/transactions/receipt` は「マウント時に1度だけ `takePendingReceiptFiles()` を呼ぶ」実装だったため、一度この画面を開いた後にカメラ FAB から2回目の撮影をしても、遷移先はマウントし直されず(同じコンポーネントインスタンスが再利用される)、マウント時1回きりの effect は再実行されない——`setPendingReceiptFiles()` でファイルは置かれても、誰も取りに来ないまま放置されていた。P10-15/P10-16 で見つかった `staleTimes` 起因の「戻ってきても更新されない」系統の不具合と同根。**対応**:`pending-receipt-files.ts` に購読(pub-sub)の口を追加(`subscribePendingReceiptFiles()`、新規)。`setPendingReceiptFiles()` が呼ばれるたびに購読者へ通知するようにし、`/transactions/receipt` 側は「マウント時点で既に置かれている分」に加えて「マウントされたまま以後に届く分」もこの購読で拾うよう修正(`useEffect` の中で `subscribePendingReceiptFiles(consume)` を購読し、クリーンアップで解除)。マウント時点の初回チェックは従来どおり `Promise.resolve().then()` 経由でマイクロタスクへ逃がす(react-hooks/set-state-in-effect 対応、fetchAccounts 等と同じ理由)。購読コールバック自体は effect の外(FAB のファイル選択という別のユーザー操作)から呼ばれるため、直接 `onFiles` を呼んでも同ルールには抵触しない。テスト6件追加(`tests/features/import/pending-receipt-files.test.ts`、新規):set/take の基本動作に加え、購読が `setPendingReceiptFiles()` のたびに通知されること・マウントされたままでも2回目以降のファイルを拾えること・解除後は通知されないことを検証。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(697件全通過)/`npx next build` すべて成功。**未検証**:このセッションには実機・実ブラウザでカメラ FAB を連続して使う手段が無く、実際の Router Cache 再利用下での修正後の挙動(2回目の撮影が正しく画面に反映されること)は実機確認できていない(購読の発火・解除ロジック自体はユニットテストで保証)。**追記(2026-09-21、本人からの訂正)**:「正確には AI に読み込ませるボタンまで押下できます。ただその分析結果が出ない」と訂正を受け、上記の FAB の不具合とは別に、押下後「読み取っています…」の表示が消えたあと何の表示も出ない(明細一覧も警告文も出ない)経路を再調査。`app/(app)/transactions/receipt/page.tsx` の `entryPreviews` が `if (!accountId) return [];` で口座未選択の間は**エントリ全体を丸ごと空にして返していた**——読み取り結果(`extracted.warnings`)は口座の有無と無関係のデータなのに、口座が決まるまでの間はそれも一緒に握りつぶされ、「AI に読み取らせた→スピナーが消えた→本当に何も表示されない」状態になっていた(`buildFromAiRows()`/`route.ts` はどの失敗経路でも必ず warnings を1件は返す設計だったため、この抑制さえなければ何かしらの文言は必ず出ていたはず)。口座取得(`fetchAccounts()`)は取り込みの初期化と並行して非同期に走るため、AI 抽出(Claude 呼び出し)の方が先に終わるタイミングでは常にこの状態になりうる。**対応**:`entryPreviews` の早期 return をやめ、口座が未選択でも各エントリ自体は返す(`rulePreview`/`itemRulePreviewByIndex` は空のまま)ようにし、`extracted.warnings` の表示を口座選択と切り離した。加えて、読み取り自体は成功(`transactions.length > 0`)しているのに口座未選択で明細を組み立てられていない場合は「N件読み取れました。上の「口座」を選ぶと明細が表示されます」という案内文を追加し、原因不明のまま放置されないようにした。テストは追加していない(このコンポーネントは React component テストの対象外という既存方針——tests/ 配下に `.tsx` のコンポーネントテストが無く、UI ロジックはビルド・型・lint・手動確認で担保する既存の慣習に倣った)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(697件全通過)/`npx next build` すべて成功。**未検証**:実際に口座取得より抽出が先に終わる競合状態そのものは、このセッションでは実機のネットワーク条件を再現できず確認できていない。ただし「口座未選択なら常に何も出ない」という抑制自体はコードから確定的に追えるため、口座が0件登録・未選択の状態で試した場合も含め、今回の修正で解消される。**追記2(2026-09-21、本人からの指摘)**:「口座って何のために追加するん？これいらなくない？普通に家計簿に登録して欲しいだけなんだけど」——上記の修正で警告文・案内文は出るようになったが、本人がそもそも口座を1件も登録していないため、レシートを撮るたびに「上の「口座」を選ぶと明細が表示されます」→ `/accounts` で先に口座を作る、という手順を要求される状態は解消できていなかった。設計原則2(記録の手間を最小化)違反であり、現金・電子マネー払いの主経路であるレシート取り込みの入り口でその手間を強いるのは本末転倒だった。**対応**:`src/features/accounts/store.ts` に `getOrCreateDefaultAccount()`(新規)を追加。口座が1件でもあれば `kind='cash'` を優先(無ければ `'e_money'`、それも無ければ先頭の口座)を返し、1件も無ければ名前「現金」・`kind='cash'` の口座を自動作成する(`account_kind` に元から `'cash'`/`'e_money'` があり、この用途は設計時から想定されていた)。`app/(app)/accounts/actions.ts` に Server Action `ensureDefaultAccountAction()`(新規)を追加し、`/transactions/receipt` の口座取得を「`fetchAccounts()` が空配列を返したら `ensureDefaultAccountAction()` を呼ぶ」形に差し替え。あわせて、口座が実質1件しかない(自動作成された「現金」、または本人が普段使う唯一の口座)場合は「口座」カード自体を表示しない(選ぶ意味の無い1択を毎回見せない)ようにし、2件以上あるときだけ選択肢として出す。これにより、口座を1つも登録していない本人でもレシート撮影→AI読み取り→保存までが `/accounts` に一切触れずに完結するようになった(口座という概念自体は `transactions.account_id` が NOT NULL のため schema からは無くせないが、本人が意識する必要はなくなる)。テストは追加していない(`getOrCreateDefaultAccount()` は他の accounts/categories ストア関数と同じく DB 呼び出しのみで純粋関数を含まないため、既存方針に倣い実際の Supabase プロジェクトに対して手動で検証する対象とした)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(697件全通過)/`npx next build` すべて成功。**未検証**:このセッションには本番 Supabase 資格情報が無く、実際に口座0件の状態から「現金」口座が自動作成されレシートが保存できることの実機確認は行えない(型・lint・ビルドでの確認に留まる)。 | 2026-09-21 |
| P10-19 | 本人からの不具合報告「明細の編集で保存ボタンが押下できない」に対応。**調査**:`/transactions` の明細一覧(`app/(app)/transactions/split-editor.tsx` の `TransactionRowWithSplit`)は、行をタップすると開く編集UIが P8-2(複数カテゴリ分割、#41)で丸ごと分割フォームに差し替えられていた。分割フォームの保存条件 `canSave` は「2行以上・全行の金額が0より大きい・合計が明細額と一致」を要求するため、単に1つのカテゴリへ直したいだけの操作(分割ではない、本来最も多いはずの編集)ではこの条件を満たしようが無く、保存ボタンが常に押せなかった。**さらに遡って確認**:P8-2 以前は明細一覧に使われていたのは表示専用の `components/ui/transaction-row.tsx` で、そもそも編集自体ができなかった——つまり「1つのカテゴリだけを直す」という単純な編集経路は、確認待ちキュー(`/transactions/review`)以外では一度も存在したことが無かった(`updateTransactionAction()`/`updateTransaction()` 自体は既存で、review-queue.tsx だけがこれまで使っていた)。**対応**:`TransactionRowWithSplit` に `mode: 'simple' | 'split'` を追加。行を開くと、既に分割済み(`initialSplits.length > 0`)の明細だけ分割フォームから開き、それ以外(大半の明細)は単一カテゴリの `<select>` + 保存ボタンから開くようにした(`saveCategory()`、`updateTransactionAction()` を呼ぶ、review-queue.tsx と同じ経路)。「カテゴリを分ける」ボタンで明示的に分割フォームへ切り替えられる。分割フォーム側にも「やめる」(未保存の分割ならモードを戻すだけ)・「分割を解除」(保存済みの分割は `replaceSplitsAction(id, [])` で解除してから単一カテゴリのフォームへ戻す)を用意し、単純な編集⇄分割を行き来できるようにした。テストは追加していない(このコンポーネントは他の React component と同じくテスト対象外という既存方針——tests/ 配下に `.tsx` のコンポーネントテストが無く、UI ロジックはビルド・型・lint・手動確認で担保する既存の慣習に倣った)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(697件全通過)/`npx next build` すべて成功。**未検証**:このセッションには本番 Supabase 資格情報が無く、実データでの保存・分割⇄単一カテゴリの切り替えの実機確認は行えない(型・lint・ビルドでの確認に留まる)。 | 2026-09-21 |
| P10-20 | 本人からの不具合報告「明細とかでheightが膨らんできた時にしたのメニューが固定じゃなくて付いてくる結果的に下のコンテンツが選択できない」に対応。**調査**:ボトムナビ(`app/(app)/layout.tsx`)は `position: fixed` の div として実装されているが、`<main>` と同じ `<div className="mx-auto ... flex-col">` の内側に描画されていた。Chromium で実際に Tailwind の実コンパイル結果を再現した静的HTMLを作り、長いページ(200行分のダミー明細、documentの高さ約13,000px)でスクロール時のナビの `boundingClientRect` を計測したところ、祖先チェーン(`html`/`body`/ラッパー div)に `transform`/`filter`/`backdrop-filter`/`contain`/`will-change` は無く、Chromium 単体では常に viewport 基準で正しく固定されることを確認した——つまり Chromium では再現しないが、`more-menu.tsx`(P10-4)で見つかった「一部のブラウザでは祖先の `backdrop-filter` が子孫の `position: fixed` の基準になる」不具合と全く同じ構造的リスク(ナビが `<main>` と共通の祖先に同居している)を抱えており、モバイル実機のブラウザ(iOS Safari 等)で同種の問題が起きても不思議ではない状態だった。**対応**:P10-4 と同じ対処を踏襲し、ボトムナビ全体(FAB・ナビのピル・その他メニューの丸ボタン)を `BottomBar` コンポーネントへ切り出し、`createPortal` で `document.body` 直下に描画するよう変更(`app/(app)/layout.tsx`)。これにより、`<main>` 側の祖先に何が来ても(将来どんな CSS 変更が入っても)ボトムナビの `position: fixed` は祖先の影響を受けなくなる。`more-menu.tsx` に定義されていたクライアント判定フック `useIsClient()`(`useSyncExternalStore` ベース、react-hooks/set-state-in-effect を避けるための実装)を `src/components/ui/use-is-client.ts`(新規)へ切り出し、`more-menu.tsx` と `app/(app)/layout.tsx` の両方から共有するようにした(2箇所目の利用が出たため重複を解消)。テストは追加していない(この種のレイアウト・CSS の挙動は React component テストの対象外という既存方針に倣った。ビルド確認用に作った静的HTML+Playwrightのスクロール計測はデバッグ目的のみで、恒久的なテストとしては残していない)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(697件全通過)/`npx next build` すべて成功。**未検証**:このセッションには実機のモバイルブラウザ(特に本人の報告に近い状況を再現できる実機)が無く、修正後に実際にボトムナビが長いページでも固定されたままになることの実機確認は行えていない。ただし Portal による対処は `more-menu.tsx` の同種不具合で既に実績があり、`document.body` 直下に描画する以上、祖先のCSSに起因するこの種の不具合は構造的に発生しなくなる。 | 2026-09-21 |
| P10-21 | AI家計診断(本人発案「AIの分析が弱い。もっと客観視した分析が必要。投資家目線で今のが浪費か必要経費なのか判断する機構とそれを分析結果を蓄積表示改善する機能が必要」、ADR-030)。事前に4点を確認し、①判断は明細1件ごと、②AI呼び出しは本人がボタンを押した時だけ、③表示は家計簿(/spending)の新カード、④「蓄積して改善」は判定結果を保存し月ごとの浪費傾向の推移を見せること、をいずれも推奨案どおりに決めた。**スキーマ**:`transaction_diagnoses` テーブル(`spending_verdict` enum、1明細1行・再診断は upsert で上書き、`supabase/migrations/20260921000100_transaction_diagnoses.sql`・`20260921000200_transaction_diagnoses_rls.sql`、`docs/schema.sql`・`src/lib/supabase/types.ts` に同期)。**ドメイン**(`src/domain/diagnosis.ts`、テスト9件):`summarizeDiagnoses()`(今月の浪費/必要経費内訳)、`summarizeDiagnosesByMonth()`(月次集計)、`wasteRatioOf()`(診断していない月は null を返し0%と誤読させない)。**AI層**(`src/features/diagnosis/diagnosis-ai.ts`、テスト7件):ADR-010 のモデル階層に従い分類ではなく推論寄りのタスクとして Sonnet(`claude-sonnet-5`)を使用(Haiku ではない。「分析が弱い」という不満への直接対応)、構造化出力は既存の receipt-ai.ts と同じ `zodOutputFormat()` + `messages.parse()`、プロンプトに「迷う場合は『必要経費』側に倒さない」という非寛容指示を明記。`buildFromAiRows()`(exported pure function)がモデル出力を信用しきらず検証(捏造id無視・重複id先勝ち・空文字理由破棄・不足時警告)。**ストア層**(`src/features/diagnosis/store.ts`):`listUndiagnosedTransactions()`(今月の未診断支出、`MAX_BATCH_SIZE=30` で1回の操作の課金上限を歯止め)、`saveDiagnoses()`(upsert、テーブル未適用時は「診断機能はまだ利用できません」)、`loadSpendingDiagnosisView()`(今月のスナップショット+直近6ヶ月推移を1回で返す。`goals`・`transaction_splits` と同じくテーブル未作成のエラー(PGRST205)を「診断はまだ無い」として握り潰す読み取り/握り潰さない書き込みの使い分け、B-13 参照)。**Server Action**(`app/(app)/spending/actions.ts`):`diagnoseSpendingAction()` が AI 呼び出しの唯一の入り口(ANTHROPIC_API_KEY 未設定時は分かりやすいエラー)。**UI**(`app/(app)/spending/diagnosis-card.tsx`、`page.tsx` に追加):今月の浪費比率・浪費/必要経費の内訳・浪費上位5件(理由付き)、直近6ヶ月の浪費比率推移の棒グラフ(`reports/category-trend-chart.tsx` と同じ組み方、未診断の月はバー無し)、診断対象が残っていれば「今月のN件をAIで診断する」ボタン。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(713件全通過、新規16件含む)/`npx next build` すべて成功。**未検証**:このセッションには本番 Supabase の管理APIも実際の Anthropic API キーも無く、①`transaction_diagnoses` マイグレーションの本番適用(B-13)、②実際の AI 呼び出しの判定・理由文の質、③本番データでのカード表示・棒グラフの見た目、は確認できていない。 | 2026-09-21 |
| P10-22 | AI家計診断(P10-21)への追加要望「分類機能について。無駄金ではないと判断されたものについてもなぜそう考えてもらえたのか。それを記載して欲しい」に対応。P10-21 時点では「浪費」と診断された明細だけ理由付きの上位5件を表示し、「必要経費」側は集計額(`summary.necessaryYen`)にしか反映されず、個々の判断理由は保存はされているのに画面上どこにも出ていなかった。**対応**:`src/features/diagnosis/store.ts` の `loadSpendingDiagnosisView()` に `necessaryItems`(今月「必要経費」と診断された明細、金額の大きい順・高々5件、`wasteItems` と同じ形)を追加し、`SpendingDiagnosisView['currentMonth']` に含めて返すようにした。`app/(app)/spending/diagnosis-card.tsx` は `wasteItems`/`necessaryItems` の描画を共通の `DiagnosisItemList` コンポーネントへまとめ、「浪費と判断した内訳」「必要経費と判断した内訳」の2つの見出し付きリストとして両方表示するようにした(金額の色だけ `--over`/`--ink` で区別)。AI側(`diagnosis-ai.ts`)・保存側(`saveDiagnoses`)は「必要経費」の判定時も理由文を必須で書かせる設計のまま変更なし(元から保存はしていた。表示していなかっただけ)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(713件全通過、既存のドメイン・AI層テストのみで新規テストは追加していない。集計ロジック自体は変更せず既存の `wasteItems` と対称な項目を増やしただけのため)/`npx next build` すべて成功。**未検証**:B-13(`transaction_diagnoses` の本番適用待ち)は解消しておらず、実データでの見た目は依然として本番適用後でないと確認できない。 | 2026-09-21 |
| P10-23 | P10-22 のレビュー中に本人から「上位5件という必要はなく別に全文表示すればいい」と指摘を受け対応。**対応**:`src/features/diagnosis/store.ts` の `loadSpendingDiagnosisView()` から `wasteItems`/`necessaryItems` それぞれの `.slice(0, 5)` を外し、今月診断済みの全件(金額の大きい順)を返すようにした。`app/(app)/spending/diagnosis-card.tsx` のコメントも「上位」から「全件」に合わせて修正。ソート順・集計ロジック(`summarizeDiagnoses`)自体は変更していない。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(713件全通過)/`npx next build` すべて成功。**未検証**:B-13 と同じ制約(本番適用待ち)に加え、件数が多い月でカードが縦に長くなりすぎないかは実データでの確認が必要(本セッションでは未確認)。 | 2026-09-21 |
| P10-24 | AI月次レポート(本人発案「AI関連もっと増やしたい。もっと画期的な機能ない?」「日次レポートと月次レポートどっちも出力できるように...性格の特定もお願いしたい」、ADR-031)。当初案の「ホルモンが多い」「性格だからこうしないといけない」といった医学的な断定は実データの裏付けが無いため実装しないことを提案し合意を得た(「ホルモンとかはいいけど、一般的にこういうタイプとか入って欲しい」)。事前に4点を確認し、①生成は本人がボタンを押した時だけ、②表示は新しい画面 `/reports/ai`、③性格タイプは毎回そのレポート時点のデータで判定し直す(固定プロフィールとして保存しない)、④最初は月次レポートから作る、をいずれも推奨案どおりに決めた。**性格タイプ**:`src/domain/persona.ts` に固定6分類(`SpendingPersonaType`:衝動買い型・堅実型・交際費型・目標志向型・倹約家型・メリハリ型)を定義し、AIには新しい分類名を作らせず必ずこの中から選ばせる(`transaction_diagnoses` の `spending_verdict` enum と同じ「固定語彙にしてAIの自由記述を制限する」設計)。**スキーマ**:`spending_persona_type` enum と `ai_monthly_reports` テーブル(1ヶ月1行、`user_id, month` に unique 制約、再生成は upsert で上書き、`supabase/migrations/20260921000300_ai_monthly_reports.sql`・`20260921000400_ai_monthly_reports_rls.sql`、`docs/schema.sql`・`src/lib/supabase/types.ts` に同期)。**AI層**(`src/features/ai-report/monthly-report-ai.ts`、テスト6件):Sonnet(`claude-sonnet-5`、ADR-030 と同じ判断)を使用、システムプロンプトで「医学的な診断、体質の断定、食事・サプリ・栄養に関する助言は一切書かない」ことを明示し、`insights`/`advice` は渡した実際の数字だけを根拠にさせる。`buildFromAiOutput()`(exported pure function)がモデルの出力を信用しきらず検証(空文字除去・5件に切る)。**ストア層**(`src/features/ai-report/store.ts`):`loadMonthlyReportInput()` は既存の `loadMonthlyLedger()`・`loadSpendingDiagnosisView()`・`loadHomeSummary()` をそのまま呼び出すだけで新規クエリを増やさない(AIへ渡す明細例は `MAX_ITEMS_PER_LIST=10` 件に絞り、P10-23 のカード全件表示とは別にAI入力のコストを歯止め)、`saveMonthlyReport()`(upsert、テーブル未適用時は「レポート機能はまだ利用できません」)、`loadMonthlyAiReportView()`(数値データ+レポートを1回で返す。`goals`・`transaction_diagnoses` と同じくテーブル未作成のエラー(PGRST205)を「レポートはまだ無い」として握り潰す読み取り/握り潰さない書き込みの使い分け、B-14 参照)。**UI**(`app/(app)/reports/ai/` 新規):今月の総支出・浪費比率推移(diagnosis-card.tsx の DiagnosisTrendBars と同じ組み方)・負債返済進捗(既存の ProgressGauge 再利用)は常に表示、レポート生成後は浪費傾向のタイプ・気づき・アドバイスを追加表示。カテゴリ別内訳は `/spending` の `CategoryBreakdownChart` をそのまま再利用。`/reports` ヘッダと `more-menu.tsx` にリンクを追加。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(722件全通過、新規9件含む)/`npx next build` すべて成功。`scripts/verify-schema.sh`(32テーブル、RLS全有効)・`scripts/verify-migrations.sh`(1011項目一致)も成功。**未検証**:このセッションには本番 Supabase の管理APIも実際の Anthropic API キーも無いため、①`ai_monthly_reports` マイグレーションの本番適用(B-14)、②実際の AI 呼び出しのタイプ判定・気づき・アドバイスの質、③本番データでの画面表示、は確認できていない。日次レポートは今回のスコープに含めていない(次の段階)。 | 2026-09-21 |
| P10-25 | AI月次レポート(P10-24)マージ直後、本人から「月次と日次両方先に実装して」との指示を受け、日次レポートを追加(ADR-032)。P10-24時点では「最初は月次から作る」を選んでいたが、日次も先に実装する方針に変更。**なぜ日次に浪費傾向のタイプ判定を持たせないか**:1日分のデータだけでタイプ判定をすると、たまたまその日に外食が重なっただけで判定がぶれる等ノイズが大きく、月次レポート(ADR-031)が目指した安定したラベル付けの価値を損なう。タイプ判定は月次に一本化し、日次は気づき・アドバイスに絞った。**スキーマ**:`ai_daily_reports` テーブル(`ai_monthly_reports` と同じ構造だが persona_type/persona_reasoning を持たない、1日1行、`user_id, report_date` に unique 制約、`supabase/migrations/20260921000500_ai_daily_reports.sql`・`20260921000600_ai_daily_reports_rls.sql`、`docs/schema.sql`・`src/lib/supabase/types.ts` に同期)。**AI層**(`src/features/ai-report/daily-report-ai.ts`、テスト5件):月次と同じ Sonnet を使用、システムプロンプトに「1日分のデータだけで性格や浪費傾向のタイプを断定しない」ことを明記。**ストア層**(`src/features/ai-report/store.ts` に追加):`loadDailyReportInput()` は月次と同じ `loadMonthlyLedger()`・`loadSpendingDiagnosisView()` を今日の日付でフィルタするだけで新規クエリを増やさない(比較基準の「今月のここまでの1日あたり平均支出」は既存の `pace.thisMonthToDateYen / pace.dayOfMonth` から導出)、`saveDailyReport()`・`loadDailyReport()`・`loadDailyAiReportView()` は月次と同じ「読み取りは握り潰す・書き込みはエラーを返す」設計(B-15参照)。**UI**:新しい画面は作らず、既存の `/reports/ai`(ADR-031)に `DailyReportCard`(`app/(app)/reports/ai/daily-report-view.tsx`、新規)を `MonthlyReportCard` の上に追加。画面見出し・リンク文言を「AI月次レポート」から「AIレポート」に改めた(`/reports` ヘッダ、`more-menu.tsx`)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(727件全通過、新規5件含む)/`npx next build` すべて成功。`scripts/verify-schema.sh`(33テーブル、RLS全有効)・`scripts/verify-migrations.sh`(1025項目一致)も成功。**未検証**:このセッションには本番 Supabase の管理APIも実際の Anthropic API キーも無いため、①`ai_daily_reports` マイグレーションの本番適用(B-15)、②実際の AI 呼び出しの気づき・アドバイスの質、③本番データでの画面表示、は確認できていない。 | 2026-09-21 |
| P10-26 | プロジェクト全体のリファクタリング(本人発案「影響範囲を最小に関数の分離ファイル分割移動。コメントを最小限にする。重複を一切なくす(名前の概念は重複させ、同じ概念は同じ言葉)シーケンスを整理し、同じ考慮を2回しない」、ADR-033)。34,000行を調べ、機械的に同型な重複だけを対象にした。**集約したもの**:①例外クラスの本体63箇所 → `src/lib/errors.ts` の `AppError`(各クラスは1行宣言に、`instanceof` 判定はそのまま)、②想定内エラー→本人向け文言の変換13箇所 → 同 `describeUserError()`(各画面が自分の例外クラスを列挙していたのをやめた)、③Anthropic の失敗を日本語にする `describeError` 7箇所 → `src/lib/anthropic.ts` の `describeAnthropicError()`、④構造化出力の3つの考慮(API失敗/max_tokens で切れた/解釈できない)6箇所 → 同 `parseStructured()`、⑤テーブル未作成(PGRST205)の判定4箇所 → `src/lib/supabase/errors.ts`、⑥`ANTHROPIC_API_KEY` の未設定判定と文言8箇所 → `readAnthropicApiKey()`/`apiKeyMissingMessage()`(未使用だった投げる版 `getAnthropicApiKey()` は削除、生の `process.env` 読みは無くなった)、⑦月ラベル4箇所 → `src/lib/date.ts` の `formatMonthJa()`、⑧浪費比率の棒グラフ・箇条書き 各2箇所 → `src/components/ui/` の `WasteRatioBars`/`BulletList`、⑨AIレポートの Server Action 2本の同一手順 → `runReportAction()`。**同じ言葉を同じ概念へ**:`describeError` が「Anthropic の失敗」「ドメイン例外」「Google の応答」の3概念に使われていたため名前を分けた。**コメント**:同じ経緯説明が4ストアに書き写されていた(未適用テーブルの扱い)ものを集約先に1回だけ書き、各ファイルは1行参照に。仕様の引用や本人の発言はADR・TASKS.mdに既にあるためコードから外した。**意図的に変えた振る舞い**(いずれも表示文言の統一、ADR-033に列挙):/job-change の想定外エラーが生メッセージを出していたのを他画面と同じ文言に、AI相談の「切れた/解釈できない」文言を他のAI機能と統一、分類バッチの切れた時の括弧位置。**却下**:カードの見た目27箇所の共通化(既存 `Card` とは実際に別の見た目で、寄せると表示が変わる。ロジックの集約と混ぜると差分が読めなくなるため今回は手を付けず、既知の残重複として記録)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(727件全通過)/`npx next build`/`scripts/verify-migrations.sh`(1025項目一致)すべて成功。99ファイル・純減856行。**未検証**:本番での実機確認(実アカウントでログインする手段が無い)。 | 2026-09-22 |
| P10-27 | 本人からの不具合報告「今日作ってもらったai分析うごかない エラーが出る this page couldnt load」に対応。**原因**:日次レポート(P10-25)の「今月のここまでの1日あたり平均」を `thisMonthToDateYen / dayOfMonth` の**割り算のまま** `formatYen()` に渡していた。`formatYen()` は `assertYen()` で小数を `MoneyError` にする設計(ADR-008:円は整数で扱う)のため、平均が割り切れない限り必ず例外になり、`/reports/ai` がサーバー描画で落ちて Next.js のエラー画面(This page couldn't load)になっていた。月次カードと診断カードは整数の合計しか渡していないため無事で、日次カードを足した時点から壊れていた。**対応**:`src/domain/spending.ts` に `averageDailySpendYen()`(既存の `projectedMonthTotalYen()` と同じく `Math.round` で整数の円に丸める純粋関数)を追加し、ストアはこれを通す。AI プロンプト側(`daily-report-ai.ts`)にあった `Math.round()` は丸めが手前で済むため削除した(同じ丸めを2回しない)。**再発防止**:`tests/domain/spending.test.ts` に3件追加。うち1件は「割り切れない入力でも整数になり `formatYen()` が例外を投げない」ことを直接検証する(修正前の値 `10000/3` が `MoneyError` になることも確認済み)。他に割り算を `formatYen()` に渡している箇所が無いことを全 `formatYen()` 呼び出しの引数を辿って確認した(他は整数の合計・DB値のみ)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(730件全通過)/`npx next build` すべて成功。**未検証**:本番での実機確認(実アカウントでログインする手段が無い)。ただし本件は入力値によらず決定的に例外になる計算の誤りで、純粋関数のテストで再現・修正の両方を確認できている。 | 2026-09-22 |
| P10-28 | 本人の指示「Sqlも適用するので適用しやすいようにして」に対応(以前「むずい」と言われた、ファイルを1つずつ貼る手順の改善)。**成果物**:`supabase/apply-pending.sql`(新規)——本番未適用の16マイグレーション(B-4/B-5/B-7/B-10/B-12/B-13/B-14/B-15)を1ファイルに連結し、SQL Editor へ1回貼って Run するだけで済む形にした。**冪等**:`create table if not exists`・`create index if not exists`・`add column if not exists`・enum は `do $$ ... exception when duplicate_object then null`・トリガとポリシーは `drop ... if exists` の後に作成。既に一部が適用済みでも、二度実行しても壊れない。全体を1トランザクションに包み、途中で失敗したら何も適用されないようにした。末尾に確認用の SELECT を置き、テーブル・RLS・ポリシー・追加列が揃っているかを「ok / NG」の表で見られるようにした(本人が成功を目で確認できる)。**除外**:`20260913000400_receipt_storage_rls.sql`(storage.objects へのポリシー)は権限で失敗し得てトランザクション全体を巻き戻すため、かつ既に適用済みのため含めていない。**写しのずれ対策**:この1ファイルは migrations/ の写しなので放置すればずれる。`scripts/verify-apply-pending.sh`(新規)で、①未適用分を除いた「本番相当」DBを作り、②apply-pending.sql を2回適用し、③全マイグレーション適用済みDBとスキーマダンプを比較する、という手順を機械的に検証し、CI(`.github/workflows/ci.yml`)にも追加した。**検証**:`./scripts/verify-apply-pending.sh` 成功(1014項目一致、2回実行しても同じ結果、確認表は全 ok)。`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(730件全通過)も成功。**未検証**:本番 Supabase への実適用そのもの(本人の操作待ち)。ローカル PostgreSQL 16 では本番相当の状態から適用できることを確認済み。 | 2026-09-22 |
| P10-29 | 本人の音声入力での不具合報告「レシートの明細(小項目)がなくてどこから見れるかの動線がわからない、ちゃんと詳細も置いとくようにしないと分析でも飲み物に使ってるとかがわからない」に対応。当初は「よく買う品目のランキングをどこに表示するか」を尋ねる設問を用意したが、本人の回答は場所の選択ではなく「そもそも、基本的な概念のレシートっていうのは、店とか品目とか全てを合わせてその結果レシートって言うふうな概念にしたい。そのため今レシート個別表示しているようなところはすべて共通に品物、も表示すべきだと思っている」という、レシートの概念そのものについての指摘だった(ADR-034)。**原因**:`receipt-ai.ts` の `buildItems()` は元々「商品行2件以上・合計が支払額と一致」の場合しか品目を返さない実装で、これは既存のカテゴリ分割機能(`transaction_splits`、B-7)の前段処理として作られていたため。1点だけの買い物や読み取り誤差で合計が合わない場合、AIがせっかく読み取った品目が保存前に静かに捨てられていた。**対応**:カテゴリ分割(`transaction_splits`、2件以上・合計一致必須)とは独立した新しいテーブル `receipt_items` を追加し、商品行が1件でもあれば常に保存・表示する「情報としての品目記録」にした。`buildItems()` から2件以上・合計一致の制約を外し、分割の可否判定は新しい純粋関数 `itemsReconcileWithTotal()` に切り出して分離。スキーマは `supabase/migrations/20260922000100_receipt_items.sql`・`..._rls.sql`(`docs/schema.sql`・`src/lib/supabase/types.ts` に同期)。ストア層は `src/features/receipts/items-store.ts`(新規、`replaceReceiptItems()`・`listReceiptItemsForTransactionIds()`、`splits-store.ts` と同じパターン)。取り込み(`receipt/page.tsx`)は商品行があれば分割対象かどうかに関わらず `receiptItems` として送り、分割対象の場合は同じ `sourceRef` で `receiptSplits` も併送する(`actions.ts` の `saveImportBatchAction()` が両方をこの鍵で紐付ける)。表示は `/transactions`(`page.tsx`・`split-editor.tsx`、分割サマリが無い明細にだけ品目名を表示し重複を避ける)と `/spending`(`ledger-types.ts`・`store.ts`・`transaction-list.tsx`、明細行の下に読点区切りで表示)の両方。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(733件全通過、P10-28時点の730件から純増3件)/`npx next build` すべて成功。`scripts/verify-schema.sh`(34テーブル、RLS全有効)・`scripts/verify-migrations.sh`(1042項目一致)・`scripts/verify-apply-pending.sh`(10テーブル全 ok、1031項目一致)も成功。**未検証**:このセッションには本番 Supabase の管理APIも実際のレシート画像・Anthropic API キーも無いため、①`receipt_items` マイグレーションの本番適用そのもの(B-16 参照)、②実際のAI抽出が本番の様々なレシートでも妥当な品目名を返すこと、③本番データでの `/transactions`・`/spending` の表示、は確認できていない。よく買う品目のランキング機能は今回のスコープに含めていない(品目データの記録基盤は今回で揃ったため、次に作る場合は `receipt_items` を集計するだけで済む)。 | 2026-09-22 |
| P10-30 | 本人が `supabase/apply-pending.sql`(P10-28、B-16 で更新)を本番 Supabase の SQL Editor で実行し、「適用した」と報告。**解消したBlocked**:B-4(`rescued_emails`)・B-5(`net_worth_snapshots`)・B-7(`transaction_splits`)・B-10(`goals`)・B-12(`app_settings.google_backup_spreadsheet_id`)・B-13(`transaction_diagnoses`)・B-14(`ai_monthly_reports`)・B-15(`ai_daily_reports`)・B-16(`receipt_items`)の計9件を Blocked から除去。**自動的に使えるようになった機能**:資産の記録・棚卸し(T-11)・純資産推移グラフ(P6-3)・明細の複数カテゴリ分割(P8-2)・AI相談での目標管理(ADR-023)・Googleスプレッドシート自動バックアップ(P9-5、B-9のGoogle OAuth設定が別途必要)・AI家計診断(P10-21/22/23、ADR-030)・AI月次/日次レポート(P10-24/25、ADR-031/032)・レシートの品目常設記録(P10-29、ADR-034)——いずれも読み取りは既にテーブル未作成時の握り潰しで動いていたため、コード変更は不要(適用は本人の操作のみで完結)。**このセッションでの検証**:本番 Supabase の管理API資格情報がこのサンドボックスに無いため、実際に本番へ反映されたことを直接照会して確認することはできていない。本人の「適用した」という報告と、`apply-pending.sql` 自体が `scripts/verify-apply-pending.sh` で機械的に検証済み(実行すれば確実に9テーブル/2列が揃う内容であること)を根拠に、TASKS.md 側の状態のみ更新した。 | 2026-09-22 |
| P10-31 | 本人からのUX指摘「機能が増えすぎて見にくい。情報の重複があってはならない。レシートの品目もどこから飛べばいいかわからない、タップしても何も見れない」の調査結果、3件に分けて対応することにした最初の1件(残り2件はP10-32/P10-33)。**原因**:`/transactions` の明細行(`split-editor.tsx`)は行全体がボタンで開閉はできていたが、開いた先は常にカテゴリ編集フォームのみで、レシートの品目(ADR-034/P10-29)はプレビューの読点区切りテキストとして見えるだけで、行を開いても品目そのものを確認する手段が無かった(`/spending` 側の `transaction-list.tsx` は行自体がボタンですらなく、そちらは重複削除(P10-32)で表示ごと `/transactions` に一本化する予定のため今回は対象外)。**対応**:`split-editor.tsx` に、行を開いたときレシートの品目(商品名+単価)を一覧表示する区画を追加(カテゴリ編集フォームより前に配置。「何が入っていた買い物か」を先に見せてから分類を直す動線)。新規の型・クエリは追加せず、既存の `receiptItems` プロパティ(name/amountYen)をそのまま表示するだけ。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(733件全通過、UIのみの変更のためユニットテスト追加なし)/`npx next build` すべて成功。**未検証**:実機・実データでのタップ操作の確認はこのセッションの手段(本人のログイン手段が無い)では行えていない。 | 2026-09-22 |
| P10-32 | 本人からのUX指摘「機能が増えすぎて見にくい。情報の重複があってはならない、どこか一箇所見ればその情報がわかるように」の3件のうち2件目(1件目はP10-31、別PR)。**対象**:`/spending` の `MonthlyTransactionList`(今月の明細をこの画面だけ読み取り専用で再掲)が `/transactions`(全期間、並び替え・分類編集も可能)とほぼそのまま重複していた。**対応**:`app/(app)/spending/page.tsx` から `MonthlyTransactionList` の呼び出しを削除し、唯一の利用元だった `app/(app)/spending/transaction-list.tsx` を削除(新たな導線は追加していない。ヘッダーに既にあった「明細(全期間)」リンクが同じ役割を担うため)。`features/spending/store.ts`/`loadMonthlyLedger()` 自体は `ai-report`・`diagnosis` の両ストアも呼び出しており手を入れていない(この画面での表示をやめただけ)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(733件全通過、表示専用コンポーネントの削除のためテスト変更なし)/`npx next build` すべて成功。**未検証**:実機での見た目確認はこのセッションの手段(本人のログイン手段が無い)では行えていない。 | 2026-09-22 |
| P10-33 | 本人からのUX指摘「機能が増えすぎて見にくい。パンパンパンパン、詳細見たかったら詳細見るみたいな感じがいい」の3件のうち3件目(1件目は#74、2件目は#75、いずれも別PR)。**対象**:`/spending` の `DiagnosisCard`(AI家計診断)は診断済みの月には内訳(浪費/必要経費の理由付きリスト全件)・直近6ヶ月の浪費比率推移グラフまで常に全展開しており、この画面で縦に一番場所を取っていた。**対応**:見出しと浪費比率(%)は常に見せたまま、内訳・推移グラフは「詳しく見る」ボタンを押すまで畳むように変更(`useState` を1つ追加しただけ、新規の型・クエリは無し)。未診断分がある場合の「今月のN件をAIで診断する」ボタンとエラー・警告表示は開閉に関わらず常に見せる(操作に直結するため)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(733件全通過、開閉状態のみの変更のためテスト変更なし)/`npx next build` すべて成功。**未検証**:実機での見た目・タップ操作確認はこのセッションの手段(本人のログイン手段が無い)では行えていない。 | 2026-09-22 |
| P10-34 | ADR-034(レシートの品目)への追加要望3点(ADR-035)。①「表示できる場合は表示・できない場合は一言。品目が無ければ無い、合計が一致しなければその旨を書いて金額を修正できるようにする」②「合計不一致は撮り直しを1回促す、2回目で無理ならそのまま登録可、手入力修正も用意。ただしどちらも本人必須ではない」③「レシートのAI分類ボタンは最初から自動で。品目もできれば分類したい」。並行して進んでいた別セッションの2つのPR(P10-31、レシート品目のタップ表示/`split-editor.tsx`、および P10-32、`/spending` の明細一覧削除/`transaction-list.tsx`)と衝突したため、`main` を取り込んでから解消した(`split-editor.tsx` は両方の追加区画を残す形、`transaction-list.tsx` は削除を受け入れた)。**3状態判定**:`src/domain/receipt-items.ts`(新規、テスト7件)の `receiptItemsStatus()`(none/reconciled/mismatched、表示のたびに再計算)と `assertEditableReceiptItems()`(手入力修正の保存前チェック、合計一致は求めない)。**撮り直し**:`receipt/page.tsx` に `ReceiptEntry.retryCount` を追加し、不一致の明細に1回だけ「この写真を撮り直す」ボタンを出す(`extractByTargets()` に共通化した抽出処理で該当1枚だけ再抽出)。2回目以降はボタンを出さず文言だけ変える(粘着質にしない)。**手入力修正**:`split-editor.tsx` に品目の金額・カテゴリ編集フォームを追加(不一致の明細だけ、新規 `replaceReceiptItemsAction()`)。分割編集(mode)とは独立した開閉状態にした(分割の合計一致必須という保存条件に引きずられないため)。`/spending` の明細一覧は P10-32 で削除されたため、こちらでの不一致表示は無し(表示先が `/transactions` 一本になったため)。**品目も分類**:`entryPreviews` の `itemRulePreviewByIndex` を分割対象の明細だけでなく商品行がある明細すべてに広げ、分類結果を `receipt_items.category_id` として保存(`supabase/migrations/20260922000300_receipt_items_category.sql`、`docs/schema.sql`・`types.ts` に同期、本番未適用=B-17)。**分類の自動化**:読み取り完了(`extractAll`・撮り直し)の直後に自動でAI分類まで実行(`wasExtractingRef` で完了の瞬間だけ検知)、ボタンは失敗時の再試行用に残した。**列が無い間の後方互換**:`category_id` は `receipt_items` 本体より後の列で本番未適用のため、`items-store.ts` は列付きで INSERT/SELECT が失敗したら列を外して再試行する(新規 `isMissingColumnError()`、`lib/supabase/errors.ts`。PGRST204/42703、`transactions/store.ts` の `receipt_image_path` と同じ実コード)。これにより B-17 未適用でも品目そのものの保存(ADR-034)は退行しない。**マージ時の副次対応**:P10-32 で `/spending` の明細一覧(`transaction-list.tsx`)が削除された結果、そこでしか使っていなかった `LedgerTransaction.itemNames`/`itemsStatus` とその集計クエリ(`loadMonthlyLedger()` 内の `listReceiptItemsForTransactionIds()` 呼び出し)が使われなくなったため、あわせて削除した(使われなくなったコードを残さない、ADR-033の方針)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(740件全通過、P10-29時点の733件から純増7件)/`npx next build` すべて成功。`scripts/verify-schema.sh`(34テーブル、RLS全有効)・`scripts/verify-migrations.sh`(1044項目一致)・`scripts/verify-apply-pending.sh`(1項目、2回実行しても同じ結果)も成功。**未検証**:このセッションには本番 Supabase の管理APIも実際のレシート画像・Anthropic API キーも無いため、①`receipt_items.category_id` の本番適用そのもの(B-17 参照)、②実際の撮り直し・自動分類の体感、③本番データでの手入力修正フォームの使い勝手、は確認できていない。 | 2026-09-22 |
| P10-35 | 本人からのUX指摘(9/22)「機能が増えすぎて見にくい。パンパンパンパン、詳細見たかったら詳細見るみたいな感じがいい」への対応の続き(1〜3件目はP10-31/32/33、いずれもマージ済み)。**対象**:`/transactions` は全期間・全件をそのまま流し込むだけの一覧で、明細が増えるほど目的の行を探しにくい(本人の提案「プルダウンなどで表示する」)。**対応**:`app/(app)/transactions/filters.tsx`(新規、Client Component)で口座・カテゴリ・月の3つの絞り込みプルダウンを追加。選択状態は `router.push()` でURLのクエリパラメータ(`?account=&category=&month=`)に持たせ、`page.tsx` がサーバー側で `searchParams`(この Next.js バージョンでは Promise、`node_modules/next/dist/docs` で確認済み)を読んで絞り込む設計にした(ブックマーク・共有・再読み込みで状態が消えない。クライアント側 state だけで絞り込むよりも壊れにくい)。カテゴリの選択肢には実データに無い「未分類」も追加(`categoryId: null` を指す特別値 `'none'`)。月の選択肢は実際に明細がある月だけを動的に列挙(無い月を選ばせても空になるだけのため)。**絞り込みの対象外にしたもの**:リボ・キャッシングバナー、二重計上バナー、確認待ちバナーは「今すぐ対応が要るもの」を知らせる目的のため、絞り込みの影響を受けず常に全件を対象に数える(見えている一覧を絞っても、対応すべきことを見失わせないため)。フッターの件数表示は絞り込み中なら「N件 / 全M件」に切り替える。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(740件全通過、`matchesFilter`/`listMonthOptions` は本ファイル内の `groupByDate` と同じ既存の慣例に倣いページ内のプライベート関数のままテスト追加なし)/`npx next build` すべて成功。**未検証**:実機でのプルダウン操作・実データでの絞り込み結果はこのセッションの手段(本人のログイン手段が無い)では行えていない。 | 2026-09-22 |
| P10-36 | 本人からの不具合報告「明細を開くとカテゴリ分けのやつが出てくる。これもいらん」に対応(P10-31で品目のタップ導線を直した直後に発覚)。**原因**:`/transactions` の明細行(`split-editor.tsx`)は元々「行を開く=カテゴリ編集フォームを出す」というボタンだったところに、P10-31で品目一覧を同じ `open` state に相乗りさせたため、品目を見たいだけで行を開いてもカテゴリ編集フォーム(単一カテゴリの選択+保存ボタン、あるいは分割フォーム)まで常に一緒に出てしまっていた。**対応**:新しい state `categoryFormOpen` を追加し、レシート品目がある明細(`items.length > 0`)だけ、開いた直後は品目一覧のみを見せ「カテゴリを変更する」ボタンを押すまでカテゴリ編集フォームを出さないようにした。**品目が無い(大半の)明細は変更していない**——それらは開く目的がそもそもカテゴリ編集なので、今までどおり開けば直接フォームが出る(ここで余計な1タップを増やすと逆に不便になるため)。行を閉じる・保存が成功するとカテゴリ編集フォームの開閉状態もリセットする(次に開いたときまた品目一覧だけの状態から始まるように)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(740件全通過、UIの開閉状態のみの変更のためテスト変更なし)/`npx next build` すべて成功。**未検証**:実機でのタップ操作確認はこのセッションの手段(本人のログイン手段が無い)では行えていない。 | 2026-09-22 |
| P10-37 | P10-36 の直後、本人が実機のスクリーンショット(品目の無い明細でもカテゴリ編集フォームが開いたまま複数並んでいる画面)を添えて「このカテゴリ分けが出てくるしこれも編集でしか不要」と指摘。P10-36 は「品目がある明細だけ」に絞って直していたが、品目の有無に関わらず全ての明細で直してほしいという指摘だったと判断した。**対応**:`split-editor.tsx` の `showCategoryForm`(P10-36で追加)から `items.length === 0 ||` の例外を外し、`categoryFormOpen` の値だけで判定するようにした(常に「カテゴリを変更する」を押すまでフォームを隠す)。「カテゴリを変更する」ボタンの表示条件からも `items.length > 0` を外し、品目の無い明細でも開いた直後はボタンだけを見せる形に統一。コメントも「品目がある明細だけ」という前提を削除し、実機のスクリーンショットで判明した経緯を追記。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(740件全通過、UIの表示条件のみの変更のためテスト変更なし)/`npx next build` すべて成功。**未検証**:実機でのタップ操作確認はこのセッションの手段(本人のログイン手段が無い)では行えていない。 | 2026-09-22 |
| P10-38 | 本人発案「品目が不明な場合はその旨書いてくれ」。`/transactions` の明細行を開いたとき、品目(`receipt_items`)が1件も無い場合は今まで何も表示していなかった(黙って空白になるだけ)。**対応**:`split-editor.tsx` の品目パネルを、開いたときは常に表示する形に変え、品目が1件以上あれば今までどおり一覧、無ければ「品目の記録はありません」という文を出すようにした。**折りたたんだ行のプレビュー(P10-31)は変更していない**——プレビューは全明細で常に見える行のため、品目の無い(=大半を占める)明細すべてに「品目不明」を出すと今度はそちらが雑音になる(P10-33/P10-36/P10-37で繰り返し出ている「見えるものを減らす」方針と矛盾するため)。事実を明示するのは、本人が実際に開いて見に行った時(この行の主旨どおり)に限定した。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(740件全通過、UIの表示分岐のみの変更のためテスト変更なし)/`npx next build` すべて成功。**未検証**:実機での表示確認はこのセッションの手段(本人のログイン手段が無い)では行えていない。 | 2026-09-22 |
| P10-39 | 本人発案「追加で更なる小分類(AIが独自判断でいいので)、レシートの商品の分類と、あとは全体を通して生活費の何系か小分類まで分けてほしい。AIの判断に基づいていい」(ADR-036)。事前に3点(呼び出しタイミング・対象範囲・ラベルの形)を確認し、いずれも推奨案どおり決めた後、本人から「レシート解析時のやつに一個つけたい」との補足を受け、新しいボタンを増やさずレシート取り込み(receipt-ai.ts)の抽出に相乗りさせる方針にした。**AI抽出層**(`src/features/import/receipt-ai.ts`):`itemSchema`に`product_type`(商品の種類、例:飲料・調味料)、`rowSchema`に`expense_subtype`(生活費の何系か、例:食費・日用品)を自由記述で追加。固定選択肢は渡さず、「画像に写っていることだけを返す」という基本原則の例外(モデル自身の判断でよい)と明記。空文字はnullに変換(`toNullableLabel()`新規)。**スキーマ**:`receipt_items.product_type`(`supabase/migrations/20260922000400_receipt_items_product_type.sql`)、新テーブル`transaction_expense_subtypes`(`...000500`・`..._rls`)。`transactions`本体に列を足さなかった理由はreceipt_items・transaction_splitsと同じ(この値は今のところレシート取り込み経路からしか生まれない、ADR-036参照)。**後方互換**(`items-store.ts`):`category_id`(ADR-035)と`product_type`は同時期に追加した列として一括りにし、列付きで失敗したら両方まとめて外して再試行(2列×2列の総当たりは複雑さに見合わないと判断)。**新規ストア**(`src/features/receipts/expense-subtype-store.ts`)。**取り込み**(`receipt/page.tsx`・`actions.ts`):既存の`sourceRef`の仕組みに`ReceiptExpenseSubtypeInput`を追加。**発見したバグを修正**:従来`sourceRef`は商品行がある場合だけ発行していたため、内訳が無いのに小分類だけ付くレシートで対応付けの鍵が無く保存できなかった——発行条件を「商品行がある、または小分類がある」に修正。**表示**(`split-editor.tsx`):品目一覧に商品の種類を括弧書きで添え、生活費の小分類はカテゴリのcodeが`living`のときだけ「生活費の内訳:〜」と表示(表示名ではなくcodeで判定、ADR-016)。品目の手入力修正フォーム(ADR-035)は全行置き換えのため、`productType`を編集させずに持ち回るよう修正(そうしないと金額修正のたびにAI分類が消える)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(742件全通過、新規テスト2件)/`npx next build` すべて成功。**未検証**:このセッションには本番 Supabase の管理APIも実際のレシート画像・Anthropic API キーも無いため、新規マイグレーションの本番適用・実際のAI判断の精度・本番データでの表示は確認できていない。 | 2026-09-23 |
| P10-40 | 本人発案「品目の記録がありませんについて、その項目についてはその明細のところにレシートを再度登録するというようなところを作ってもらってもいいですか」(ADR-037)。品目が読み取れなかった明細は「品目の記録はありません」(P10-38)と出るだけで、後から直す手段が無かった。**対応**:`/transactions` の明細行の「品目の記録はありません」の直下に「レシートを登録する」ボタンを追加。その場で写真を選ぶと**既存の明細へ**品目を追加する(新しい画面遷移を挟まない、「その明細のところに」という要望どおり)。**新しいAPIは作らず既存の`/api/import/receipt`を再利用**(レシート抽出=画像→店名・金額・品目・生活費の小分類、ADR-036と全く同じ経路。1時間20回の呼び出し歯止めも自然に共有)。読み取った内容のうち品目一覧・生活費の小分類だけを使い、店名・日付・合計金額(紐付け先の明細は既に確定しているため)は捨てる。**共有化**:`receipt/page.tsx`だけが持っていた画像リサイズ処理を`src/features/import/resize-image.ts`(新規)へ切り出した(ADR-033の方針)。**品目の分類はルールのみ(AIには通さない)**:明細1件への追加操作のたびにAI分類を課金で呼ぶのは割に合わないため、`DEFAULT_DETECTION_RULES`+学習済みルールだけで分類し、保存は既存の`replaceReceiptItemsAction()`・`setExpenseSubtypeAction()`をそのまま呼ぶ(新しい保存経路を増やさない)。それでも未分類・不一致なら既存の「金額を直す」(mismatched時の手入力編集、ADR-035)からカテゴリも直せる。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(742件全通過、UIとオーケストレーションのみの変更のためユニットテスト追加なし)/`npx next build` すべて成功。**未検証**:このセッションには本番 Supabase の管理API・実際のレシート画像・Anthropic API キーが無いため、実機での撮影→読み取り→保存の一連の動作は確認できていない。**#82(P10-39)を土台にしたブランチ**——先にそちらをマージしてからこのPRをマージすること。 | 2026-09-23 |
| P10-41 | 本人が銀行アプリ(三菱UFJ)の「送金明細」画面(受付番号・送金元/先の支店・口座・氏名・送金金額・送金日を表示)のスクリーンショットを提示し「読み取りできるようにして」と依頼(ADR-038)。**原因**:`receipt-ai.ts` のシステムプロンプト・スキーマがレシート・領収書限定で、「レシート・領収書でない画像は `is_receipt` を false にする」と明記していたため、振込明細のスクリーンショットは全て「レシート・領収書として認識できませんでした」で弾かれていた。**対応**:新しい画面・テーブルは作らず、`buildFromAiRows()` 以降の処理(保存経路・画面)は一切変更せずに、モデルへの指示だけを拡張した——振込から取り出したい情報(日付・金額・相手・内訳)はレシートと同じ形に落とし込めるため。`store_name` は「振込の場合は送金先(受取人)の名前」、`amount_yen` は「振込の場合は送金金額(手数料は含めない)」、`payment_method_text`・`items`・`expense_subtype`(ADR-036)は振込の場合は空でよいことをスキーマの説明文とシステムプロンプトに追記。判定フィールド `is_receipt` は実態に合わせて `is_recognized` に改名(呼び出し箇所は本ファイルとテストの2箇所のみ)。`receipt/page.tsx` の案内文に「銀行アプリの振込/送金完了画面のスクリーンショットも読み取れます」を追記(画面タイトル・保存経路は変更なし、スクリーンショットは元々「選ぶ」から取り込める)。振込も送金した側から見れば支出(ADR-008)のため符号の扱いは変えていない。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(743件全通過、振込明細の抽出を検証する新規テスト1件)/`npx next build` すべて成功。スキーマ変更が無いため `scripts/verify-schema.sh` 等は対象外。**未検証**:このセッションには実際の Anthropic API キーも本人のログイン手段も無いため、①提示されたスクリーンショットの実物をモデルへ渡した実際の認識結果、②本番データでの取り込み・表示、は確認できていない。テストはモデルの出力を固定した状態での後段処理のみを検証している。 | 2026-09-23 |
| P10-42 | 本人からの不具合報告「起動が遅い。とりあえずレシートはすぐ登録したい。どんなけ読み込んでてもカメラアイコンはすぐ表示して。あと、カメラアイコン押した時に写真を撮るか選ぶか選択できるようにして。起動が遅いのはネックすぎる」(ADR-039)。**原因(表示の遅さ)**:`app/(app)/layout.tsx` のボトムナビは `createPortal(<BottomBar />, document.body)` で描画しており、`isClient`(ハイドレーション完了で true になる)が false の間は `null` を返す——つまりページの読み込みが遅いとJSのダウンロード・ハイドレーション完了までカメラアイコンを含むボトムナビ自体がHTMLに存在しなかった。**対応**:`isClient` が false の間はポータルを使わずその場に `<BottomBar />` を直接描画するよう変更(`position: fixed` の見た目は変わらない)。`<label><input type="file">` はネイティブHTML要素でJS非依存のため、サーバーレンダーされたHTMLに含まれてさえいればハイドレーション未完了でもタップして機能する。ハイドレーション後は従来どおりポータルに戻し、P10-20が対策した「祖先のbackdrop-filterがposition:fixedの基準になる」不具合の防止は維持(root layoutの祖先に該当スタイルが無いことを確認済み)。**対応(撮る/選ぶの選択)**:`src/components/ui/fab.tsx` の `<input type="file">` から `capture="environment"` を外した。これを付けると多くのモバイルブラウザは選択肢を出さずカメラを直接開くが、外すとOS標準の「写真を撮る/ライブラリから選ぶ」というネイティブな選択肢がその場に出る(新しいUIは作らず、`/transactions/receipt` 画面が既に持っていた「撮る」「選ぶ」2ボタンと同じ仕組みを1つのアイコンに集約)。以前の「タップした瞬間にカメラアプリを開く」という方針(P10-4/ADR-021)からの転換。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(743件全通過、UIの表示・属性のみの変更のためテスト変更なし)/`npx next build` すべて成功。**未検証**:このセッションには実機・本人のログイン手段が無いため、①実際に低速な回線・端末でカメラアイコンがハイドレーション前から表示・操作できること、②`capture` を外した後の実機での選択肢の見た目・挙動、は確認できていない。 | 2026-09-23 |
| P10-43 | 本人発案「家計簿のところからレシートの詳細が見れないのはナンセンス。明細のところからは見れるけど、家計簿の方からも見れるようにしてほしいし、レシート登録もどちらかというと、家計簿の方からの責務だと感じました」(ADR-040)。レシートの品目詳細(P10-31/P10-40)は`/transactions`の明細行を開いたときにしか見えず、`/spending`(家計簿)には無かった。表示形式・登録動線を確認したところ「カテゴリ別の内訳を押したら使った一覧が見れて、さらにそれを見るとレシートの詳細が見れる、画像は表示しなくて良い」「(登録は)まず両方に残してもいい」との回答。**対応**:新しい専用ページは作らず、`/spending`のカテゴリ別内訳(`CategoryBreakdownChart`)の各行を押すとそのカテゴリの当月明細一覧が開き、その明細を押すとレシートの品目詳細(画像は扱わない)が見える2段階の開閉にした。P10-32が排除した「今月明細の再掲」とは別物(既定で畳んであり、カテゴリで絞った上での深掘り経路)。**品目の表示・未登録時の再登録ボタン(P10-40)を共通部品化**:`split-editor.tsx`だけに書かれていたロジックを`app/(app)/transactions/receipt-items-panel.tsx`(新規、制御コンポーネント)へ切り出し、`/transactions`の明細行と`/spending`のカテゴリ内訳の両方から使う(ADR-033、同じ考慮を複数箇所に書かない)。**データ**:`features/spending/ledger-types.ts`の`LedgerTransaction`に`categoryId`・`accountId`・`paymentMethod`を追加(レシート添付処理と、カテゴリ行との対応付けに必要)。品目・生活費の小分類は`/transactions`と同じ`listReceiptItemsForTransactionIds()`・`listExpenseSubtypesForTransactionIds()`を当月分だけまとめて読む。**`/reports/ai`も同時対応**:同じ`CategoryBreakdownChart`を再利用している既存のAIレポート画面にも同じデータを渡し、深掘りできる/できないの差が出ないようにした。撮影による新規取り込み(`/transactions/receipt`)自体は変更していない(撮影FABがどの画面からも開けるため、実質的に元から家計簿側にも入口があった)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --write .`/`npx vitest run`(742件全通過、UIとオーケストレーションのみの変更のためユニットテスト追加なし)/`npx next build` すべて成功。**未検証**:このセッションには本番 Supabase の管理API・実際のレシート画像が無いため、実機での表示・登録の一連の動作は確認できていない。 | 2026-09-23 |
| P10-44 | 本人発案「レシート読み込んだあと、すぐに編集できるようにして。再度写真撮るのは手間すぎる」(ADR-041)。品目の手入力修正(ADR-035)は「品目の合計が明細額と一致しない(mismatched)」ときにしか出せず、合計は合っていても品目名・金額の読み間違いは直せなかった。さらにその編集フォーム自体`split-editor.tsx`だけにあったため、P10-43で`/spending`にも広げた`ReceiptItemsPanel`には編集手段が無く、読み取りを誤ると撮り直す以外に直しようが無かった。**対応**:編集フォーム(品名・金額・カテゴリ)を`receipt-items-panel.tsx`へ一本化し、`/transactions`・`/spending`どちらからも同じ編集ができるようにした(ADR-033・ADR-040と同じ方針)。**条件を緩和**:「mismatchedのときだけ」の制限を外し、品目が1件でもあれば常に「編集する」から直せる。合計不一致の警告自体は情報として残す(編集の条件にはしない)。**読み取り直後、自動で編集フォームを開く**:`attachReceipt()`の保存が終わった時点でそのまま編集状態にし、追加の操作(ボタン押下・再度の写真撮影)を挟まない。**`/transactions`側の行閉じたままの警告は維持**:ADR-035の「行を開かなくても気づける」という視認性は保ちつつ、押すと行を開くだけにし(`setOpen(true)`)、実際の編集は共通パネルに任せる。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --write .`/`npx vitest run`(742件全通過、UIとオーケストレーションのみの変更のためユニットテスト追加なし)/`npx next build` すべて成功。**未検証**:このセッションには本番 Supabase の管理API・実際のレシート画像が無いため、実機での編集動作は確認できていない。 | 2026-09-23 |
| P10-45 | 本人発案「機能はいっぱいついてきたけど動線がちょっと複雑すぎる…その他にある機能については別にいいんだけれども、今メインのところにある機能については動線がシンプルである必要がある…操作を重複させることって大事だと思ってて…左フリックで編集とか長押ししたらレシートの内容を見れるとか」(ADR-042)。ボトムナビ4画面を監査したところ、複雑さの実体は`TransactionRowWithSplit`(`split-editor.tsx`)だったが、並行するP10-43/44(ADR-040/041)が`ReceiptItemsPanel`への切り出し・編集フォームの「編集する」ボタンへの一本化を既に済ませていたため、このADRのスコープはジェスチャーの追加のみに絞った。**ジェスチャー**:AskUserQuestionで確認した割当てどおり、明細行を`SwipeableRow`(新規、`src/components/ui/swipeable-row.tsx`)で包み、左フリックはカテゴリ変更に直行(`open`+`categoryFormOpen`を同時に立てる)、長押しは編集不可のその場プレビュー(新状態`previewOpen`、閲覧専用)を開く。タップ(行の開閉)は従来通り内側の`<button onClick>`が処理する。`SwipeableRow`は`pull-to-refresh.tsx`と同じ`{passive:false}`のネイティブtouchイベント+ref/state使い分けの流儀。**共通化**:長押しプレビューに使う下からのシート(Liquid Glass)は`more-menu.tsx`が既に持っていたのと同じ見た目・挙動だったため、2箇所目が生まれる前に`BottomSheet`(新規、`src/components/ui/bottom-sheet.tsx`)へ切り出し、`more-menu.tsx`もこちらを使うよう書き換えた(ADR-033の重複排除、本人の「操作を重複させることが大事」をUIの共通化にも適用)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(743件全通過、UIの開閉構造・ジェスチャーのみの変更のためユニットテスト追加なし)/`npx next build` すべて成功。**未検証**:このセッションには実機・本人のログイン手段が無いため、実機でのタッチ操作(左フリックの閾値の感触、長押しの誤発火の有無)は確認できていない。 | 2026-09-23 |
| P10-46 | 本人発案「カレンダー追加。家計簿のトップはカレンダーその下に詳細カレンダー押したら何に使ったかすぐ見れるように編集できるように」(ADR-043)。`/spending`には今月使った額・カテゴリ別内訳・着地予測はあったが、月内のどの日に使ったかという時間軸の一覧性が無かった。**対応**:`SpendingCalendar`(新規、`app/(app)/spending/calendar.tsx`)をページの一番上に配置。月の全日をグリッドで並べ、日ごとの支出額を小さく添える(`ledger.transactions`をクライアント側で日付ごとにグルーピングするだけで新しいクエリは無し)。日を押すとカード直下にその日の明細一覧が展開され(既定選択は今日)、各明細は`updateTransactionAction()`(`split-editor.tsx`と同じ経路)でカテゴリだけをその場で直せる。分割・レシート品目の編集はスコープ外(`/transactions`・`CategoryBreakdownChart`の深掘りに既にある、ADR-033の重複排除)。**新規ユーティリティ**:`weekdayOf()`(`src/lib/date.ts`)をカレンダーグリッドの曜日合わせ用に追加(ADR-015の規約に沿い`Date.UTC`ベース)。**保存後の整合**:カテゴリを変えるとカテゴリ別内訳も本来ずれるため、`pull-to-refresh.tsx`と同じ理由で保存直後に`router.refresh()`。`updateTransactionAction()`に`revalidatePath('/spending')`を追加。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(744件全通過、`weekdayOf()`の新規テスト1件)/`npx next build` すべて成功。**未検証**:このセッションには実機・本人のログイン手段が無いため、実機でのカレンダー表示・タップ操作は確認できていない。 | 2026-09-23 |
| P10-47 | P10-45(ADR-042)への本人からの不具合報告「長押しでプレビューできるんだけど選択されてしまうバグってる」(実機スクリーンショット付き)。**原因**:`SwipeableRow`は`setTimeout(500ms)`で長押しを自前検出する独自実装だが、その待機中にiOS Safari自身の「長押しでテキスト選択」処理を止めていなかった。明細行の`<button>`を長押しすると、ブラウザの選択・コールアウト(「コピー/調べる/翻訳/Webを検索」)がこの自前の長押し検出とほぼ同じタイミングで割り込み、`BottomSheet`のプレビューが開いてもテキストが選択されたまま重なって見えていた。**対応**:`app/globals.css`に`button { -webkit-touch-callout: none; -webkit-user-select: none; user-select: none; }`を追加。ボタンはテキストのコピー元ではなく操作対象であるため、明細行に限らずアプリ全体の`<button>`から選択・コールアウトそのものを止める判断にした(P8-3の`input, select, textarea { font-size: 16px }`と同じ、iOS Safari固有の挙動をグローバルCSSで直すパターンを踏襲)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`/`npx next build`すべて成功。**未検証**:実機での長押し確認はこのセッションの手段では行えない。ただし`-webkit-user-select`/`-webkit-touch-callout`は標準的なiOS Safari対応であり、副作用(ボタン内テキストが選択できなくなること)はボタンの用途上問題にならないと判断した。 | 2026-09-23 |
| P10-48 | P10-46(ADR-043)への実機スクリーンショット付きフィードバック(ADR-044)。「これは一番上に持っていって」(`SummaryCard`を丸で囲みカード上部への矢印)、「カレンダーに紐づくやつもカテゴリーごとに分けてレシート表示して。今カテゴリーが弱いな生活費ってなっちゃう全部」。**並び順**:`SummaryCard`を`SpendingCalendar`より前に戻した。**日別明細のグルーピング**:カレンダーの選択日の明細一覧を、フラットな時系列リストからカテゴリ名+小計の見出しごとにまとめる形に変更(`groupByCategory()`、支出額の大きい順)。**レシート表示**:各明細を開くと`CategoryBreakdownChart`の深掘り(ADR-040/041)と同じ`ReceiptItemsPanel`を出し、レシートの品目・生活費の小分類・未登録なら登録ボタンまで見えるようにした(新しい部品は作らず既存を再利用、ADR-033)。ADR-043で「3箇所目の重複になる」として却下していた選択肢を、実際の要望に合わせて覆した。カテゴリの変更セレクトは維持し、選び直した値に応じて小分類の表示条件(`categoryCode`)も追従するようにした。**型の共有**:`category-breakdown-chart.tsx`の`DrilldownTransaction`に`categoryId`・`categoryName`を追加し、`page.tsx`・`reports/ai/page.tsx`の両方で1回だけ組み立てた`drilldownTransactions`を`CategoryBreakdownChart`・`SpendingCalendar`の両方に渡す。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(744件全通過、UIの並び替え・表示のみの変更のためユニットテスト追加なし)/`npx next build` すべて成功。**未検証**:このセッションには実機・本人のログイン手段が無いため、実機での表示・操作は確認できていない。 | 2026-09-23 |
| P10-49 | 本人発案「編集が微妙。既に登録したやつも編集すぐできるようにしたい。編集押したら編集ダイアログ出る。さらに再度レシート読み込みボタンを編集エディタに設ける。あと確認待ちのやつあるけどあれもうなくして。もうAIが勝手にやっていい。それが気に食わんかったら編集する、だから編集は手軽にできるような形にしといて欲しい」(ADR-045)。2つの独立した対応。**①レシート編集をダイアログへ一本化**:`ReceiptItemsPanel`(P10-43/44、ADR-040/041)のインライン編集フォームを、`split-editor.tsx`の長押しプレビュー・`more-menu.tsx`が既に使っている共通の`BottomSheet`(P10-45、ADR-042)を使ったダイアログへ作り替えた。ダイアログの中に「レシートを読み込む/読み込み直す」ボタンを常設し、品目の有無に関わらずいつでも呼べる——以前は「品目が無いとき」専用の別ボタンだった。読み込み直しは即保存せず編集中の行を置き換えるだけにし(見直してから「保存」を押す1つの流れ)、以前の「読み取り直後に自動保存してから編集を開く」という二度書きをやめた。**②確認待ちキューを撤廃**:AI分類の確信度閾値判定(`applyConfidenceThreshold`→`toAppliedClassification`に改名・簡素化、`src/features/classification/ai.ts`)とルール不一致時(`import-pipeline.ts`)のどちらも、常に`review_status='auto_ok'`を返すようにした。`/transactions/review`(確認待ちキュー画面一式)・`/transactions`の「確認待ちが N件」バナー・明細行の「確認待ち」バッジを削除。**学習ルール作成を引き継ぐ**:確認待ちキューの確定時だけ学習ルールを作っていた`createLearnedRule()`の呼び出し元が無くなる(誤爆検知P5-2の入力が絶える)ため、`updateTransactionAction()`(本人が明細のカテゴリを直すたびに呼ばれる、唯一の残る呼び出し元)へ引き継いだ。失敗しても明細の更新自体は失敗させない。**未使用になった設定を削除**:`app_settings.classification_confidence_threshold`を読む`AppSettings.classificationConfidenceThreshold`は参照が無くなったため型・クエリから削除(DBの列自体は残す、スキーマ変更なし)。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --write .`/`npx vitest run`(742件全通過、確認待ち関連のテストを新しい挙動に更新)/`npx next build` すべて成功。**未検証**:このセッションには本番 Supabase の管理API・実際のレシート画像・本人のログイン手段が無いため、実機でのダイアログ操作・確認待ち撤廃後の分類挙動は確認できていない。 | 2026-09-23 |
| P10-50 | 本人からの不具合報告(スクリーンショット付き)「これよく出てくる」——ホームを開いた最初のタイミングで、Vercelのプラットフォーム側の汎用クラッシュ画面("This page couldn't load / A server error occurred")が頻繁に出る(ADR-046)。**原因**:このアプリには`error.tsx`が1つも無かった。ホーム(`/`)は`loadHomeSummary()`・`getCheckinStreak()`・`loadCategoryMonthDetail()`など複数のSupabase呼び出しをawaitしており、どれか1つが一時的な接続断・タイムアウトで失敗すると、Next.jsのError Boundaryがどこにも無いため例外がそのままVercelのプラットフォーム層まで突き抜け、アプリの見た目と無関係な汎用エラー画面が出ていた(このセッションにはVercelの関数ログへのアクセス手段が無いため、失敗の発生源そのものの特定はできていない)。**対応**:`app/(app)/error.tsx`(新規)を追加。ホーム・家計簿・明細・給料日の主要4タブすべてを含む`(app)`ルートグループの直下に置くことで、`layout.tsx`(ボトムナビ)はそのまま描画され続けたまま、例外発生時はアプリのデザインに沿った「読み込めませんでした/もう一度試す」カードに差し替わるようにした。根本の一時的な接続断自体への個別対応(リトライ・タイムアウト調整等)はスコープ外——まずは「アプリらしい見た目でその場からやり直せる」状態にする。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(742件全通過、UIのError Boundaryのみの追加のためユニットテスト追加なし)/`npx next build` すべて成功。**未検証**:このセッションには実機・本人のログイン手段・Vercelの関数ログへのアクセスが無いため、実機での再現・表示確認はできていない。 | 2026-09-23 |
| P10-51 | 本人発案「ていうかそこの部分自由にレイアウト変えれるようにしたい」(ADR-047)。並び替えのたびに本人からの指摘(手書き注釈付きスクリーンショット含む、P10-46/48)→コード変更という往復が続いていたため、本人が直接操作できるようにした。並び替えの操作方法はAskUserQuestionで確認し「長押し+ドラッグ」が選ばれた。**対応**:`ReorderableCards`(新規、`app/(app)/spending/reorderable-cards.tsx`)で家計簿の6枚のカード(今月使った額・カレンダー・予測・AI診断・カテゴリ別内訳・ちりつも)を並び替え可能にした。「並び替え」ボタンを押した編集モードの間だけ、各カードの右上に専用のつまみ(`MdDragIndicator`)が出て、そこだけを長押し+ドラッグの起点にする(カード本体はそれ自体が押せる要素を大量に持つため、どこでもドラッグ開始にすると普段の操作と衝突する)。**アルゴリズム**:ドラッグ中は実際の並び順を変えず、指の位置から「今離したらどこに入るか」だけを計算し、間のカードは transform で場所を空ける。指を離した瞬間に初めて並び順を確定・保存する(位置の再計算・ズレが起きない設計)。**保存先はlocalStorage**:`app_settings`(ADR-014、業務パラメータの置き場)とは性質が違うため、新しいテーブル・マイグレーションは増やさなかった。**新しいlintルールへの対応**:`react-hooks/set-state-in-effect`・`react-hooks/refs`(React Compiler向け)に抵触しないよう、localStorageの読み込みは`useEffect`ではなく既存の`useIsClient()`+レンダー中の比較(`more-menu.tsx`と同じ書き方)で行い、ドラッグ中に使う値はrefではなくstateに持たせた。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`(742件全通過、UIの並び替えのみの変更のためユニットテスト追加なし)/`npx next build` すべて成功。**未検証**:このセッションには実機・本人のログイン手段が無いため、実機でのドラッグ操作(長押しの感触、追従・場所を空ける動きの滑らかさ)は確認できていない。タッチ操作専用、オートスクロールは未実装。 | 2026-09-23 |
| P10-52 | 本人発案(スクリーンショット付き)「今金額と日付が一切編集できない。別のダイアログ表示していいしむしろ戻れるなら画面遷移してもいいのでもっと編集しやすくして」(ADR-048)。`/transactions`の明細行はカテゴリ変更・分割・レシート品目編集まで揃っていたが、`updateTransaction()`(`src/features/transactions/store.ts`)は元々category_idしか更新できず、金額・日付の入力ミスを直す手段が無かった。**対応**:新しい画面・ダイアログは作らず、既存の「カテゴリを変更する」フォーム(`split-editor.tsx`のmode='simple')に`<input type="date">`・金額入力(`payment-history.tsx`と同じ見た目)を追加し、ボタンを「金額・日付・カテゴリを編集する」に改めた。金額は分割編集と同じく正の大きさだけを入力させ、元の収入/支出の符号(ADR-008)はそのまま保つ。`updateTransaction()`/`updateTransactionAction()`は破壊的に変えず、任意の第4引数(`patch`。ADR-045で追加された第3引数`description`の次)として追加——家計簿カレンダー(`calendar.tsx`)は既存どおり3引数のままカテゴリのみを直す。`assertYen()`/`assertDateOnly()`で検証し、`describeUserError()`でエラー文言を統一。**検証**:`npx tsc --noEmit`/`npx eslint .`/`npx prettier --check .`/`npx vitest run`/`npx next build`すべて成功。静的HTMLモックアップをPlaywrightで360px幅・light/dark両方スクリーンショットし、横スクロール無し(NFR-07)で収まることを確認。**未検証**:実機での入力・保存後の反映はこのセッションの手段では確認できていない。 | 2026-09-23 |

---

## 本人向け設定手順の書き方(ルール)

本人発案(「私の認知負荷を下げる説明をして。手順には必ずURLを記載し脳死で設定できるような仕組みで」)。
今後このファイル・チャットで本人に設定作業を頼むときは、必ず次の形式に従う。

1. **各手順の先頭に、実際にブラウザで開く URL を書く**。「〜の設定画面を開く」のような曖昧な指示だけで終わらせない。固定 URL が無い操作(例:Discord の自分のサーバー内の操作)は、その旨を明記する
2. **手順は「開く→押す→貼る→保存する」だけの番号付きリストにする**。本人が判断する箇所(「適切な値を入力」のような曖昧な指示)を作らない。入力する値は必ずコードブロックでそのままコピペできる形にする
3. **各手順の末尾に、完了すると何が起きるかを一文で書く**(やった意味が本人に伝わるように)
4. 本番の実値(プロジェクトURL・プロジェクトIDなど)が分かっているものはプレースホルダにせず実値を書く。このプロジェクトの固定値は以下のとおり:
   - アプリ本体:`https://git-study-lemon.vercel.app`
   - GitHub リポジトリ:`https://github.com/toshikikotani/git-study`
   - Vercel プロジェクト:`https://vercel.com/toshikikotani/git-study`
   - Supabase プロジェクト:`https://supabase.com/dashboard/project/ajeezsnwzhjauhukaxrg`

## 本人への確認待ち

実装を先に進めるうえで、いずれ必要になるもの。急ぎ順。手順は上記ルールに従って書く。

1. **Vercel の `CRON_SECRET` を GitHub Secrets と同じ値に直す**(M0-6、B-8、**最優先**)。**2026-09-15 に再調査**:GitHub Secrets 側(`CRON_SECRET`/`APP_BASE_URL`)は設定済みと確認できたが、Vercel 側の `CRON_SECRET` の値が一致していない(全ジョブが 401「認証情報が正しくありません」で失敗し続けている、直近23:22の実行でも失敗)。設定されるまでアラート検知(FR-20〜23)・朝配信(FR-30)・Gmail自動取り込みは本番で一度も実行されない
2. **Discord Webhook**(B-3):サーバーとチャンネルを1つ作り、Webhook URL を発行
3. **LINE のチャネルアクセストークン・userId**(B-6):Discord と両方でも片方だけでもよい
4. **LINE経由レシート受信のセットアップ**(P10-5、B-11、3が先に必要):チャネルシークレット・取り込み先口座IDを設定し、Webhook URL を有効化
5. **Google連携のセットアップ**(P9-5、B-9):Google Cloud ConsoleでOAuthクライアントを作成し発行、`/settings/google` で同意
6. **Gmail 自動取り込みの有効化**(M2-7c):`/accounts` で実口座を作った後、その口座IDを環境変数に設定し `app_settings.gmail_enabled` を true にする。**2026-09-15 確認:`accounts` テーブルはまだ0件**(口座が1つも登録されていない)ため、CSV・レシート・Gmailのどの取り込み経路も現状はまだ使えない
7. **負債の棚卸し**(B-1):借入先ごとの残高・金利・最低返済額・返済日。`/debts` にシードの3件が表示されているので、「編集」から直接正確な値に直せる。**2026-09-15 確認:3件とも `is_estimated: true` のまま**(未着手)
8. **明細 CSV 1ヶ月分**(B-2):利用中の銀行・カードのもの。フォーマットが判明するとアダプタを実データで検証できる

（`supabase/apply-pending.sql` の本番適用(B-4/B-5/B-7/B-10/B-12/B-13/B-14/B-15/B-16)は2026-09-22に本人が実行済み、P10-30参照)
