import { Placeholder } from '@/components/ui/placeholder';

export default function TransactionsPage() {
  return (
    <Placeholder
      title="明細"
      taskId="M2-2 / M2-5"
      description="CSV 取り込みと自動分類、確信度の低い明細の確認キュー(FR-10〜13)。"
    />
  );
}
